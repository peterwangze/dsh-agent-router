# REL-012 E-4 发布审查 **R1（扩范围重审）** — v0.4.6 发布就绪独立审查（Release Reviewer → Coordinator）

**审查结论：NEEDS_CHANGE**
（BLOCKING=1（version 面）/ WARNING=5 / SUGGESTION=3；候选文件的**事实面**（三分账/破坏性披露/CI 口径/证据存在性）经独立复核**全部属实**，阻断项仅出在「版本位与重划后范围的匹配性」及其连带的版本权威性缺口。NEEDS_CHANGE 非终态，M7.4 返工后须由同一 Reviewer 复审 R2。）

- **round**: R1
- **前轮引用**: `REVIEW-REL-012-R0`（`review-REL-012-R0.md` / `review-REL-012-R0-input.md`）——R0 结论 **APPROVED_WITH_NOTES / unresolved_blockers=0**，但 R0 审定对象是**旧范围**（`git rev-list --count v0.4.5..HEAD` = 2，仅 FIX-032 单点）。本轮对象 = **范围重划后的候选**（HEAD `01e0446`，v0.4.5..HEAD = 41 提交，承载面 = FIX-032 + ARCH-004 兼容性适配链）。**R0 的版本位裁定（「PATCH 合规」）不构成本轮的任何依据**——其事实前提（单点 bug fix、无新功能面）已被范围重划推翻。
- **审查对象**: v0.4.6 发布候选 @ HEAD `01e0446`（`package.json` / `CHANGELOG.md` v0.4.6 节 L6-59 / `README.md` / `.github/workflows/ci.yml` / `.governance/` 三件套与审查记录），非代码 diff（ARCH-004 链各批代码审查已分别机录，本轮核验其**存在性、范围覆盖与用户面表述准确性**）。

---

## 一、审查方式与事实红线（诚实披露）

- **只读**：全部事实经 read / grep / glob 与 `.git` 对象层直读（`.git/refs/**`、`.git/logs/HEAD`）。**未执行 git 命令**——提交范围与三分账由**自实现的只读对象遍历**（zlib 解压 loose object → commit/tree 解析 → 逐提交 tree 差异）独立算出；**未修改任何文件**（Write 仅用于本报告）、未建子 agent、未与用户交互、**未操作任何真实环境**。
- **判定分级**：①「直读」= 本审查员可直接复查的文件/行级事实；②「机录采信」= 治理记录载明且经交叉印证、审查员只读约束下未独立复跑的事实（逐处标注）；③「未验证」= 本报告明确标注未做（回滚演习、CI 首跑）。

---

## 二、独立复核①：范围账目（git 对象层实算，不采信 prompt 与治理记录）

| 事实 | 本审查员实算 | 方法锚 |
|---|---|---|
| HEAD | `01e04460c6d07e75a5f64eb0844502be17a55cc7` ✓（与 prompt 一致） | `.git/refs/heads/main` 直读 |
| tag v0.4.5 | annotated 对象 `a993dcea…`，**解引用得 `tag v0.4.5 / object 79d67c7e… / type commit`** ✓（R0 未解引用，本轮补齐） | 对象层直读 |
| **`git rev-list --count v0.4.5..HEAD`** | **= 41** ✓（与 prompt「41」一致）；HEAD 可达 406，tag 目标可达 365，差集 41 | 自实现祖先遍历（406−365） |
| tag v0.4.6 | **不存在** ✓（无提前打标；`.git/refs/tags` 全量列举止于 v0.4.5） | `.git/refs/tags` 直读 |
| origin/main | `969531b…` ✓（未推送面 = 范围前 41 笔中除 `969531b` 外的全部；凭据恢复与 `push --dry-run` 属机录） | `.git/refs/remotes/origin/main` 直读 |

**三分账机器复核（41 笔逐提交 tree diff，按「仅 `.governance/` 改动计治理、其余计产品/发布面」规则）**：

| 类 | 本审查员实算 | CHANGELOG 声明（L58） | 一致 |
|---|---|---|---|
| 产品提交 | **16** | 16（ARCH-004 链 15 + FIX-032 `640e2e4`） | ✓ |
| 治理提交 | **20** | 20（20 个 SHA 逐一核对，**全部命中**） | ✓ |
| E-1 提交 | **5**（`65201ad` `4d7889b` `19f9a94` `0ef1844` `01e0446`） | 4（冻结时点口径，不含 `01e0446` 自身） | ✓（差 1 = 自指，见裁定④） |
| 合计 | **41** | 40（冻结时点 `0ef1844`） | ✓（自洽） |

**冻结时点自洽性验证（关键）**：剔除 `01e0446`（账目校正提交自身）后 = **40 笔，且恰为 16 + 20 + 4** —— 与 CHANGELOG L58 的「40 = 16 + 20 + 4」**逐项相等**。

**引证 SHA 与审查记录存在性**：
- v0.4.6 节内出现的 **41 个 7-hex token**：40 个为范围内提交（与 L58 列举逐一命中），1 个为 tag 引用 `79d67c7`（范围定义面，合法）——**零虚构 SHA、零范围外引证**；`dc8b8a1` 等 15 笔本版定稿后落入范围者虽未写入 L58 计数，但已随「范围以 tag 时点实采为准」兜底条款覆盖。
- CHANGELOG 声明的 **16 条审查记录文件全部存在**（REVIEW-EVO-018~024 ×7 / FIX-033~036 ×4 / FIX-037 R0·R1·R2 ×3 / FIX-032 R0 / REL-012 R0），且 `-input.md` 完整报告均在（15.2KB~37.4KB，非空壳）。

---

## 三、7 项裁定项逐项结论（本轮 MUST）

### 裁定① 版本位 PATCH vs MINOR → **不成立：不可裁定为 PATCH；本项为 BLOCKING（F-1）**

- **可复核事实（逐项直读）**：
  - 新增能力面：`lib/host-abi/` 全新分域模块（8 文件，`package.json` files 白名单新增 `lib/host-abi/`）；新增公开 RPC `router/hostFaceDiagnostics`（`lib/schemas.js:427` `hostFaceDiagnosticsResult` codec + `lib/service.js:3459` 类方法）；新增用户可见设置页健康面板与徽章（宿主版本 + 面健康 + 诊断环形）。
  - 破坏性面：支持面**终止**（<0.1.5-rc.2 宿主不再受支持，apiProxy 旧面回落路径删除，`lib/host-abi/llm-selection.js` 单形态化）。
  - 依赖声明面权威变更：peerDeps 8 项 `^0.1.0-rc.8` → `^0.1.5-rc.2`；deps 3 项 `^0.1.0-rc.6` → `^0.1.5-rc.2`；`dsh.client.inject` 删 1 行（**字段级 diff 实测，见裁定②**）。
- **裁定理由**：0.x 阶段 PATCH 位的本仓惯例内涵 = 「bug fix / 适配 / 收口」（v0.4.3 `7965c9d`、v0.4.4 `f0438f9`、FIX-032 均为此类）。本版范围含**新增能力面 + 支持面收窄**，已越出该内涵；且仓内已有**成文先例直接支持 MINOR 判法**——CHANGELOG v0.4.2 版本说明原文立版：「MINOR：完整新能力面——新配置节 + 新设置卡片 + 新事件行为，向后兼容」。CHANGELOG 自身在「破坏性变更·是否算 breaking」结论行已承认「按 semver 严格口径，第 2 项属**支持面收窄**——对『运行在低于 0.1.5-rc.2 宿主上』的场景构成 **breaking**」：**既自认 breaking，则 0.4.x 位下的正确递进即 MINOR（0.5.0），PATCH 与自认事实互相矛盾**。
- **是否阻断**：**阻断 E-5**。前置 PATCH 的授权前提（用户 2026-09-10「推送**小**版本」裁决）成立于**旧范围**（FIX-032 单点，当时实采 2 笔）；范围重划的授权（用户 2026-09-12「发布版本承载这次适配修改」）**未重述版本位**，故不存在可裁定 PATCH 的**后置权威**。CHANGELOG L57 已自认「版本位与重划后范围的匹配性属发布链（E-4 / E-5 与用户）裁决事项」——**本 R1 即为该裁决时点，不能回避**。
- **修复路径（二选一，均须落痕）**：
  - **(a) 建议路径**：改 MINOR → `0.5.0`。同步面明确且可控：`package.json` version 1 行；`README.md` 8 处（L7/L41/L46/L53/L82/L86/L91/L98，其中 L41/L86 各含 3 处）；CHANGELOG 版本号行 + 摘要节；plan-tracker REL-012 行「目标版本」。因 **tag/push/Release 均未发生**，不存在已发布版本号改写问题。返工后须**重跑 E-2 门控**（`package.json` 属被测对象，snapshot 套件读取 version/inject/peerDeps 面）并**重跑 E-3 隔离冷装**（tarball 名与包内 version 随之变化），R1 结论随之吊销重审（R2）。
  - **(b) 保留 0.4.6 的唯一合法路径**：**用户明示裁决**「以 PATCH 位承载本重划范围」并记入 `decision-log`（DEC），CHANGELOG L57 同步引用该 DEC 号。**审查员不得代替用户作此裁决**（发布决策属用户门禁）。

### 裁定② 破坏性变更披露准确性 → **通过（属实）**

- **「依赖声明变更（记录性）」逐字核对（字段级 diff：HEAD vs v0.4.5 tree）**：`dsh.client.inject` 由 `[dsh-client-runtime, dsh-client-ui-settings, dsh-client-locale, dsh-api-remotes]`（4）变为 3 项——删除行属实；8 项 peerDeps 由 `^0.1.0-rc.8` 统一为 `^0.1.5-rc.2`——**逐项实测一致**；dsh 包 deps 3 项（`dsh-llm` / `dsh-tools` / `dsh-typert-protocol`）由 `^0.1.0-rc.6` 统一为 `^0.1.5-rc.2`——实测一致（deps 总 6 项，与「3 项」措辞不冲突）；全仓 grep `dsh-client-runtime` 在**声明面与代码面零残留**（仅测试守卫的「已删除断言」与历史文档/注释）。
- **「低于基线不在支持面」披露充分性**：CHANGELOG L43 明确「**低于 0.1.5-rc.2 的旧宿主不在本版支持面**，不承诺回退；建议先把宿主升到实测基线再升级本插件」，且在**「破坏性变更」独立小节**（非仅埋在已知行为里）加黑体结论行——位置与强度充分，符合 no-overclaim 纪律。README L138-141「宿主兼容性（实测基线）」同步给出基线数值与权威防护指针。
- **残留不一致（转 F-6，P2 非阻断）**：README 安装前置（L30、L130）仍写「DSH ≥ 0.1.1-rc.2」，与本版硬要求 0.1.5-rc.2 存在**口径落差**——真实支持面不得由用户从「受支持与实测基线见下方」自行推导。

### 裁定③ 回滚方案 → **部分通过（方案可执行、数据面无新增边界；回滚组合未验证 + 校验步骤缺失 → F-5，P2）**

- **可执行性：通过**。通道存在且步骤具体：`README.md` L60-65（`dsh plugin --profile web remove dsh-agent-router` / `update` 命令矩阵）+ L39-56/L86-98（离线 `file:` 装前版发行包：下载 tarball → `tar -xzf` → `dsh plugin add <解压目录> --profile web`）。前版资产可用性有实证（机录 EV-169：v0.4.5 tarball HTTP 200 + 尺寸逐字）。
- **数据安全步骤：通过（沿用 v0.4.5 已披露口径）**。CHANGELOG L51 载明：v2 统计行（含预设作用域 scope 行）无法被 v0.4.4 及更早版本读取，**最坏情形不可逆清除**；降级前 MUST 备份 `DSH_HOME/dsh-agent-router/stats/`；并声明「**本版不改变该边界**」。
- **本版是否引入新的降级边界 → 「无新增存储/回滚边界」有构造性证据（本轮新增独立核验，R0 未做）**：
  1. `lib/schemas.js` 与 v0.4.5 **行级 diff 实测：新增 35 行、删除/修改 0 行**——全部新增为 ARCH-004 新增 RPC 的 wire codec（`faceHealthCodec` / `hostDiagEntryCodec` / `hostFaceDiagnosticsResult`）；`routerSchema` 配置节**零变更**（键序与形状一致，L266-302 直读），且未知配置键经 schemastery 透传（L278-279 注释成文）→ **配置面回滚零风险**。
  2. `lib/host-abi/` 全域 grep：**零文件写/零 `DSH_HOME`/零存储路径**（`writeFile|appendFile|mkdir|rmSync|unlink|DSH_HOME|createWriteStream` 全部 0 命中）→ 诊断环形 `noteHostDiag` 与 FaceHealth 快照均为**进程内纯数据**，不落盘、不产生新格式；v2 统计行边界仍唯一来源于 v0.4.5。
  3. 新增 `files` 条目 `lib/host-abi/` 只影响 0.4.6 自身包内容，回滚卸载即消失。
  → **结论：本版不产生新的数据/存储降级边界；唯一不可逆面（v2 行）与 v0.4.5 同源、披露未变。**
- **两项缺口（F-5，P2，非阻断）**：
  - **(i) 回滚组合未验证**：E-2/E-3 只验证「新插件 × 新宿主」方向；**「v0.4.6 → 回滚到 v0.4.5、宿主已是 0.1.5-rc.2」这条实际降级路径零证据**。风险点具体：v0.4.5 仍保留 `apiProxy` 形态判定链（本版已删），其在 0.1.5-rc.2 宿主上的行为**未经验证**（v0.4.5 CHANGELOG 只声明其面向 ≥0.1.2-rc.1 的「双形态」，未声明对 0.1.5-rc.2 的支持）。本版实现了**正向**兼容（旧插件形态→新宿主适配）的守卫，但**反向组合无任何断言**。
  - **(ii) 回滚后无一致性校验步骤**：README 回滚仅到「重装前版」为止，未含「回滚后确认插件已加载 + 统计可读（或已备份）」的校验动作，与「回滚方案步骤可执行、数据一致性验证」要求存在落差。
  - **处置建议**：E-5 发布 notes 的回滚段 MUST 含三项——① 命令步骤（remove + 从 v0.4.5 Release `file:` 装）；② **数据安全步骤**（先备份 `DSH_HOME/dsh-agent-router/stats/`，或明确接受 v2 行丢失）；③ **明确限定语**：回滚插件本体不隐含宿主降级（用户环境 0.1.5-rc.2 且 npx cache 单向，DEC-031 BR-04），并如实标注该组合**未验证**（禁写成「已验证」）。

### 裁定④ 范围账目自引用 → **可接受（处理方式成立）**

- 段内数字为「文档面冻结时点 `0ef1844` 实采 = 40」并附兜底条款「范围以 tag 时点实采为准（发布收口 EV 回填终值）」——**以机器重算验证：`0ef1844` 时点恰 40 笔（= 16 产品 + 20 治理 + 4 E-1），与段内数字、三分账逐项相等**，非自指虚数。
- 当前 HEAD 实采 = 41（含校正提交自身 `01e0446`），tag 时点将 = 41 + E-5a 治理入仓 = 42；**差值与兜底条款声明一致**，且与先例 REL-011（以收口 EV 记终值、治理提交不入用户面）同型。**裁定：可接受，处理方式合法**。
- **附带义务（转 F-3，P2）**：E-5 收口 EV 的终值回填 MUST 按 tag 时点实算（预期 41/42 + 三分账），不得沿抄 40；同时须覆盖 W-2 全链。

### 裁定⑤ CI 口径 → **通过（如实）**

- `.github/workflows/ci.yml` 直读：单 job `guard`，`on: push(branches **)/pull_request/workflow_dispatch`，step 序列 = checkout@v4 → pnpm/action-setup@v4(9) → setup-node@v4(LTS, `node-version: '24'`, cache pnpm) → `pnpm install --frozen-lockfile` → `node tests/run-all.mjs` —— 与 CHANGELOG L36/L52 的逐字描述**完全对应**。
- 文件自身 header 明确「**首跑属 push 后动作**（本批次只落结构 + 本地等价命令实证，不触发远端 CI）」+「**『公开 registry 可解析』属远端事实，待 CI 首跑确认**」；CHANGELOG L52 同步「**尚无 CI 首跑结果**——本节**不声称 CI 已通过**」；L59 重复同口径；README L250 同。`risk-log.md` RISK-001 仍为**活跃**（未回填首跑）——台账与文档口径一致，**无未验证声称**。
- **覆盖边界声明如实**：CI 能覆盖/不能覆盖面（PS 5.1 臂、win32 junction 语义、宿主运行时装配、GUI、OAuth 真实端点、CLI 真机）逐条列出，并指明预期内 skip 口径（ubuntu `#SKIP 3` / Windows `#SKIP 1`）与判据取「相对基线增量」——与 `run-all.mjs` 的 skip 回显实现一致。

### 裁定⑥ no-overclaim 抽查 → **基本通过；1 处新引入的绝对化措辞不实（F-2，P2）**

- **抽查通过项**：全文无「已验证/已确认」类预填（用户真机复验、E-2/E-3/E-4 结论均声明「由发布链各自入账，本节不预填」，L24/L59 逐处核到）；「**宿主可达侧 82 断言** / 宿主不可达侧 71 条照跑、只记 skip 不失败」措辞与 `host-contract.mjs` 的 skip 语义一致（源内 check() 静态 58 处 + 循环展开，量级吻合，口径为「断言数」非「测试数」）；「**20 独立套件 + 4 runner 模块**」经**独立构造复算成立**：`tests/*.mjs` 共 25 个 − run-all 自身 − 4 runner（`attachments` / `audit-001-concurrency` / `client-render` / `install-entry`，`run-all.mjs:58` 显式清单）= **20**，且反向守卫要求未排除套件须含独立入口/退出闸；台账/未修项未被表述为已修复——RISK-001（CI 首跑未回填）、`DEFERRED-001`、ARCH-004 各级 P3 台账（plan-tracker EVO-024 行「P3×8 台账」、FIX-037 行「台账保留 open：R0 P3×7 + CI 首跑后回填两项」）在版本面均未被声称已清。
- **不实项（F-2，详见 findings）**：CHANGELOG L14「（RPC `router/hostFaceDiagnostics`，**读缓存快照、零探测**）」——`lib/service.js:3459-3463` 实为**条件式读时复检**（`cached.length === 0 || cached.some(state !== 'ok')` → `runFaceProbes(this.ctx)`），该语义在 `review-EVO-023-R0-input.md:95/107` 已被审查记录明确（并裁定「非全绿态下每次 RPC 读 = 一次全量同步 probe」为有界代价）。「零探测」在**空缓存**（进程启动后必然）与非绿态下均不成立。

### 裁定⑦ R0 findings 回溯 → **逐条标注**

| R0 finding | 本轮状态 | 依据 |
|---|---|---|
| **W-1**（资产名逐字 `dsh-agent-router-0.4.6.tar.gz`（无 v）+ curl 200 + Content-Length 终验 = E-5 MUST） | **待 E-5 执行（未满足，未到时点）** | tag v0.4.6 不存在（`.git/refs/tags` 直读）；E-5 未执行；资产名/尺寸随版本位裁定结果可能变化（若走 MINOR 路径则整体变更为 0.5.0 资产名 + 新尺寸）——**W-1 的参数 MUST 在版本位裁定后重定** |
| **W-2**（收口 EV 覆盖 E-1~E-6 = MUST） | **待 E-5 执行（未满足）** | `evidence-log.md` 当前止于 **EV-184**（GOV-008 治理升级），**无覆盖 E-1~E-6 的发布收口行**；E-2/E-3 现仅散在 plan-tracker 与 EV-171 段内（EV-171 记的是「R0 时点的 E-2/E-3」，非新范围终值） |
| **S-1**（EV-170 遗留边缘随版披露或裁定留痕） | **已满足（采纳裁定留痕路径）** | plan-tracker L39「**S-1 裁定留痕：遗留边缘（切换后未发请求即派生）台账候选、不随版披露**（README L165 措辞未超界 + EV-170 已机录）」+ commit `bc7533d` |
| **S-2**（plan-tracker 总览行刷新） | **部分满足（仍未刷新，转 F-9）** | plan-tracker **L20** 总览行仍为「41 任务 / **v0.4.3 已发布（2026-09-04）** / G4 待评 / RISK-001 活跃」——活跃行（L26-41）准确，总览行**差三个已发布版本**；未发布状态陈述本身不假（v0.4.4/0.4.5/0.4.6 发布都是本轮之事），但任务数与 Gate 结论已陈旧 |

---

## 四、五维度结论（逐维含证据锚）

| 维度 | 结论 | 证据与说明 |
|---|---|---|
| ① 发布就绪 | **不通过（阻塞项在 version 面）** | HEAD `01e0446` 直读；范围 41 笔 + 三分账 16/20/5 机器复算（见二）；三件套齐备（`package.json` L4=0.4.6；CHANGELOG v0.4.6 节 L6-59 六段完整；README 8 处版本位全 0.4.6）；P0 无未完成；**版本位与重划范围不匹配 = 唯一阻塞（F-1）**；RISK-001/003 活跃属延续披露，三版先例不阻发布 |
| ② 质量门禁 | **通过（口径如实 + 独立旁证）** | E-2 门控 20 套件 + 4 runner exit 0 = **机录采信**（plan-tracker L39 / prompt 复跑 @`0ef1844`、@`01e0446`）；**独立构造旁证**：tests/ 25 文件清点 → 20 套件 + 4 runner 恒等式成立（run-all.mjs:58/113-114/144 直读）；`host-contract.mjs`（719 行，S1a-e/S2/S3/S4/S5/S6/S7 八组守卫）与 `host-version-snapshot.mjs`（147 行，5 组 23 断言源级计数）在位；CI **无首跑**（如实披露，不计通过） |
| ③ 回滚能力 | **通过（附 F-5 两项缺口，非阻断）** | 通道命令具体可照做（README L60-65 / L39-56）；数据安全步骤在位（CHANGELOG L51 备份 `stats/`）；**新增边界 = 无**（三轮构造性证据：schemas 纯增 35 行零改删 / host-abi 零落盘 / files 仅新增条目）；**未验证 = 回滚组合（v0.4.6→v0.4.5 × 0.1.5-rc.2 宿主）与回滚后校验步骤**，如实标注 |
| ④ 用户影响 | **通过（F-6 措辞需收口）** | 破坏性三段披露逐字与实测 diff 一致（裁定②）；迁移成本为零成本描述成立（配置/存储/API 零变更，且 schemastery 未知键透传 = 回滚亦零配置阻塞）；逃生口在（显式配置 `cfg.subagent`）；新增能力面用户可见且为增益方向（面级降级 + 健康徽章）；README L30/L130 前置口径落差 = F-6；`router/hostFaceDiagnostics` 用户面文档在 README L249 有指针 |
| ⑤ 版本号合规（semver） | **不通过** | 顺序递进无跳号成立，但 **PATCH 与「新增能力面 + 自认 breaking」不匹配**（裁定①）：仓内先例 v0.4.2 明确以 MINOR 承载「完整新能力面」；`package.json` files/依赖/公开 RPC 均为能力面扩张。**裁定 MINOR 0.5.0 为合规解，PATCH 需用户 DEC 明示豁免** |

---

## 五、硬门槛裁决（发布检查清单逐项）

| # | 门槛项 | 阈值 | 结果 | 证据锚 |
|---|---|---|---|---|
| 1 | 发布检查清单全部 PASS | 100% | **未达 100%（1 项 FAIL）** | 见下表 #11（semver/版本位）= FAIL；其余 PASS/N-A |
| 2 | 回滚方案存在且已验证 | = 已验证 | **部分（方案在，专项未验证）** | README 双通道 + CHANGELOG 数据步骤；回滚组合与校验步骤未验证（F-5，如实标注；与 R0 同型处置） |
| 3 | CHANGELOG 用户视角完整 | 关键段全覆盖 | **PASS** | 摘要/新增/修复/变更/破坏性变更/已知行为与口径披露/版本说明 七段直读（L6-59） |
| 4 | breaking changes 已标注 | 100% | **PASS** | 三声明逐项与 package.json 实测 diff 一致；支持面收窄显式披露（CHANGELOG L40-47） |
| 5 | Feature Flag 关闭验证 | 全部通过 | **N/A（本版零新 flag）** | `routerSchema` 配置节零变更（L266-302）；kill-switch 语义面未变（`enabled`/账号级/登出删除三层）；诊断面非 flag 且不可关闭（设计使然） |

**发布检查清单（逐项）**

| # | 检查项 | 结果 | 锚 |
|---|---|---|---|
| 1 | 版本号 0.4.5→0.4.6 已 bump | PASS（但版本位不合规，见 #11） | `package.json` L4 直读 |
| 2 | README 版本同步（8 处） | PASS | L7/L41(×3)/L46/L53/L82/L86(×3)/L91/L98 全 0.4.6；残留 L18/L20/L153/L268/L270 均为历史引入标注 |
| 3 | CHANGELOG v0.4.6 节完整性 | PASS | L6-59 七段直读 |
| 4 | 三分账与实采对账 | PASS | 41 笔对象层实算 16/20/5；冻结时点 40 = 16/20/4（见二） |
| 5 | 引证 SHA 真实性 | PASS | 节内 41 token 全数落在实算范围内 + `79d67c7` 为 tag 定义合法引用 |
| 6 | 审查记录存在性 | PASS | 16 个 `review-*.md` 全在，`-input.md` 全文齐（非空壳） |
| 7 | 门控 E-2（单入口全量） | PASS（机录 + 旁证） | plan-tracker L39；20+4 恒等式独立复算 |
| 8 | 冷装 E-3（隔离环境） | PASS（机录） | 1,675,743B / version 0.4.6 / 23 lib 模块（15+8）/ IMPORT OK exports=5；隔离性由重定向保证 |
| 9 | 回滚方案存在且可执行 | PASS（演练未验证，如实） | README L60-65/L39-56；F-5 |
| 10 | 破坏性变更标注与实测对照 | PASS | 裁定② |
| 11 | semver 合规 / 版本位 | **FAIL** | 裁定①（F-1） |
| 12 | 复验后置措辞零预填 | PASS | L24/L59 逐处核到 |
| 13 | CI 口径如实 | PASS | 裁定⑤ |
| 14 | Feature Flag / Kill Switch | N/A | 零新 flag（见上 #5） |

---

## 六、findings 列表

### BLOCKING

**F-1（P1 → BLOCKING）版本位 PATCH 不成立 / 缺版本位权威**
- **位置**：`package.json` L4（`"version": "0.4.6"`）；`CHANGELOG.md` L57（版本说明）。
- **事实依据**：范围 = 新增能力面（`lib/host-abi/` 8 文件 + `router/hostFaceDiagnostics` RPC + 设置页健康面板，见 `package.json` files L36、`schemas.js:427`、`service.js:3459`）+ 破坏性支持面收窄（`CHANGELOG` L43 自认「低于 0.1.5-rc.2 的旧宿主不在本版支持面」）；仓内成文先例 v0.4.2「MINOR：完整新能力面」；前置 PATCH 授权（用户 2026-09-10「小版本」）成立于旧范围（当时实采 2 笔），范围重划授权（2026-09-12）未重述版本位。
- **建议**：见裁定①——(a) 改 0.5.0（并重跑 E-2/E-3）；或 (b) 用户 DEC 明示豁免后保留 0.4.6。二者必居其一，**不得以「惯例先例 v0.4.1」单方通过**（该先例系用户裁决产物，本版无对应后置裁决）。

### WARNING

**F-2（P2）CHANGELOG 新引入的绝对化措辞与实现不符：「读缓存快照、零探测」**
- **位置**：`CHANGELOG.md` L14。
- **事实依据**：`lib/service.js:3459-3463` —— `const cached = faceHealthSnapshot(); const faces = cached.length === 0 || cached.some(f => f.state !== 'ok') ? runFaceProbes(this.ctx) : cached`：**空快照或含非 ok 面时本次读触发一次全量 probe（13 面同步 ctx.get）**；实现 docstring（:3452-3457）与 `review-EVO-023-R0-input.md:95/107` 均载明此语义并裁定为有界代价（B5 审查已接受）。「零探测」在进程启动（缓存必空）与非绿态下不成立；R0 报告与旧版文案为「读**一次性**快照」（不含此绝对断言），故此为补正轮**新引入**的表述。
- **建议**：改回或限定：如「读缓存快照（全绿态零探测；空快照/非全绿时本次读触发一次自检，探针不进轮询与渲染路径）」。

**F-3（P2）范围计数与当前 HEAD 不一致（41 vs 段内 40）+ E-5 终值回填义务**
- **位置**：`CHANGELOG.md` L58（「main 全部提交 **40 个**」）。
- **事实依据**：本轮实算 HEAD 时点 = **41**（含账目校正提交 `01e0446` 自身），tag 时点预期 **42**（+E-5a 治理入仓）。段内自洽（40 = 16+20+4 @`0ef1844`）且兜底条款声明「以 tag 时点实采为准」，故**非虚报**——但读者在 tag 前看到的数字与当时 HEAD 实况差 1。
- **建议**：E-5 收口 EV 回填终值（41/42 + 三分账）时，同步在段内补一句「tag 时点终值见收口 EV」或直接以终值改写该数字（不改写则须保证兜底句在 tag 后仍可读）。

**F-4（P2）W-2 未满足：收口 EV 仍未覆盖 E-1~E-6**
- **位置**：`.governance/evidence-log.md`（当前止于 **EV-184**，无 E-1~E-6 发布链合账行）。
- **事实依据**：R0 W-2 = MUST；R0 之后新增的 EV-184 为 GOV-008 治理升级证据，非发布链合账；E-2/E-3 现值仍散在 plan-tracker L39 与 EV-171（旧范围时点值）。
- **建议**：E-5/E-6 收口 EV MUST 覆盖 E-1~E-6 全链（含新范围 E-2 终值、E-3 五数字与包内模块数、E-4 本 R1/R2 结论、E-5 push/tag/Release/资产终验、E-6 归档检查），不得再依赖 tracker 进度列作为唯一载体。

**F-5（P2）回滚组合未验证 + 回滚后无一致性校验步骤**
- **位置**：`README.md` L60-65/L39-56；`CHANGELOG.md` L51（数据边界段落）。
- **事实依据**：E-2/E-3 仅验证正向（新插件 × 新宿主）；`v0.4.6 → v0.4.5` 在 0.1.5-rc.2 宿主上的组合**零证据**，而 v0.4.5 仍含本版已删除的 `apiProxy` 形态判定链（`lib/host-abi/llm-selection.js` 单形态化）；README 回滚步骤止于「重装前版」，无「回滚后确认加载/统计可读」步骤。
- **建议**：E-5 发布 notes 回滚段补三项（命令步骤 / 数据备份步骤 / **「插件回退 ≠ 宿主回退」限定语 + 该组合未验证的如实标注**）；标注不得写成「已验证」。

**F-6（P2）README 安装前置口径（≥0.1.1-rc.2）与本版硬要求（0.1.5-rc.2）落差**
- **位置**：`README.md` L30、L130。
- **事实依据**：新版删除了 apiProxy 旧面回落（DEC-031）并统一声明 `^0.1.5-rc.2`，故真实支持面 ≥0.1.5-rc.2；当前前置句仍以更早版本为门槛，仅以「受支持与实测基线见下方」间接引用。CHANGELOG 披露充分，但**README 是安装入口**，落差会误导按前置条件安装的用户。
- **建议**：前置句加内联限定（如「实际支持面 = 实测基线 0.1.5-rc.2 及以上（见「宿主兼容性（实测基线）」）；低版本宿主不再受支持」）。

### SUGGESTION

**F-7（P3）`host-version-snapshot.mjs` 断言计数口径勘正**：CHANGELOG L59 声明「**24 断言** PASSED」，而该文件**源级可枚举 23 条**（3+9+3+2+7——`packages.json` inject/platform 3、peerDeps 集合 1 + 8 项 8、deps 3、非 dsh 2、README 7）。同段「host-contract 82 断言」为运行时数（源 58 处 + 循环展开），两处口径混写。建议按实测运行输出的 `passed` 数回填并标注为运行时值。

**F-8（P3）EVO-018（B0）缺内部注解 + 全链覆盖句的过度断言**：`lib/host-abi/llm-selection.js` 生产代码注释原文「**B0-B6 全部节点均有生产代码证据**」——B0（`3344f0d`：仅 `package.json` + README + tests）**无生产代码证据**（其证据 = `tests/host-version-snapshot.mjs` 静态守卫 + README 矩阵，属正确设计）。同型问题在 CHANGELOG L58 兜底句「产品提交在本节语义全覆盖」（B0 语义覆盖正确，但无内部注解锚）。建议 B0 行补内部注解或把该句收窄为「除 B0 声明面止损批外均有生产代码证据」。

**F-9（P3）plan-tracker 总览行（L20）陈旧**：R0 S-2 未刷新——总览仍为「41 任务 / v0.4.3 已发布 / G4 待评」，与活跃行（L26-41）相差三个已发布版本与 15 个终态任务。建议随 E-5 治理入仓刷新（任务数 / 最近发布 / Gate 结论 / RISK 数）。

**F-10（P3，观察）降级段点的连续定义**：CHANGELOG 用「v0.4.5 **起**」表述 v2 统计行边界。本版不新增边界（已证），但「起」字下若未来版本再加行版本，回滚可行域需逐版重建。建议在 E-5 收口 EV 或 README 维护段记一句「降级可行域 = 目标版本行版本 ≥ 数据行版本」的判据，避免逐版文字重述。

---

## 七、核验方法边界（诚实披露）

- **直读/实算**：全部文件面事实（package.json/CHANGELOG/README/ci.yml/schemas.js/service.js/host-abi 全域/tests 结构与守卫/治理三件与审查记录清单）；`.git` 对象层实算（HEAD、tag 解引用、`v0.4.5..HEAD` = 41 的祖先遍历、逐提交 tree diff 三分账、peerDeps/deps/inject/files 字段级 diff、`schemas.js` 行级 diff）。
- **机录采信（未独立复跑）**：E-2 门控 exit 0 与计时、E-3 隔离冷装五数字（旁证：lib 模块清点与 files 白名单一致、包内 version）、E-1 smoke 两跑、push 凭据恢复与 `--dry-run` exit 0、各批审查结论文本（旁证：16 个记录文件与全文报告存在，节内引证 SHA 全数属实）。
- **未验证（如实标注）**：回滚组合演练（F-5）、CI 首跑与平台事实（pwsh 存在性、registry 可解析性——CI 未跑）、W-1 资产名与 200/Content-Length 终验（未到时点）。

## 八、结论与下一步

- **NEEDS_CHANGE**（非终态；复审未闭合，M7.4 → 返工 → R2 由同一 Reviewer 复审）。
- **通过面（可留作 R2 基线）**：三分账与引证真实性、破坏性变更披露与实测 diff 一致、CI 口径如实、Feature Flag N/A、无新增降级边界、复验后置零预填、16 条审查记录存在且相互匹配。
- **返工项（R2 前必须闭环）**：F-1（版本位：改 0.5.0 并重跑 E-2/E-3，或取用户 DEC 明示豁免）；F-2（措辞）；F-3/F-4/F-5/F-6 为 E-5 收口与文档收口的 MUST/前置义务。
- **E-5 执行段门禁（沿用 R0，参数须按版本位裁定结果重定）**：W-1（资产名逐字无 v 前缀 + `curl -sIL` 200 + Content-Length 终验）；W-2（收口 EV 覆盖 E-1~E-6）。
- **提示 H-1（非 finding）**：若走 (a) MINOR 路径，E-1 已推送前无需任何已发布版本改写（tag/push/Release 均未发生），修正成本仅限文档与重新验证——这是当前唯一低成本修正窗口。
