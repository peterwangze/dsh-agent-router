# 代码审查报告（review-FIX-038-R1-input）

- **round = R1**（聚焦复审：验证 R0 P2-1 家族处置）
- **前轮引用**：`REVIEW-FIX-038-R0` / 报告 `.governance/review-FIX-038-R0-input.md`（R0 = APPROVED_WITH_NOTES，`unresolved_blockers=0`）；本轮逐条比对 R0 findings 中**本轮涉及项**
- **审查对象**：`96aca2c`（ci.yml +11/−3 · README +1/−1）/ `7fd8cb5`（install-entry +5/−1）/ `e1fe160`（smoke +4/−2）；受审 HEAD = `e1fe160acef49b31bb8b08a92e2092a42877ab39`
- **审查者**：Code Reviewer Agent（同一实例复审轮；只读；唯一写入 = 本报告）
- **结论（四态）**：**APPROVED_WITH_NOTES**
- **独立结构字段**：`unresolved_blockers=0`
- **计数**：P0 = 0 / P1 = 0 / P2 = 0（**R0 P2-1 已闭合**）/ P3 = 4（新发现）；另含 R0 遗留项状态汇总与 1 项我方 R0 自我更正

---

## 一、独立取证清单（本轮全部实测，含对 Coordinator 机验的抽样复核）

| # | 事实 | 命令 / 结果 |
|---|------|------------|
| 1 | HEAD / 工作树 | `git rev-parse HEAD` = `e1fe160acef49b31bb8b08a92e2092a42877ab39`；`git status --short` = ` M .governance/{evidence-log,plan-tracker}.md` + `?? .governance/review-FIX-038-R{0-input,0}.md`（治理记录面在途，非本批 commit 内容） |
| 2 | 门控独立复跑（第 3 次独立实测） | `node tests/run-all.mjs` @`e1fe160` = `ALL 20 SUITES + 4 RUNNER MODULES (via smoke.mjs) PASSED (29.0s)  #SKIP 1 (smoke.mjs×1)`，**exit 0**（R0 @`5d8af6e` = 26.7s / Coordinator @`e1fe160` = 29.4s，三次同向） |
| 3 | **纯性核验（机器判定）** | `96aca2c`：ci.yml changed lines **11/11 为注释行**；`7fd8cb5`：**6/6 注释行**；`e1fe160`：**6/6 注释行**（正则 `^[+-]\s*(\*|/\*\*|\*/|//|#)` 分类，无一行落在代码/字符串字面量） |
| 4 | ci.yml YAML 体未变 | 抽取**非注释行**逐字对比：before = 32 行 / after = 32 行 / **`-ceq` 相同 = True** ⇒ `name: ci` / `on:`（push 全分支 + PR + workflow_dispatch）/ `jobs.guard` / `runs-on: ubuntu-latest` / 5 steps 全部未动（YAML 语义面零变化，与 Coordinator 独立解析结论一致） |
| 5 | 「覆盖对称」残留扫描 | 显式路径扫 `README.md / CHANGELOG.md / AGENTS.md / .github/workflows/ci.yml / tests/*.mjs / lib/**/*.js / install.{ps1,sh}`：**shipped 文件零命中**（唯一命中在我的 R0 报告 `.governance/review-FIX-038-R0-input.md`，属历史记录，预期） |
| 6 | 新表述与代码门控一致性 | 三处新表述所称「在 sh 可用时由 install.sh 臂承担」对照实测：`if (sh !== null)` 门控在 `tests/install-entry.mjs:374`（§6 sh）/ `:438`（§6c-sh）/ `:517`（§8 sh）；「sh 缺失时另有 `POSIX online checks` skip 行」对照 `:346` ✔ 一致 |
| 7 | §6d「无 POSIX 对照臂」事实 | `install.sh:94-101` = `step "源码自带依赖目录：$SRC_NODE_MODULES（跳过依赖链接）"` ✔；`:113-131` = `if [ "$LINKED" = 0 ]` + 安全护栏（`绝不对真实目录 rm -rf`）+ tar 拷贝 ✔；`tests/*.mjs` 对 `real node_modules` 的断言仅存在于 PS1 面（`:455/:468/:471/:475/:479`）⇒ 表述成立 |
| 8 | P3-2 标注事实 | `ci.yml` / `README.md` 新文：ubuntu `#SKIP 7` = 「**预期值——修复后首跑前尚未实测，以首跑日志逐行复核**」；Windows `#SKIP 1` = 「**本地实测**」——与取证 #2（三次本机实测均 `#SKIP 1 (smoke.mjs×1)`）一致，未把未实测写成实测 ✔ |
| 9 | `:263` 自锚漂移史（裁定用） | `git show <rev>:tests/smoke.mjs` 定位 `typert contribution registered` 断言：`c57de5b`（锚写入点）= **2215**（锚写 2213 ⇒ **出生即偏 2 行**）；`4672973`（批前）= 2256（累计偏 43）；`5d8af6e` = 2268（FIX-038 再 +12）；`e1fe160` = 2270（本轮 +2）；`git log -S"2213 typert contribution registered"` 命中 `c57de5b` + `e1fe160` |
| 10 | `:77` 自锚漂移史 | `git log -S"install-entry.mjs:74-77"` 命中 `7fec25e` / `3dc2fa1` / `f4340ac`（锚写入链）+ `e1fe160`（改写）；`install-entry.mjs` 经 `cc89119` **+43 行** ⇒ 锚由 74-77 漂至 122（`powerShellHosts` 现 `:122`，调用点 `:314`）⇒ 属**本批（FIX-038）自引入** |
| 11 | 我方 R0 引证核对 | R0 期 `5d8af6e:tests/install-entry.mjs:471` §6d skipArm 原文 = `…——拷贝/链接回退语义仅 win32 可判定`；「同一断言面由下方 … 臂承担」仅出现于 §6 `:357` 与 §6c `:420` ⇒ **R0 报告 §五 P2-1 位置行引证有误（见 §二 更正）** |
| 12 | README 行号锚偏差实测 | 「留空 = 继承主 Agent 模型」：`65201ad` = 158 → 批前（`2a119f9`/`4672973`）= 164 → 现 = 165；「跟随主 Agent 当前实际模型」：`65201ad` = 165 → 批前 = 171 → 现 = 172 ⇒ **现值偏差 7 行 = 批前 6 + 本批 `5d8af6e` README +1**。引用方 = `lib/preset-defaults.js:348`、`tests/preset-defaults.mjs:1067/1074`、`CHANGELOG.md:25`（均未被本批四笔 commit 触碰） |
| 13 | metrics.mjs 证据锚偏差实测 | `metrics.mjs:131` 引 `smoke.mjs:1162-1177`，实测该断言现 `smoke.mjs:2200/2204`（偏 ~1038）；`:245` 引 `1473-1480` → 实测 2652（偏 ~1175）；`:501` 引 `1557-1569` → 实测 2741（偏 ~1180）；`:326` 引 `1669-1682` → 实测 2876（偏 ~1200）⇒ Developer「既有 ~1200 行」**成立**；全库同类共 **12 处**（`:57/:117/:131/:132/:244/:245/:326/:327/:328/:437/:501/:502` 及 `:521` 控制台串），`metrics.mjs` 未被本批任一 commit 触碰 |
| 14 | 既有锚守卫（重要） | `tests/host-abi-health.mjs:691-705` 已存在**锚清扫守卫**（§9h R-1）：按文件列出 `stale`（行号式锚，须零残留）+ `fresh`（函数名式锚，须在位），覆盖 5 个文件（ctx-services.js / events.js / stats.js / fix-031-attribution.mjs / client-render.mjs）——本轮清扫的 `llm-selection.js`、`metrics.mjs`、`smoke.mjs`、`install-entry.mjs`、`install.sh` **均不在守卫清单内** |
| 15 | 越权面 | `git show --stat`：三笔文件集 = {ci.yml, README.md} / {tests/install-entry.mjs} / {tests/smoke.mjs} ⇒ `.governance/**`、`AGENTS.md`、`package.json`、`CHANGELOG.md` **零触碰** ✔ |

---

## 二、R0 findings 本轮涉及项逐条处置

| R0 项 | 处置 | 证据 |
|-------|------|------|
| **P2-1**（POSIX 覆盖对称不实 + §6d 缺口未标注） | **已修复（A 方案，三处全覆盖）** | ① `ci.yml:43-47`：删「覆盖对称」→「**POSIX 对偶覆盖按臂分列（仅陈述实际成立范围）**：§6/§6c 在 sh 可用时由 install.sh 臂承担（sh 缺失时另有 `POSIX online checks` skip 行可辨）；**§6d 无 POSIX 对照臂**——install.sh:94-101/:113-131 明载同一数据保护语义却零判别测试，属已知覆盖缺口（台账候选 · R0 P2-1-A 如实标注，不声称对称）」；② `README.md:249`：同口径（断言未删除 + §6d 缺口明示）；③ `tests/install-entry.mjs:47-51`（`PS1_OFFLINE_APPLICABLE` 常量 JSDoc）：同口径，保留「非适用平台分支均先打印可见 skip 再空循环，不静默丢弃」。取证 #5 零残留、#6 与 `if (sh !== null)` 门控逐点一致、#7 §6d 缺口事实成立 ⇒ **闭合**。B 方案（补 sh 臂）未采纳、已入台账——与 R0 建议（A 立即 + B 单列任务）一致 ✔ |
| **P3-2**（`#SKIP 7` 标注为预期值） | **已修复** | 取证 #8：ubuntu = 预期值（未实测）+ 以首跑日志逐行复核；Windows = 本地实测（三次独立跑一致）✔ 且未把未实测写成实测 |
| **P3-1**（ci.yml 超长行） | **我方撤回（R0 论断不成立）** | 实测：R0 所指行为 **76 字符**；文件批前已有 4 行 >80（86/91/83/81），批后仍为 4 行（max 91 不变）、新增 5 行均 ≤80 ⇒「明显超出全文 ~78 列基线」**不成立**，P3-1 系未测量即下的判断，**撤回**（可读性 nit 保留为可选） |
| **R0 §五 P2-1 位置行引证** | **更正（我方错误）** | 取证 #11：R0 报告称 §6d：「对照 `tests/install-entry.mjs:470-472`（§6d skip 文案自称"由 sh 臂承担"）」——**引证错误**：§6d 原文为「拷贝/链接回退语义仅 win32 可判定」，「由 sh 臂承担」在 §6 :357 与 §6c :420。**P2-1 实质结论不受影响**（README/ci.yml 的"覆盖对称"总括句 + §6d 无 POSIX 臂两项事实均经独立取证，见 R0 取证与本轮 #7）；但该跳转会误导定位，故显式更正 |
| **其余 R0 项**（P2-2/P2-3/P2-4/P2-5、P3-3~P3-8） | **未被误标** | 三笔 commit 信息未声称其中任何一项已闭合（措辞均为「R0 P2-1-A 补正」/「P2-1 家族第三处」/「自引入锚漂移清零」）；逐项状态见 §五 |

---

## 三、复审要求 7 项逐项结论

### 1. P2-1 是否真闭合 —— **是（三处口径只陈述实际成立范围，零残留不实）**
- §6/§6c 的 sh 对偶表述**准确**：「在 sh 可用时」与 `if (sh !== null)`（`install-entry.mjs:374`/`:438`）严格对应；「sh 缺失时另有 `POSIX online checks` skip 行可辨」与 `:346` 对应（取证 #6）。
- §6d 的「无 POSIX 对照臂 + 已知覆盖缺口」表述**与代码事实一致**（取证 #7：install.sh:94-101 同语义分支、`:113-131` 拷贝回退护栏、断言面仅 PS1）。
- **残留的不实扫描**：shipped 文件「覆盖对称」= 0 命中（取证 #5）；三处新文均带「仅陈述实际成立范围/台账候选」限定 ⇒ 无复发的总括性声明 ✔。
- 唯一遗留（非 P2-1 未闭合，属运行日志措辞，新发现 N1）：`install-entry.mjs:475` 的 §6d skip **理由串**仍写「拷贝/链接回退语义仅 win32 可判定」——与同文件新 JSDoc「install.sh 同载该语义却零判别测试 = 覆盖缺口」**内在不一致**（前者暗示平台不可判定，后者明示是缺测试）。详见 §四 N1。

### 2. P3-2 是否成立 —— **是（标注与事实一致）**
- ubuntu `#SKIP 7` 标为「预期值——修复后首跑前尚未实测」：与我 R0 的推导结论一致，且**未**标为实测 ✔。
- Windows `#SKIP 1 (smoke.mjs×1)` 标为「本地实测」：本机三次独立跑（R0 @`5d8af6e`、Coordinator @`e1fe160`、本轮 @`e1fe160`）均实测该值 ⇒ 措辞属实 ✔。

### 3. 纯性核验 —— **三笔均为零代码行变化（机器判定）**
- `96aca2c`：ci.yml **11/11** changed lines 为注释行；README 为 1 行文档句替换（±0 行数）；**YAML 体 32 行非注释行逐字未变（`-ceq` True）**、触发器/作业/步骤全在 ⇒ 未触断言/判据逻辑 ✔。
- `7fd8cb5`：**6/6** 注释行（仅 `PS1_OFFLINE_APPLICABLE` 上方 JSDoc）；`e1fe160`：**6/6** 注释行（`smoke.mjs:77-78` 与 `:263-265` 两处注释）⇒ 零代码行、零字符串字面量变化（运行期零影响，与门控复跑 exit 0 互证）✔。

### 4. R0 其余 P2/P3 是否被误标 + `:263` 顺带的裁定 —— **未被误标；顺带准予保留，但需更正"自引入"表述**
- `e1fe160` 顺带改 `:263` 自锚：**准予保留（不回退）**，理由三条：
  1. **同族同型**（行号锚漂移；与本批 S-2 / P2-1 家族同源），且为**同文件内自引用**，修法（符号名式锚）与既有一致；
  2. 该锚**出生即偏**（`c57de5b` 时目标在 2215，锚写 2213）、批前已累计偏 43 行，**本批（`cc89119` +12 / `e1fe160` +2）又各推进一步** ⇒ 本批对该锚的失效有增量责任（非纯他人债务）；
  3. 零行为影响、纯注释、消除一个会误导维护者的错误坐标（该句本身就是"计数三处同步"指引，指错行会带偏后续维护）。
- **但表述须更正（P3-N4 附属项）**：commit 主标题「**自引入**锚漂移清零」对 `:263` **不准确**——`:77` 是本批自引入（cc89119 +43 行所致，取证 #10），`:263` 属「出生即偏 + 既有债务为主 + 本批增量」。台账应记为「自引入（:77）+ 同族既有债务（:263）」。
- R0 其余项：无被误标（见 §二末行）；R0 P3-1 由本审查**自我撤回**（不成立，见 §二）。

### 5. Developer 三项残余报告裁定 —— **分类正确（既有债务入台账），但两处需精确化**
| 残余项 | 裁定 | 依据 |
|--------|------|------|
| `metrics.mjs:326/:521` evidence 字符串锚漂移（既有 ~1200 行） | **分类正确：既有债务 → 台账**（不随本批修）。**精确化 ①**：同类不止 2 处，全库共 **12 处**（取证 #13）；**精确化 ②**：`metrics.mjs` 四笔 FIX-038 提交均未触碰 ⇒ 债务确为既有；实测偏差 ~1038~1200 行（Developer 量级成立） | 取证 #13 |
| README `L158/L165` 锚「既有偏 6 行」 | **分类正确（既有为主）→ 台账，但现值须记为 7 行**：批前 6（`65201ad`→`2a119f9`）+ 本批 `5d8af6e` README +1 行 = **现值 7**（取证 #12）；且引用落在**shipped 文件**（`lib/preset-defaults.js:348`、`tests/preset-defaults.mjs:1067/1074`）+ `CHANGELOG.md:25` ⇒ 台账须列明这些位置（此前 R0 P3-3 只列 llm-selection.js 三处） | 取证 #12 |
| `powerShellHosts` 坐标现 `:122` | **无动作**：`7fd8cb5` 使 install-entry 再 +4 行（118→122），而 smoke 侧引用已改**符号名式锚** ⇒ 无残留数字坐标被再撞坏（取证 #10）；此点反证符号名式锚的抗漂移价值 | 取证 #10 |

### 6. 无新引入 + 无越权 —— **成立**
- 无 P0/P1/P2 新引入（本节 §四仅 4 条 P3 措辞/守卫类建议）；三笔文件集不含 `.governance/**`、`AGENTS.md`、`package.json`、`CHANGELOG.md` ✔（取证 #15）。
- 门控复跑 @`e1fe160` exit 0，且**新增注释不会破任何守卫**（installed 文档守卫 install-entry §2 / `host-version-snapshot.mjs` README 断言均绿，取证 #2）。

### 7. **关键判定：三笔是否使 ubuntu CI 具备转绿条件** —— **不改变 R0 结论：转绿条件已具备（由 `cc89119` 建立），本三笔不增不减；仍不得声称"已验证绿"**
- **本三笔运行期零影响**：全部为注释/文档（取证 #3、#4：ci.yml 非注释体逐字未变、YAML 触发器/步骤完整）；断言集合、skip 门控、`run-all` 聚合机制**无任何变化** ⇒ 对 ubuntu 执行结果既无正向也无负向作用。
- **转绿条件仍取决于 R0 的 FIX-038 面**：`cc89119` 的①smoke `.cmd` 平台门控 ②LRU 夹具 POSIX 判据修正（R0 三重取证）③`lstatOrUndefined`（ENOENT 不再裸抛阻断后续断言）——本轮未触碰，条件与 R0 判定一致。
- **不可声称"已验证绿"**：CI 实测须 push 后由 Coordinator 核验；本机已实测的 Windows 面 `#SKIP 1 (smoke.mjs×1)`（三次）**不等于** ubuntu 面结论。
- **未验证面（与 R0 P2-5 同，本批未缩小）**：ubuntu 侧 `§6c-sh` / `§6d` / `§8`（含 ghost 臂）仍属**首次执行**面；`#SKIP 7` 仍为预期值（现已在 ci.yml/README 如实标注，取证 #8）⇒ FIX-038 闭环判据仍应含「修复后 ubuntu 首跑绿 + `#SKIP 7` 实测复核」。

---

## 四、新发现（本轮）

### P0 / P1 / P2 —— 0 条
无。

### P3 —— 4 条（措辞/守卫类，不阻塞）

**N1｜§6d skip 理由串与同文件新 JSDoc 内在不一致（运行日志口径）**
- 位置：`tests/install-entry.mjs:475`（`——拷贝/链接回退语义仅 win32 可判定`）vs 同文件 `:47-51`（新 JSDoc：`§6d … 无 POSIX 对照臂——install.sh:94-101 / :113-131 同载该数据保护语义却零判别测试（已知覆盖缺口，台账候选）`）
- 事实依据：`install.sh:94-101` 确实实现同一保护语义（取证 #7）⇒「仅 win32 可判定」的措辞把"缺测试"表述成"平台不可判定"，与同文件新增说明相反；该串在 ubuntu CI 日志中**逐字可见**（`#SKIP | skip real node_modules dir left untouched (-File) (…)`）。
- 影响：口径内部不自洽（本批主体精神即"只陈述实际成立范围"）；无功能影响。
- 建议：改为与 §6d 缺口同口径，例如 `——install.sh 同载该保护语义但无对照臂（已知覆盖缺口，台账候选）`（1 行；可与后续清扫同批）。

**N2｜本批新引入 `install.sh` 行号锚（现值准确，但属漂移族）**
- 位置：`ci.yml:46`、`README.md:249`、`tests/install-entry.mjs:49-50`（三处均引 `install.sh:94-101 / :113-131`）
- 事实依据：三处引用为**本批新写入**（`96aca2c`/`7fd8cb5`）；现值经逐行核实准确（取证 #7）。但同一族锚的失效模式已在库内反复发生（R0 P3-3：llm-selection.js ×3 已漂；`:263` 自锚出生即偏；metrics 证据锚偏 ~1200），且库内已有"行号式锚零残留 + 函数名式锚在位"的守卫先例（取证 #14）。
- 建议：可选改符号名式（如 `install.sh 的「源码自带依赖目录：…（跳过依赖链接）」分支 / `LINKED=0` 拷贝回退护栏`），或把 `install.sh` 纳入锚守卫（见 N4）；**不回改不影响本轮通过**。

**N3｜`e1fe160` 插入行破坏了「下一行」指代（注释自指精度）**
- 位置：`tests/smoke.mjs:263-266`（现文：`:264` 「…三处：本行 / 下一行 / \`typert contribution registered\` 断言行」/ `:265` 新增括号行「（符号名式锚；原写死行号已随本文件行数变动漂移）」/ `:266` `check('19 invocations', …)`）
- 事实依据：改前该注释块紧邻 `check('19 invocations')`（"下一行"成立）；本轮在注释与断言之间插入 `:265` ⇒ 字面「下一行」现指向注释行而非断言（+1 行错位）。
- 影响：纯注释自指精度（计数三处仍语义可解：`19 项` 行 / `19 invocations` 断言 / `typert contribution registered` 断言）。
- 建议：把括号说明并入同一行，或改写为「紧随的 `19 invocations` 断言行」。

**N4｜锚清扫未接线既有守卫（同类可复发）+ 历史行号字符串残留**
- 位置：`tests/host-abi-health.mjs:691-705`（§9h `ANCHOR_CASES`，覆盖 5 文件）vs 本轮清扫/台账面（`lib/host-abi/llm-selection.js`、`tests/metrics.mjs`、`tests/smoke.mjs`、`tests/install-entry.mjs`、`install.sh` 均**不在**清单）
- 事实依据：库内已存在自动化锚守卫（stale/fresh 双断言），但本批（含 R0 的 S-2/P2-1 家族与 R1 两处自锚）**全部靠手工逐个修**；未接线 ⇒ 同类漂移可复发（本项目原则 4"看护/防护网"精神）。
- 建议（单列任务）：把本轮清扫面 + 台账清单（llm-selection.js ×3、metrics.mjs ×12、README-L158 式 ×3~4、install.sh 引用）纳入 `ANCHOR_CASES`（或泛化扫描器），使"行号式锚零残留"成为机器看护而非人力巡检。
- 附属细节：`tests/smoke.mjs:78` 以引号形式保留了历史串 `install-entry.mjs:74-77`——若将来上泛化扫描会被命中；建议改写为不含数字行号的表述（如「原写死 install-entry.mjs 行号区间锚」）。

---

## 五、R0 findings 状态汇总（本轮后）

| R0 项 | 状态 | 备注 |
|-------|------|------|
| P2-1（覆盖对称不实 + §6d 缺口） | **已修复（A）** | B（补 sh 臂）已入台账；附属 N1（skip 理由串）待同批微调 |
| P2-2（`resolveCliInvocation` 平台注入缺失） | 未修复（未在本轮范围） | 建议单列任务（R0 建议不变） |
| P2-3（判据双份 P5） | 未修复（未在本轮范围） | 同上 |
| P2-4（`3f3e59b` 空 diff 证据口径） | 未修复（台账事项） | R0 建议：注记基线 `2090267..3f3e59b^` |
| P2-5（ubuntu 闭环证据） | 未修复（过程项） | 本轮已由 P3-2 如实标注缩小一半（"预期值"），闭环判据仍待首跑 |
| P3-1（ci.yml 超长行） | **撤回** | 我方 R0 论断不成立（实测 76 字符，批前已有 4 行 >80） |
| P3-2（`#SKIP 7` 限定语） | **已修复** | — |
| P3-3（llm-selection.js ×3 锚） | 未修复（台账） | 建议并入 N4 的守卫接线任务 |
| P3-4（`3f3e59b` 注释措辞/括号跨度） | 未修复（台账） | 仍可单列 |
| P3-5（证据脚本留痕 + 历史残留目录） | 未修复（治理事项） | — |
| P3-6（`removeTempDir` 的 `lstatSync`） | 未修复（可选） | 低风险 |
| P3-7（skip 文案与 sh 可达性耦合 + 格式契约分散） | **部分已闭合**：新文案已加「sh 可用时」限定 ⇒ 耦合点消除；格式契约分散仍为可选 | — |
| P3-8（失败 `detail`） | 未修复（可选） | — |

---

## 六、硬门槛裁决

| 门槛项 | 阈值 | 实测 | 裁决 |
|--------|------|------|------|
| P0 阻塞问题数 | = 0 | 0（§四 P0 无） | **PASS** |
| 5 维度全覆盖 | = 100% | 见下逐维度结论 | **PASS** |
| 每条发现标注级别 | = 100% | 新发现 P3×4 均带文件:行号 + 事实依据 + 建议；R0 处置逐条带「已修复/未修复/撤回/更正」 | **PASS** |
| 设计一致性检查 | 已完成 | 与 FIX-037 ①「禁静默降级 + skip 可见」纪律、`ci.yml` 头部契约（§5.3 D3(c) 第①步）、ARCH-004 §5.3(c) 演进检测层、`AGENTS.md` 原则 1/4/7/8/9 与 P5、库内既有锚守卫先例（`host-abi-health.mjs` §9h）逐项比对：本批**无偏离**（口径已如实、未削弱门控留红能力、零产品逻辑改动） | **PASS** |
| AI 代码专项 5 项 | 全部完成 | ① mock 残留：无（纯注释/文档，无夹具变更）② 硬编码：无（未改断言条件）③ 幻觉 API：无（未引入任何 API/符号；新引用的 `install.sh` 行号经逐行核实存在）④ 未实现 TODO：无（三笔 grep `TODO/FIXME/XXX/HACK` 零命中）⑤ 过度实现：无（最小改动；R0 P3-4/P3-1 未顺带扩大） | **PASS** |

**五维度逐项结论（摘要）**：**正确性 PASS**（三笔运行期零影响；YAML 体逐字未变，触发器/步骤完整；门控三次独立跑 exit 0）；**安全性 PASS**（零产品代码/配置/数据路径改动；无越权文件）；**可维护性 PASS**（口径如实化、限定语补齐；N1~N4 为措辞/守卫建议）；**性能 PASS**（26.7s / 29.0s / 29.4s 同量级，无新增步骤）；**测试覆盖 PASS**（断言集合与 skip 机制零变化；§6d 缺口已如实标注并入台账，替代了此前的"覆盖对称"不实声明）。

### 审查结论

> **APPROVED_WITH_NOTES**
> `unresolved_blockers=0`
>
> 理由：R0 P2-1 家族**已闭合**（ci.yml/README/install-entry 三处按臂分列、§6d 缺口明示、"sh 可用时"限定与 `if (sh !== null)` 门控严格一致、shipped 文件「覆盖对称」零残留），P3-2 **已修复**且与实测一致；三笔经机器判定为零代码行变化（ci.yml 非注释体逐字未变），无 P0/P1/P2 新引入、无越权；本轮独立复跑门控 `exit 0`（Windows `#SKIP 1`）。新增 4 条 P3（§6d skip 理由串口径、新引入 install.sh 行号锚、注释自指"下一行"、锚守卫未接线）为措辞/看护类建议，**不构成阻塞**；同时撤回 R0 P3-1、更正 R0 P2-1 的位置行引证（实质结论不变）。**ubuntu 转绿条件与 R0 判定一致（由 `cc89119` 建立，本三笔不增不减），仍不得声称"已验证绿"。**

---

## 七、未验证项声明（事实依据红线）

1. **ubuntu 执行结果未验证**：`#SKIP 7` 为预期值（现已在 ci.yml/README 如实标注）；`§6c-sh` / `§6d` / `§8` 仍属首次执行面；本机无 Linux/WSL/docker ⇒ 不可复跑。
2. **CI 日志本体不可本地取证**（远端仓库）：FIX-038 首跑失败面为可推导证实（R0 已述），本轮不重复主张。
3. **Windows 侧为本机实测**：`#SKIP 1 (smoke.mjs×1)`、全量门控 `exit 0` @`e1fe160`（本轮第 3 次独立跑）。
4. **第三方机验的抽样复核范围**：Coordinator 的 YAML 解析结论经我以更强判据替代验证（**非注释行逐字对比为同一**，等价于解析结果不变）；其门控数字经我独立复跑复核一致。

---

## 八、建议 Coordinator 动作（按依赖排序）

1. **复审判定**：本结论为通过终态（APPROVED_WITH_NOTES / `unresolved_blockers=0`）⇒ 无需再 spawn 本 Reviewer；以 review-record CLI 机录（canonical `review-FIX-038-R1.md`）。
2. **台账更正（4 项，与 F-8 裁决同批）**：
   - ① `:263` 锚表述更正为「自引入（`:77`）+ 同族既有债务（`:263`：出生即偏 2、批前累计 41、本批 +14）」；
   - ② README 行号锚偏差记**现值 7 行**（批前 6 + `5d8af6e` +1），并列出引用位置 `lib/preset-defaults.js:348` / `tests/preset-defaults.mjs:1067/1074` / `CHANGELOG.md:25`（不与历史记录回改）；
   - ③ metrics.mjs 锚台账条目补全为 **12 处**（含 `:521`）并记实测偏差 ~1038~1200 行；
   - ④ 追加我方 R0 更正记录：P3-1 撤回（未测量即判断）+ P2-1 位置行引证更正（§6d 原文 = "仅 win32 可判定"）。
3. **后续候选任务**（非阻塞）：N4（锚守卫接线：把 llm-selection.js / metrics.mjs / smoke.mjs / install-entry.mjs / install.sh 纳入 `host-abi-health.mjs` §9h `ANCHOR_CASES`，一劳永逸）；N1（§6d skip 理由串口径，1 行）；N2（install.sh 行号锚符号化）；N3（注释自指措辞）；以及 R0 未闭合项 P2-2 / P2-3 / P2-4 / P3-3 / P3-4 / P3-6 / P3-8。
4. **FIX-038 闭环条件不变**：修复后 ubuntu 首跑绿 + `#SKIP 7 (host-contract.mjs×2, smoke.mjs×5)` 逐行实测复核（首跑前不得判闭环）。
