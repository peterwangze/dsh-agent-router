# REL-012 E-4 发布审查 **R2（返工后复审）** — v0.5.0 发布就绪独立审查（Release Reviewer → Coordinator）

**审查结论：APPROVED_WITH_NOTES**

**unresolved_blockers=0**

（BLOCKING=0 / WARNING=1（E-5 执行段门禁义务）/ SUGGESTION=3；R1 全 10 条 findings 逐条处置完毕——**已修复 6 / 正当延后 4 / 未修复 0 / 新引入 0**。仅 `APPROVED` 与 `APPROVED_WITH_NOTES`（含独立字段 `unresolved_blockers=0`）为通过终态，本结论属后者。）

- **round**: R2
- **前轮引用**: `REVIEW-REL-012-R1`（`review-REL-012-R1.md` / `review-REL-012-R1-input.md`，结论 NEEDS_CHANGE，唯一 BLOCKING = F-1；机录行已核：`next_round: REVIEW-REL-012-R2` / `prev_report: .governance/review-REL-012-R1-input.md`；`evidence-log.md` L619 = REVIEW-REL-012-R1 机录行在案）
- **本轮对象**: 返工后候选 @ HEAD **`a81d56b`**（`package.json` 0.5.0 / `CHANGELOG.md` v0.5.0 节 L6-59 / `README.md` / `.github/workflows/ci.yml` / 治理三件套与 `DEC-032`）
- **返工链**: `857f515`（F-1 升位 + F-2 + F-7 + F-3）→ `51b9154`（F-6）→ `a81d56b`（F-8 文档半边）；三笔均经 `.git` 对象层逐文件核验

---

## 一、审查方式与事实红线

- **只读**：read / grep / glob + `.git` 对象层直读（`refs/**`、`logs/HEAD`、loose object 解压遍历）。**未执行 git 命令**（范围、三分账、逐提交文件清单、字段级/行级 diff 全部由自实现只读解析算出）；**未修改任何文件**（Write 仅本报告）；未建子 agent、未与用户交互、**未操作任何真实环境**。
- **判定分级**：① 直读/实算 ② 机录采信（逐处标注）③ 未验证（明确标注：回滚演习、CI 首跑）。
- **复审纪律**：R1 全量 findings（F-1~F-10）逐条比对并标注「已修复 / 正当延后 / 未修复 / 新引入」，无跳过项（见五）。

---

## 二、独立复核：返工链与当前范围（git 对象层实算）

### 2.1 版本面

| 核查项 | 本审查员实算 | 结论 |
|---|---|---|
| `package.json` version | **`0.5.0`** | ✓ |
| `857f515` 相对 `01e0446` 的 package.json diff | **仅 version 字段变更**——files 数组 / deps / peerDeps / inject / scripts **逐项相等**；files 仍含 `lib/host-abi/`（R1 已核的包内容面未变） | ✓ |
| `0.4.6` 残留（发行面） | `package.json` / `README.md` / `CHANGELOG.md` / `lib/*.js` / `install.ps1` / `install.sh` / `cordis.patch.yml` = **零命中**（Coordinator `git grep` 结论独立复现） | ✓ |
| `0.4.6` 残留（全仓） | 仅存于 `.governance/**`（R0/R1 审查报告、change-triage JSON、execution-packets、EV-171/EV-172 当时态文本、`DEC-032` 决策沿革引用）——**历史留痕，非发行面** | ✓ 合法 |
| README 版本引用 | **8 行 12 处**全 0.5.0（L7 徽章 / L41×3 资产名+URL / L46 / L53 / L82 固定版本例 / L86×3 / L91 / L98）；资产名 `dsh-agent-router-0.5.0.tar.gz`（**无 v**）+ 路径 `/releases/download/v0.5.0/`（**带 v**）= 既证惯例 | ✓ |
| tag v0.5.0 | **不存在**（`.git/refs/tags` 止于 v0.4.5；无提前打标）；origin/main 仍 = `969531b` | ✓ |

### 2.2 范围与三分账（两时点实算）

| 时点 | `v0.4.5..HEAD` | 产品 | 治理 | E-1（文档/版本声明面） | 段内声明 |
|---|---|---|---|---|---|
| `857f515` | **42** | 16 | 20 | **6** | 「42 = 16 + 20 + 6，与实采一致」→ **逐项相等 ✓** |
| `a81d56b`（当前 HEAD） | **44** | 16 | 20 | **8** | 兜底条款「范围以 tag 时点实采为准（发布收口 EV 回填终值）」覆盖 ✓ |

- **逐提交文件清单实算**（44 笔全量）：仅 `.governance/**` 改动 = **20 笔**（治理）；其余 24 笔 = **16 笔产品提交**（含 `lib/`、`tests/`、`.github/`、`pnpm-lock.yaml`，以及 `.governance`+产品混合的 `2090267`）+ **8 笔 E-1 文档提交**（`857f515` / `51b9154` / `a81d56b` / `01e0446` / `0ef1844` / `19f9a94` / `4d7889b` / `65201ad`）。
- **与 R1 旧账对账**：R1 时点 41 = 16 + 20 + 5；`0ef1844` 冻结时点 40 = 16 + 20 + 4。返工新增 3 笔全部落入 E-1 桶 → **三分账结构自 R1 以来零漂移**（产品 16 / 治理 20 恒定，仅 E-1 桶增长）。
- `857f515` 实采 42 = 战前 41 + 其自身；其后的 `51b9154` / `a81d56b` 两笔由兜底条款覆盖——与段内「本返工提交（hash 见 git log）」单笔表述一致（同 R1 对 F-3 的「可接受」裁定）。

### 2.3 返工链逐笔内容（对象层 diff）

| commit | 实际内容 | 与声明一致 |
|---|---|---|
| `857f515` | `CHANGELOG.md` **6 行变更**（节标题 / L14 措辞 / L47 breaking 结论行 / L57 版本号条 / L58 发布范围 / L59 验证基线）+ `README.md` **8 行变更**（版本引用）+ `package.json` version 1 行 | ✓ |
| `51b9154` | `README.md` 1 行（L30） | ✓ |
| `a81d56b` | `CHANGELOG.md` 1 行（L58 覆盖句括注） | ✓ |

**机验纠正（协作留痕）**：本审查员 R1 报告内曾并列出现「源级 15 处 `check()`」与「源级可枚举 23 条」两条互斥自述；本轮据此独立重算并给出结论（详见裁定①），**R1 该处计数有误，R1 报告不追改（历史留痕纪律），以本 R2 为准**。

---

## 三、6 项裁定项逐项结论（本轮 MUST）

### 裁定① F-7 机验纠正 → **纠正成立；正确口径 = 24（运行时断言数）**

- **独立复核方法**：剥除注释后逐个解析 `host-version-snapshot.mjs` 的 `check()` **执行态调用点**，并按其真实循环基数展开：
  - 单次执行调用点 **13** 处（L111/L112/L113 三态 inject+platform；L118 peerDeps 集合恒等；L132/L133 cordis+schemastery 范围覆盖；L137~L143 README 七项）；
  - 循环内调用点 **2** 处，基数由同文件常量实读：`PEER_DEP_NAMES` **8** 项（L57-66）、`DSH_RUNTIME_DEPS` **3** 项（L69-73）；
  - **13 + 8 + 3 = 24**。
- **我的 R1 错在哪（自查结论）**：R1 我把「3+9+3+2+7」错写成 23——该串自身之和实为 **24**，我在同一报告内既写了分项 9（含 peerDeps 集合断言）又漏计该 1 条，属自述不一致导致的算术错误；`passed` 自增在两次膨胀调用后发生，运行时确为 24。
- **裁定**：**Developer 的纠正成立**，返工按 F-7 的「要求」（取实测运行时值并标注口径）落实为「保留 24 + 运行时口径标注」而非改写为 23，属正确处理。CHANGELOG L59 现文「**24 断言 PASSED（运行时断言数——实跑 `passed` 值口径，与同段 `host-contract.mjs` 82 断言同口径；源级 15 处 `check()` 调用点，其中 peerDeps 8 项 / deps 3 项为循环展开）**」——**计数正确、口径明确、与 82 同口径**，R1-F-7 彻底闭合。
- **旁证（口径自洽）**：同段「83」无需引入；`host-contract.mjs` 82（宿主可达侧运行时值）与本项 24 现为同一口径，跨节无可比性问题。

### 裁定② F-6 是否满足原话要求 → **满足**

- 原话要求：「真实支持面**不得由用户从『受支持与实测基线见下方』自行推导**」。
- 现文实测：
  - `README.md` **L30**：「前置要求：DSH ≥ **0.1.5-rc.2**（本版受支持与实测基线；`dsh plugin` 命令自 0.1.1-rc.2 起提供）+ [pnpm]…」——**直接陈述支持面**，`dsh plugin` 命令存在性降为括注（信息保留、不再冒充支持面门槛）；
  - `README.md` **L130**（` ```text ` 提示词块内）：「确认前置条件：DeepSeek Harness ≥ 0.1.5-rc.2（本版受支持与实测基线）、本机已安装 pnpm」——同口径，无推导负担。
- **措辞微调（L130 未加粗）**：` ```text ` 块内**不宜**用 Markdown 粗体——加粗标记会作为字面字符进入用户复制给 AI 助手的提示词文本；不加粗是**正确处置**，非瑕疵。故该微调不构成本轮 finding。
- **裁定**：F-6 **已满足**，且与 CHANGELOG 破坏性变更段（L43「低于 0.1.5-rc.2 的旧宿主不在本版支持面」）互指一致，安装入口不再存在口径落差。

### 裁定③ F-4 / F-5 处置正当性 → **均为正当延后（E-5 执行段门禁义务），非「仍未满足」**

**F-4（收口 EV 覆盖 E-1~E-6）**：
- 义务性质 = 发布链**后段**产出（E-5 执行 + E-6 归档之后才有终值可记）；当前证据链止于 `EV-184`（GOV-008），且 `REVIEW-REL-012-R1` 已作为独立机录行存在于 `evidence-log.md` L619——**过渡期证据未断**。
- **裁定**：正当延后。约束不变（E-5/E-6 收口 EV MUST 覆盖 E-1~E-6，含新范围 E-2 终值 / E-3 五数字 / E-4 R1+R2 结论 / E-5 push·tag·Release·资产终验 / E-6 归档），且**终值须按 tag 时点实算**（见裁定④）。

**F-5（回滚方案）**：
- 义务性质 = E-5c 发布 notes 的发布物面（GitHub Release body 为仓外产物，仓库内不存在该文件的当下载体）。
- **本轮新增独立评估（比 R1 更精确，供 E-5c 直接引用）**：
  1. 「v0.4.6 → v0.4.5（宿主 0.1.5-rc.2）」在**选择面**上**形态兼容**：v0.4.5 的 `sessionSelectFaceOf`（tag 对象内 `lib/preset-defaults.js` L203-234）**首选 `sessionController` + 形状检查 `typeof controller.selectModel === 'function'`，`apiProxy` 仅为第二分支**；v0.5.0 同法（`lib/host-abi/llm-selection.js:109-128`）——两者都要求 `sessionController.selectModel`，宿主 0.1.5-rc.2 提供该面。→ R1 的保守表述「API 兼容性未验证」可以**更精确**：选择面形状兼容，且「apiProxy 删除」**不构成该回退路径的断点**。
  2. **回滚命令可执行（版本可钉）**：README L82 明示「固定版本：把命令中的 `main` 换成版本号，如 `v0.5.0`」→ 回退到 `v0.4.5` 有文档化入口；另有方式二安装脚本通道与离线 `file:` 安装。
  3. **宿主侧无强制**：用户环境的 npx cache 单向不回退（`DEC-031` BR-04）→ 典型回退 = 只回退插件、宿主维持 0.1.5-rc.2。
  4. **回滚后已知劣势（须如实标注，不得写成「无影响」）**：v0.4.5 携带本版已修复的两项缺陷——① 客户端 `$on` 死订阅（订宿主不存在的事件名 → 凭据引用更新事件静默不触发，属**功能性静默缺陷**）；② FIX-032 语义回退（`cfg.subagent` 留空时 subagent 重新固化预设主模型值，`preset-defaults.js` v0.4.5 L472 → 用户报障场景复现；逃生口 = 显式配置 `cfg.subagent` 在两侧均有效）。
  5. **数据面步骤在案**：备份 `DSH_HOME/dsh-agent-router/stats/`（CHANGELOG L51），v2 行降级边界沿用 v0.4.5 披露。
- **裁定**：正当延后（非「仍未满足」），但**义务不降级**——E-5c 发布 notes 回滚段 MUST 含三项：① 命令步骤（`remove` + 按**固定版本**装回，或离线 `file:` 装前版发行包）；② 数据备份步骤（stats 目录）；③ 「插件回退 ≠ 宿主回退」限定语 + **上述 4 的劣势如实标注**（禁「无影响/已验证」类表述）。

### 裁定④ F-3 账目终值口径 → **处置符合 R1 对 F-3 的裁定（可接受）**

- 段内锚点 = **42**，标注为「本次返工提交落地后实采」= `857f515` 时点；**我已实算复核：`857f515` 时点恰 42，且 42 = 16 + 20 + 6 逐项相等**；当前 HEAD `a81d56b` = **44**（+2 E-1 文档笔），E-5a 治理入仓后预期 tag 时点 = 45（治理 21 + 产品 16 + E-1 8）。
- R1 对此的裁定原文为「**可接受**（段内自洽 + 兜底条款成立）」——当时的处置 = 「兜底 + 收口 EV 回填终值」。本轮不再追加段内数字，是**该裁定的零 churn 延续**：每追加一次段内数字都会立刻产生新的自指差异（`51b9154`/`a81d56b` 自身未计入），反复改写反而制造口径混乱。
- **裁定**：**符合 R1 裁定，处置正当**。硬约束（F-4 项）不变：收口 EV 终值 MUST 按 tag 时点实算（预期 45，治理 21 + 产品 16 + E-1 8），并保持「治理提交不入用户面」先例口径。

### 裁定⑤ F-8 处置（文档半边 + 代码半边留台账）→ **可接受，不构成阻断**

- **文档半边已修复**：CHANGELOG L58 覆盖句现为「产品提交在本节语义全覆盖（ARCH-004 链按任务族归并到「新增 / 修复 / 变更」各节；**其中 `3344f0d` B0 为声明面 + 版本快照守卫批，无生产代码逻辑改动，语义见「变更」节**）」——B0 的语义定位已如实收窄，读者不会误以为 B0 是生产代码批。此段经「变更」节首条「宿主依赖声明面止损（内部注解：EVO-018）」闭合。
- **代码半边留台账的理由成立性裁定**：
  - 事实：`lib/host-abi/llm-selection.js` 生产注释原文含「B0-B6 全部节点均有生产代码证据」；`3344f0d`（B0）实际改动 = `package.json` + `README.md` + `tests/host-version-snapshot.mjs`（本审查员 tree diff 实证）→ **该句对 B0 不成立**（B0 的证据是静态守卫 + README 矩阵，属正确设计，非缺项）。
  - 处置权衡：该句是**注释文本**（非逻辑、非声明面、非用户可见面），修正需触碰 `lib/**` → 触发代码审查面并改动「已通过审查的本版产品代码」；而本版发布候选已过 E-4 R1 复审窗口，为**一行注释**重开代码审查链与重跑 E-2 的代价与收益不成比例。
  - **裁定**：**可接受延后**，不阻断发布（理由成立、留痕在案、无用户面影响）。附带条件：该台账项 MUST 随下一批次（宿主相关热修或文档批）落地，并在 E-5 收口 EV 的「遗留台账」中显式列名（避免沉没）。
- **本轮附带观察（P3，见 S-2）**：代码侧确有少量 v0.4.5 遗留行号注释锚（`llm-selection.js` 头注引 `preset-defaults.js:249-263`，而迁移源现位于 `lib/preset-defaults.js` L376-393 区段）——与 F-8 同域，建议并入同一台账项一次性清扫。

### 裁定⑥ 零新引入（三笔返工是否引入新的不实陈述 / 口径漂移 / 跨节矛盾）→ **未发现新引入**

定向抽查（全过）：
1. **版本号条 × 破坏性变更段**：L57「0.4.5 → v0.5.0（中位抬升 = MINOR 位）」与 L47「**版本位已按重划后范围裁决为 MINOR（v0.5.0，用户 2026-09-12）**，见「版本号」」**互指一致**，且与 `DEC-032` 的「A 升位 0.5.0（用户 2026-09-12 ask 面板裁决）」一致；L57 同时保留了 R1 所需的两项依据（新增能力面清单 / 旧「小版本」授权成立于重划前范围的说明），无自我矛盾。
2. **范围账目 × 三分账**：42 = 16 + 20 + 6 = 实采 42（实算复核通过）；产品 16 个 SHA 逐一命中、治理 20 个 SHA 未变、E-1 六个含 `01e0446` 与「本返工提交」，无漏项、无重复计数（`857f515` 仅出现一次）。
3. **README × CHANGELOG 互指**：README L30/L130（≥0.1.5-rc.2）↔ CHANGELOG L43（低于基线不支持）+ L42（记录性声明 ↔ README「宿主兼容性（实测基线）」节）——三处互指闭合；README L140-141 基线矩阵文本未随升位漂移（基线是宿主版本，非插件版本，正确地**不**改写）。
4. **F-2 措辞修正的准确性**：L14 现文「**条件式读时复检**——全绿快照为纯缓存读；快照为空或含非 ok 面时，本次读触发一次全量自检，探针不进渲染路径与设置页轮询」——与 `lib/service.js:3459-3463` 实现逐字对应（`cached.length === 0 || cached.some(f => f.state !== 'ok') ? runFaceProbes(...) : cached`），且「不进轮询」与 `review-EVO-023-R0-input.md:107` 的既有裁定一致；**R1-F-2 彻底闭合，未过度修正**。
5. **计数口径摘要**：CHANGELOG 现含三处计数（82 / 24 / 20+4）——82 与 24 同为「运行时断言数」口径并在 L59 显式标注，20+4 为套件构成（已由 `run-all.mjs` 构造复核：25 文件 − run-all − 4 runner = 20），三者口径各自自洽、无混用。
6. **`DEC-032` 记载一致性（MUST 独立核验项）**：独立读取 `decision-log.md` L37（13 列结构完整）——上下文栏记载「重划后范围 v0.4.5..HEAD = 41 commits」「前置 PATCH 授权成立于旧范围（该时点实采 = 2）」「未 push 未 tag = 唯一低成本修正窗口」；方案 A/B、结论 **A 升位 0.5.0**、理由、影响范围（package.json / README / CHANGELOG / 资产名 `dsh-agent-router-0.5.0.tar.gz` / E-2 与 E-3 须重跑）、复核日期（发布收口 EV 时复核）**与本次返工实际动作逐项一致**（实算 41→42→44 与 E-1 桶增长一致；E-3 已重跑证据见下）。
7. **E-3 重跑声明与包内容自洽（机录采信 + 旁证）**：`0.5.0.tgz` = **1,675,748 B**（较 R1 的 0.4.6 包 **+5 B**），与本次文档面变更量级自洽（版本串 `0.4.6`→`0.5.0` 等长、CHANGELOG 该节改动 ≈ +5 字符：节标题等值与措辞净增）；包内 version = 0.5.0 / 23 个 lib 模块（15 顶层 + 8 host-abi）/ IMPORT OK（exports=5）——后者与 R1 已核的 `files` 白名单面（未变）逐项吻合。

---

## 四、五维度结论

| 维度 | 结论 | 证据与说明 |
|---|---|---|
| ① 发布就绪 | **通过** | HEAD `a81d56b` 直读；版本三件套齐备且全 0.5.0（package.json L4 / CHANGELOG v0.5.0 节 L6-59 / README 8 行 12 处）；三分账 16+20+8 = 44 实算自洽、段内 42 于 `857f515` 时点逐项相等；无未解决 BLOCKING；P0 无未完成；RISK-001 为延续披露（CI 未首跑，如实）；W-1/W-2 为 E-5 执行段门禁 |
| ② 质量门禁 | **通过（口径如实 + 独立旁证）** | E-2 门控 20 套件 + 4 runner exit 0 = 机录采信（`857f515` Coordinator 复跑 27.9s / 返工 2 后 Developer 27.5s；旁证 = run-all 构造恒等式实算 + 套件与守卫文件在位）；**E-3 已按升位重跑**（0.5.0 / 1,675,748B / 23 模块 / IMPORT OK）覆盖 DEC-032 的「须重跑」要求；CI **无首跑**（如实披露，不计通过）；R1-F-7 断言计数已闭环（24 口径正确） |
| ③ 回滚能力 | **通过（附 F-5 执行段义务，非阻断）** | 方案存在且可执行（README 命令矩阵 L60-65 + 版本钉法 L82 + 离线 `file:` 通道 + 方式二）；数据安全步骤在案（stats 备份）；**本版无新增降级边界**（R1 已证：schemas 纯增零改删 / host-abi 零落盘 / files 仅新增条目，本轮 `files` 与 deps 面未变）；回滚组合（插件回退 × 0.1.5-rc.2 宿主）经本轮形态分析**无 API 断点**，残留为 v0.4.5 自身缺陷 + 命令钉版，须由 E-5c notes 如实标注（F-5 义务保留） |
| ④ 用户影响 | **通过** | 破坏性三段披露与实测 diff 逐项一致（R1 已核，v0.5.0 段文本未失准）；迁移零成本（配置/存储/API 零变更 + 未知键透传）；新增能力面为增益方向；逃生口在位（显式 `cfg.subagent`）；前置口径已对齐支持面（F-6 闭合）；新增 RPC/面板在 README 与 CHANGELOG 双处有指针 |
| ⑤ 版本号合规（semver） | **通过** | 0.4.5 → 0.5.0 = **MINOR 中位抬升**，顺序递进无跳号；与内容匹配（新增能力面 = MINOR，仓内先例 v0.4.2 成文；支持面收窄已披露）；bump 依据充分且**权威来源 = 用户 2026-09-12 裁决（DEC-032 入册）**——R1 的唯一 BLOCKING（版本位缺后置权威）已消解 |

---

## 五、R1 findings 全量处置（逐条：已修复 / 正当延后 / 未修复 / 新引入）

| R1 ID | R1 级别 | 处置 | 依据（本轮实算/直读） |
|---|---|---|---|
| **F-1** 版本位 PATCH 不成立 | BLOCKING | **已修复** | `package.json` = 0.5.0；README 8 行 12 处全 0.5.0；CHANGELOG 节标题 + L57 版本号条 + L47 结论行三处同步；发行面 0.4.6 零残留；用户裁决入 `DEC-032`（方案 A 与 R1 推荐一致）；E-2/E-3 按升位重跑在案 |
| **F-2** 「零探测」措辞 | P2 | **已修复** | L14 改为「条件式读时复检」并与 `service.js:3459-3463` 逐字对应；未过度修正（保留「探针不进渲染路径/轮询」的正确边界） |
| **F-3** 计数 40 vs HEAD | P2 | **正当延后（R1 原裁「可接受」的延续）** | 段内 42 于 `857f515` 实算成立；兜底条款 + 收口 EV 终值（预期 45）覆盖后续 2 笔；R2 裁定④确认 |
| **F-4** W-2 收口 EV 覆盖 E-1~E-6 | P2 | **正当延后（E-5/E-6 执行段门禁）** | 义务属发布链后段；过渡期证据未断（REVIEW-REL-012-R1 机录行在案，L619）；R2 裁定③确认 |
| **F-5** 回滚组合未验证 + 无回滚后校验 | P2 | **正当延后（E-5c notes 义务）** | 义务属仓外发布物面；本轮已把「未验证」精确化为「选择面形状兼容 + 命令可钉版 + v0.4.5 自身缺陷须披露」三项（裁定③），义务清单不降级 |
| **F-6** README 前置口径落差 | P2 | **已修复** | L30 / L130 直陈 ≥ 0.1.5-rc.2（L130 在 ```text 块内不加粗 = 正确处置） |
| **F-7** 断言计数 24 vs 23 | P3 | **已修复（以 24 口径闭合，我的 R1 计数有误）** | 独立重算 13 + 8 + 3 = 24（裁定①）；L59 现文含运行时口径标注，与 82 同口径 |
| **F-8** B0 缺内部注解 / 覆盖句过度断言 | P3 | **已修复（文档半边）/ 正当延后（代码半边）** | L58 覆盖句已加 B0 括注收窄；代码注释半边留台账，理由成立（裁定⑤） |
| **F-9** plan-tracker 总览行陈旧 | P3 | **已修复** | L20 现文：v0.4.5 已发布（tag + Release + asset 1,636,560B curl 200 实证）/ v0.5.0 发布链进行中（E-4 R1 NEEDS_CHANGE → 升位裁决 → 返工中）/ 终态 57 行 / RISK-001（CI 第①步已接线，首跑待回填）/ G4 待评 |
| **F-10** 降级段点连续定义 | P3 | **正当延后（非缺陷，建议性）** | 属「建议记判据」，可在 E-5 收口 EV 或 README 维护段择机落；无发布影响 |
| — | — | **新引入 = 0** | 三项定向抽查全过（裁定⑥ 1/2/3） |

**新增 findings（本轮）**

- **S-1（P3，复核提示）**：`risk-log.md` RISK-003 行状态栏仍为**活跃**（2026-08-21 原状），而 plan-tracker L20 与 ARCH-004 终态声明均称本链为 RISK-003 的**结构性根治轨道**且实施链已闭环 → 存在「缓解已落地但风险状态未升级」的治理表述陈旧风险。建议：在 E-5 收口 EV 中同步 RISK-003 的缓解证据（静态契约快照 + 双向守卫 + 逐批独立复跑）并升级其状态，或说明为何维持「活跃」（如宿主漂移属长期残留风险）。**非发布阻断**（不涉及发行面文件）。
- **S-2（P3，并入 F-8 台账项）**：`lib/host-abi/llm-selection.js` 头注保留 v0.4.5 行号锚（引 `preset-defaults.js:249-263`，实际迁移源现位于 `lib/preset-defaults.js` L376-393 区段，`:249-263` 现为 `sessionBlank`/其他区段）——代码注释锚漂移。建议与 F-8 代码半边**合并为同一台账项**，随下一批次一次性清扫（避免两次触碰 `lib/**` 触发两轮代码审查）。
- **S-3（P3，观察）**：README L153「四个**分级分类卡片**」小节未列本版新增的宿主面**健康面板与徽章**（该面板在 CHANGELOG「新增」首位 + README L249 有指针）——设置页小节的能力面列举略滞后于本版能力面。建议随下一文档批补一句（不影响发布：发布说明与诊断指针均在位）。

---

## 六、硬门槛裁决

| 门槛项 | 阈值 | 结果 | 证据锚 |
|---|---|---|---|
| 发布检查清单全部 PASS | = 100% | **PASS**（13 项适用项全过，1 项 N/A） | 见下表 |
| 回滚方案存在且已验证 | = 已验证 | **部分（方案在；专项演习未做，如实标注 + E-5c 义务绑定）** | README 通道 + 版本钉法 + 数据步骤；R1/R2 同型处置，不阻断（与 v0.4.3~v0.4.5 三版先例一致） |
| CHANGELOG 用户视角完整 | 关键段全覆盖 | **PASS** | v0.5.0 节七段直读（L6-59） |
| breaking changes 已标注 | = 100% | **PASS** | 三声明与实测 diff 逐项一致（R1 已核）；L47 结论行含版本位裁定互指 |
| Feature Flag 关闭验证 | 全部通过 | **N/A（本版零新 flag）** | `routerSchema` 配置节零变更（R1 已核，本轮 `package.json` files/deps 面未变）；kill-switch 语义面未变 |

**发布检查清单（v0.5.0 终态）**

| # | 检查项 | 结果 | 锚 |
|---|---|---|---|
| 1 | 版本号三件套一致（0.5.0） | PASS | `package.json` L4 + CHANGELOG L6/L57 + README 8 行 12 处 |
| 2 | 发行面 0.4.6 零残留 | PASS | 7 类发行面 grep 零命中 |
| 3 | CHANGELOG v0.5.0 节完整性 | PASS | L6-59 七段 |
| 4 | 三分账与实采对账 | PASS | 44 = 16+20+8（HEAD）/ 42 = 16+20+6（`857f515`）双时点相等 |
| 5 | 引证 SHA 真实性 | PASS | 节内 token 全落实算范围（R1 已核，本轮新增 `857f515` 在 `git log` 引用形式） |
| 6 | 审查记录存在性（R0/R1 机录） | PASS | `review-REL-012-R0/R1.md` 在案；R1 机录含 `next_round: R2` |
| 7 | 门控 E-2（单入口全量） | PASS（机录 + 旁证） | 20 套件 + 4 runner exit 0；构造恒等式独立复算 |
| 8 | 冷装 E-3（隔离环境，0.5.0 重跑） | PASS（机录 + 旁证） | 1,675,748B / version 0.5.0 / 23 模块 / IMPORT OK（+5B 与文档面变更自洽） |
| 9 | 回滚方案存在且可执行 | PASS（演习未验证，如实） | README L60-65 / L82 / L39-56；F-5 义务绑定 E-5c |
| 10 | 破坏性变更标注与实测对照 | PASS | 三声明 + L47 结论行 |
| 11 | semver 合规 / 版本位权威 | PASS | MINOR 0.5.0 + 用户裁决 DEC-032 |
| 12 | 复验后置措辞零预填 | PASS | L24/L59 逐处保持 |
| 13 | CI 口径如实 | PASS | 「无首跑、不声称通过」+ 覆盖边界 + skip 口径 |
| 14 | Feature Flag / Kill Switch | N/A | 零新 flag |

---

## 七、核验方法边界（诚实披露）

- **直读/实算**：版本三件套与残留扫描；两时点范围与三分账（自实现祖先遍历 + tree diff，44 笔全量逐提交文件清单）；返工三笔的对象层字段级/行级 diff；README/CHANGELOG 关键行；`DEC-032`、plan-tracker L20/L39；review 记录清单；`run-all.mjs`/`host-version-snapshot.mjs` 源码级断言解析；v0.4.5「选择面 + fixup target 链」对象层实读。
- **机录采信（未独立复跑，均经旁证）**：E-2 门控 exit 0 与计时；E-3 隔离冷装五数字（旁证：包尺寸增量与文档面变更自洽、files 白名单未变、lib 模块数不变）；E-1/E-2 时间序；push 凭据状态。
- **未验证（如实标注）**：回滚专项演习（F-5 义务，未做且本轮不主张已做）；CI 首跑与平台事实；W-1 资产名 + curl 200/Content-Length 终验（未到时点）；最终 tag/push。

---

## 八、结论与下一步

- **APPROVED_WITH_NOTES，`unresolved_blockers=0`** —— v0.5.0 发布候选**就绪，可进入 E-5**。
- **通过面**：版本位 MINOR 0.5.0（用户裁决 DEC-032 在册，三件套一致、发行面零残留）；三分账双时点实算自洽；破坏性披露与实测 diff 一致；语义版本合规；CI 口径如实；F-2/F-6/F-7/F-9 四项文档修正准确且无过度修正；F-8 文档半边已收窄；**未发现新引入的不实陈述或口径漂移**。
- **E-5 执行段门禁（随发布动作完成，非候选缺陷）**：
  1. **W-1**：资产名逐字 `dsh-agent-router-0.5.0.tar.gz`（**无 v 前缀**）+ `curl -sIL` 终验 HTTP 200 + Content-Length 与 E-3 实际包一致（`/releases/download/v0.5.0/` 带 v）；
  2. **W-2**：收口 EV 覆盖 E-1~E-6，**终值按 tag 时点实算**（预期 45 = 治理 21 + 产品 16 + E-1 8）；
  3. **F-4/F-5 义务**：E-5c 发布 notes 回滚段三项（命令含**版本钉法** / 数据备份 / 「插件回退 ≠ 宿主回退」限定 + v0.4.5 自身缺陷如实标注：客户端死订阅 + FIX-032 语义回退，逃生口 = 显式 `cfg.subagent`）；
  4. **S-1/S-2/S-3 + F-8 代码半边 + F-10**：随 E-5 收口 EV 的「遗留台账」显式列名并排入后续批次（防沉没）。
- **后续复审**：若 E-5 前发生候选面补正（如采纳 S-3 等文档改动），按 M7.4 由同一 Reviewer 复审（round R3，注入本报告路径 `.governance/review-REL-012-R2-input.md`）；若补正仅限上述执行段动作（push/tag/Release/收口 EV），则不触发新一轮发布审查。
- **终态声明**：本结论为**通过终态**（APPROVED_WITH_NOTES 且无序解决 BLOCKING finding），Reviewer 侧审查链在此闭合；最终提交、推送与 tag 动作由 Coordinator 执行。
