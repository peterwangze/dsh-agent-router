# 代码审查报告（review-FIX-040-R2-input）

- **round = R2**（R1 返工后复审；本轮为 T2 前最后一轮）
- **前轮引用**：`REVIEW-FIX-040-R1` / 报告 `.governance/review-FIX-040-R1-input.md`（R1 = **NEEDS_CHANGE**，新发现 P0=0 / P1=1 / P2=1 / P3=2）；并逐条回溯 `REVIEW-FIX-040-R0` 未闭合项
- **审查对象**：返工两笔 `8eef8f1`（R1 P1-1(new) 收口：三元组 + 声明单源 + 判别前缀 + §9h-2b）/ `d301859`（R1 P2-1(new) 收口：精确匹配 + 代码卫生 + 阈值披露）；受审 HEAD = **`d301859`**；文件集 = `{tests/host-abi-health.mjs, tests/metrics.mjs}`（`+85/−50`）
- **审查者**：Code Reviewer Agent（同一实例复审轮；只读；未修改任何产品代码；未运行任何写操作；唯一写入 = 本报告）
- **执行依据**：两笔 `git show` 全量 diff、最终态逐行核对、**R1 三态仿真谓词的复用复算**（只读 + 内存字符串替换）、三态自满足类独立复算、9h-3 新判据复刻与两向仿真、守卫套件与门控本机独立复跑（第 6 次）、断言集机比对
- **结论（四态）**：**APPROVED_WITH_NOTES**
- **独立结构字段**：`unresolved_blockers=0`
- **计数（本轮新发现）**：P0 = 0 / P1 = 0 / P2 = 0 / P3 = 3（均为代码卫生/残留局限类，非阻塞）

**一句话理由**：R1 的两项阻塞项**均已真闭合且经我独立复现**——①对象核验改**逐行声明单源**后，R1 的两个静默绿盲区（smoke 侧裸标签断言改名 / `lib/wrapper.js` 实现行改写）**双双判红**（`deadAnchors` = 3 / 1），基线仍绿；②§9h-3 改**双向精确判据**后，R1 的子串注入形态（`「takes over default model」`）**判红**，同时 20/20 现有候选名全部接受（12 精确 + 8 `「label」`结尾）⇒ 无新漏判；③自满足三类（`anchor === object` / `object ⊂ anchor` / 对象命中非声明源）经我独立复算**均为 0**，新增 §9h-2b 判据可判别（三类逐类构造违规均可判红）；④R1 P3-1/P3-2 的披露与表述改写**如实**。守门套件 **160 断言 PASSED**、门控 **exit 0**、断言实质零删除（净 +1）、越权面零 ⇒ 判通过终态。

---

## 一、R2 独立取证清单（全部实测）

| # | 事实 | 命令 / 结果 |
|---|------|------------|
| 1 | 范围与越权面 | `git log --oneline e13ca17..d301859` = 2 笔；`git diff --stat` = 2 文件 `+85/−50`，与声明**逐字一致**；`.governance/**` / `AGENTS.md` / `CHANGELOG.md` / `package.json` **零触碰** ✅；`git status --short` = 仅 `.governance/{evidence-log.md,tpa-last-run.json}`（治理在途）+ R0/R1 四份机录/报告（非本批 commit 内容） |
| 2 | **守卫套件单跑** | `node tests/host-abi-health.mjs` @`d301859` = **`ALL HOST ABI HEALTH TESTS PASSED (160 assertions)`，exit 0**；`9h-2` / **`9h-2b`** / `9h-3` 三条均 `ok`（`9h-2b` 标签 = `B5 9h-2b R-1: 对象核验零自满足（anchor≠object × 对象不被锚文本包含 × 对象仅在其声明源出现）`）✅ 与 Coordinator 机验（160）一致 |
| 3 | **门控独立复跑（第 6 次）** | `node tests/run-all.mjs` @`d301859` = `ALL 20 SUITES + 4 RUNNER MODULES (via smoke.mjs) PASSED (28.5s)  #SKIP 2 (smoke.mjs×2)` **exit 0**（Coordinator 27.9s 同量级）；两条 skip 原文与 R0/R1 实测一致（`0o600` 可见 skip + `POSIX online checks`） |
| 4 | **三态仿真复算（R1 谓词复用，核心）** | 解析 `ANCHOR_OBJECTS_3`（`:761-785`）：**21 行全部解析成功**，声明源分布 = `smoke 18 / parity 1 / wrapper 1 / metrics 1`（与 Developer 声明一致）。以「只在声明源内查」的谓词复算：**基线 `deadAnchors = 0`（绿）**；**变异① smoke 侧 3 条裸标签断言改名 ⇒ `deadAnchors = 3`（判红）**（R1 期为 **0**）；**变异② `lib/wrapper.js` 实现行（`provider: wrapRoute`→`OTHER`）改写 ⇒ `deadAnchors = 1`（判红）**（R1 期为 **0**）；**变异③ metrics 侧自引用 `checks.push(['twin resolveModel 镜像模型身份（模型 id 不变）'` 改名 ⇒ 1（判红）**；**变异④ `console.log('attachmentIds resolution (M2):'` 改名 ⇒ 1（判红）** ⇒ **R1 的两个盲区关闭，且未引入新的静默面** |
| 5 | **自满足三类独立复算（§9h-2b 同构）** | ① `anchor === object` = **0**；② `object ⊂ anchor`（`anchor ≠ object`）= **0**；③ 对象命中**非声明源** = **0**；④ 对象不在声明源（`deadAnchors`）= **0** —— 四类全 0，**与 9h-2b 的判据结果一致**（非仅采信注释/声明）|
| 6 | **§9h-2b 判据可达性（三类逐类构造）** | ① `identical`：内存构造 `anchor === object` 行 ⇒ `identical.length = 1`（可判红）；② `objectInsideAnchor`：构造「object 为 anchor 子串」行 ⇒ 命中 1；③ `foreignSourceHits`：构造「对象串出现在非声明源」行（把 smoke 的 `check('wrapper takes over default model'` 标为 `wrapper` 源）⇒ 命中 1 ⇒ **三类判据均为可达红线，非恒真谓词** ✅ |
| 7 | **§9h-3 新判据复刻（精确匹配）** | 复刻 `key === label \|\| key.endsWith('「'+label+'」')`：**基线 `unregistered = 0`**；**R1 的子串注入形态（`「takes over default model」`，为键 `smoke.mjs「wrapper takes over default model」` 的子串）⇒ `unregistered = 1`（判红）**（R1 期为 **0 静默**）；**R0 死锚形态（`「twin resolveModel mirrors model identity」`）仍判红（1）** ⇒ 旧能力无回退 |
| 8 | **9h-3 无新漏判（误红面反查）** | 当前 `metrics.mjs` 全部 `「…」` 候选 = **20 个**，命中路径 = **精确 12 / 表键以 `「label」` 结尾 8 / 未命中 0** ⇒ 双向精确判据**接受全部现存合法形态**（含合并写作 `「A」+「B」` 两段、`<前缀>「label」` 复合锚、裸标签表键），零误红 ✅ |
| 9 | **`+「label」` 分支删除后的语义覆盖** | 已删除的分支原用于兼容「表键 = `+「label」`」形态；该类键自 P3-6（R0）起已改为完整整串键 ⇒ 无实例；合并写作**后半片段**的注册现由「表键以 `「label」` 结尾」或「精确等于」两条路径覆盖（取证 #8 的 8/12 分布即此两路径）⇒ **语义无缺口** ✅ |
| 10 | 阈值披露与数值复核 | 9h-3 注释已如实标注 `<12 字符` 短名**不参与本项 ⇒ 短名死锚不会被捕获**，并给出「当前无 <12 实例、最短 22 字符」；我复测：候选 20 个、**`len<12` = 0、最短 = 22** ✅（与披露逐字一致） |
| 11 | `metrics.mjs` 本批改动纯性 | `8eef8f1` 对 `metrics.mjs` 的 diff = **纯注释块（+3/−3 行）**，`checks.push(['twin resolveModel 镜像模型身份（模型 id 不变）', twinResolved.id === MAIN_MODEL && …])` 等**代码行逐字未变**；注释改为**函数名 + 语义描述**（「展开 resolved、只覆写 provider 与 inputModalities 两字段，模型 id 原样保留；按函数名检索即可定位」）——经我对照 `lib/wrapper.js:379-387`（`const resolved = await base.resolveModel(...)` → `return { ...resolved, provider: wrapRoute, inputModalities: modalities }`）**表述准确**，且**不再逐字引用实现行**（碰撞源消除，取证 #5 的 ③ = 0 即其直接结果） |
| 12 | 断言集合机比对（R1 → R2） | 标签集：`e13ca17` = **1778** → `d301859` = **1779**；REMOVED = 2（`9h-2` 标签追加「在其声明单源」、`9h-3` 标签追加「精确匹配」——**均为同址改名**）/ ADDED = 3（上述两条改名 + 新增 `9h-2b`）⇒ **实质零断言删除、净增 1**（与守卫套件 159→160 一致）✅ |
| 13 | 对象串发射单点性（额外核验） | 逐对象统计其在**声明源**中的出现次数：**21/21 均为 1 次**（`非单点 = 0`）⇒ 无「同源多处命中」暴露面（对 P3-2(new) 的可选硬化提供现状基线） |
| 14 | 无死代码/无残留旧名（除 1 处注释） | `ANCHOR_SOURCES` / `selfSatisfied` 均被消费；旧 `+「label」` 分支已删；代码中表名统一为 `ANCHOR_OBJECTS_3`（`:833`）；**唯一残留旧名 = `:819` 的注释文本「ANCHOR_OBJECTS 首列」**（未跟随改名，见 P3-3(new)） |
| 15 | 演示脚本与证据在位 | `.test-home/fix040-r2-demo.mjs` + `fix040-r2-demo-output.txt` + `fix040-r2-selfsatisfy.mjs` + `fix040-r2-selfsatisfy-output.txt` + `fix040-r2-parsedebug.mjs` 均在档；其场景 ①/② 的变异目标（裸标签断言 / wrapper 实现行）在当前 HEAD 实存 ⇒ 演示非空转（本审查**未复跑**，见 §六） |
| 16 | 推送状态（裁定依据） | `origin/main` = **`cf0e935`**；`git rev-list --count origin/main..HEAD` = **14**（10 + 2 + 2）⇒ 本批**全部未 push**；R0/R1 机录与报告在档 |
| 17 | 环境隔离（真实环境防护） | 守卫与门控为只读/临时 `DSH_HOME` 隔离（R0 取证 #19 同源）；本审查全部变异为**内存字符串替换**，未对工作树执行任何写操作 ⇒ 零残留风险 |

---

## 二、R1（含 R0 未闭合项）逐条处置

### R1 findings

| R1 项 | 处置 | 证据 |
|-------|------|------|
| **P1-1(new)** 对象核验四源析取 ⇒ 6/21 自满足、两静默绿盲区 | **已修复（独立复现）** | 取证 #4（基线绿；smoke 裸标签改名 ⇒ 3 判红**原为 0**；wrapper 实现行改写 ⇒ 1 判红**原为 0**；另 ③④ 亦判红）+ #5（自满足三类独立复算全 0）+ #6（9h-2b 三类可达）+ #11（metrics 注释不再逐字引用实现行，碰撞源消除） |
| **P2-1(new)** `isKnown` 子串式 ⇒ 未披露假阴性通道 | **已修复（独立复现）** | 取证 #7（子串注入**判红**，R1 期静默绿；R0 死锚形态仍判红）+ #8（20/20 候选接受、零误红）+ #9（`+「label」` 分支删除后语义仍覆盖）+ #12（detail 字段改名 `candidates`→`tableKeys` 随 diff 落地） |
| **P3-1(new)** `<12 字符` 阈值盲区未披露 | **已修复** | 取证 #10：注释如实标注「短名**不参与本项** ⇒ 短名死锚不会被捕获」+ 现状数值（零 <12、最短 22），**不再声称无漏判** |
| **P3-2(new)** `:718-719` / `:749-751` 覆盖表述过度 | **已修复（逐字核对）** | 现行 `:718-720` = 「本项判『清单内连续锚串』这一形态，不判跨行/合并写作/片段形态…**入表完备性**由 9h-3 覆盖；其**对象存活**是否被核验取决于该名是否在 9h-2 表内（表内 = 按声明单源核验；表外 = 无核验）——**不声称闭环**」；9h-2 注释 `:747-752` 明示「9h-3 只保证『候选名已入表』，**不等于**对象存活已被覆盖（两者判据不同、不可互相替代），故不声称『全部候选锚名均有对象核验』」⇒ **与实现一致** ✅ |

### R0 未闭合项（本轮无变化，均为已裁定的延后项/可选）

| R0 项 | 本轮状态 | 依据 |
|-------|---------|------|
| P2-2 归因更正（0o600 实在 `1b5cead`，一笔两问题） | **正当延后（台账）**；**历史不回改裁定确认** | R2 两笔未触 `tests/smoke.mjs` / `lib/service.js`（取证 #1）；提交信息亦未声称闭合该项（见 §三.5） |
| P2-3 `4e5c848` 中间态 ubuntu 口径自相矛盾 | **正当延后（台账）** | 同上；最终口径 `#SKIP 6 / #SKIP 2` 已于 R0 穷举复核、R1/R2 门控复跑实测 Windows 侧一致 |
| P3-2（R0）`.test-home` 取证件未入治理记录 | **仍未满足（台账）** | 本轮**又新增 3 个**取证件（`fix040-r2-{demo,selfsatisfy,parsedebug}.mjs` + 2 份输出，取证 #15）⇒ 归档动作仍未执行（应按动作 5 机录） |
| P3-3（R0）镜像锚数字（每文件 15 处/自身锚 9 处 vs「各 12」） | **正当延后（台账）** | R2 未触 `lib/client.js` / `tests/served-client.js` |
| P3-4（R0）`CHANGELOG.md:59` 的 `#SKIP 1` 被 supersede（已发布节冻结） | **正当延后（台账）** | R2 未触 `CHANGELOG.md` |
| P3-5（R0）`.cmd` 断言依赖实机 `ComSpec` | **未修（可选）** | R2 未触 `tests/smoke.mjs` / `lib/service.js` |
| P3-8（R0）`README L125` 台账引用位置漏 2 处 | **正当延后（台账）** | R2 未触 `lib/service.js` / `tests/routing-paths.mjs` |
| R0 P2-1 / P3-1 / P3-6 / P3-7、R1 P1-1(A)(B)(C) | **已闭合（R1 已记，本轮无回退）** | 取证 #2/#3（9h 全绿、门控 exit 0）；取证 #12（零实质删除） |

**新引入（本轮）**：**无 P0~P2 级新引入**；仅 3 条 P3（代码卫生 / 残留局限 / 注释旧名），见 §四。

---

## 三、复审要求 7 项逐项结论

### 1. P1-1(new) 是否真闭合（核心）—— **真闭合（我复用了 R1 的谓词结构独立复算）**

- **① smoke 侧裸标签断言改名 ⇒ 判红**：`deadAnchors` = **3**（`smoke: check('vision call returns text without echoing injected images (B)'` / `check('memory segment carries id and untrust annotation'` / `check('current-turn image not double-injected as memory'`）——**R1 期同变异为 0（静默绿）** ⇒ 盲区关闭 ✅
- **② `lib/wrapper.js` 实现行改写 ⇒ 判红**：`deadAnchors` = **1**（`wrapper: return { ...resolved, provider: wrapRoute, inputModalities: modalities }`）——**R1 期为 0** ⇒ 盲区关闭 ✅（成因链亦已消除：`metrics.mjs:118-125` 注释不再逐字引用该实现行，故它只可能在 `wrapper` 源命中）
- **③ 基线 ⇒ 绿**：`deadAnchors = 0`、`missingAnchors = 0`；守卫套件与门控 exit 0（取证 #2/#3）✅
- 附加（我自主加测）：**④** metrics 自引用 `checks.push([…` 项改名 ⇒ 1（判红）；**⑤** `console.log('attachmentIds resolution (M2):'` 改名 ⇒ 1（判红）⇒ 「判别前缀」使每个对象只可能命中真正的断言发射点。

### 2. 自满足三类是否真为 0 —— **是（独立复算，非采信声明）**

- 我按 §9h-2b 同构的谓词对实际表 + 实际四源复算：① `anchor === object` = **0**；② `object ⊂ anchor` = **0**；③ 对象在非声明源出现 = **0**；④ 对象不在声明源 = **0**（取证 #5）⇒ 与 9h-2b 判绿一致，且**四类互不掩盖**（①②在表内自查，③跨源自查，④为 9h-2 主判据）。
- **判据本身可判别（非恒真）**：三类逐类构造违规行均命中（取证 #6）⇒ 9h-2b 不是「注释声称」而是有牙齿的判据（其失败路径为 `check(...)` false ⇒ `failures` ⇒ `process.exit(1)` ⇒ `run-all` 非零汇总）。

### 3. §9h-3 精确匹配 —— **子串通道已关闭；无新漏判；`+「label」` 语义仍被覆盖**

- **关闭实证**：R1 的静默形态（`「takes over default model」`）现在 `unregistered = 1`（判红）；R0 死锚形态仍判红（取证 #7）。
- **无新漏判（误红反查）**：20/20 现有候选名全部被接受（精确 12 + `「label」`结尾 8）；合并写作 `「A」+「B」`、`<前缀>「label」` 复合锚、裸标签表键三类合法形态均在覆盖内（取证 #8/#9）。
- **`+「label」` 分支删除**：该类表键自 R0 P3-6 已无实例，「后半片段」注册语义由「结尾式」判据等价覆盖 ⇒ **无缺口**（取证 #9）；detail 字段已改名 `tableKeys`（语义正确性修复）。

### 4. 新引入（不实表述 / 判据退化 / 死代码）—— **无 P0~P2；3 条 P3**

- **不实表述**：逐条核对本轮新增/修改的 4 处注释（`:43-44` 头部面说明、`:718-720` 覆盖界定、`:747-752` 9h-2 覆盖界定、`:795-800` 9h-3 阈值披露）与其实现**一致**；`metrics.mjs:118-125` 的语义描述经对照 `lib/wrapper.js:379-387` **准确**（取证 #11）⇒ 无新增不实。
- **判据退化**：无。三处判据（9h 单源核验、9h-2b 零自满足、9h-3 精确入表）**方向均为收紧**，且我逐条做了「原盲区 ⇒ 现判红」的反向验证（取证 #4/#7）。
- **死代码**：旧 `+「label」` 分支已删；`ANCHOR_SOURCES` / `selfSatisfied` 均被消费 ⇒ 无新增死代码。唯一代码卫生项 = `ANCHOR_SOURCES[origin]` 未校验来源名（P3-1(new)）；唯一文档卫生项 = `:819` 注释仍用旧变量名（P3-3(new)）。
- **9h-2b 是否可判别**：是（取证 #6 逐类构造 + 失败传导路径同 9h 家族）。

### 5. R1 feedback 全条处置 + 历史提交裁定 —— 见 §二；**历史提交不回改（确认）**

- R1 四项（P1-1(new) / P2-1(new) / P3-1(new) / P3-2(new)）**全部已修复**；R0 未闭合项状态见 §二表（均为已裁定的**正当延后（台账）**或**可选未修**，非未满足的阻塞）。
- **「历史提交不回改」裁定确认**：**维持不回改**（对齐 Coordinator 裁定）。依据：① 证据链以 commit hash 为可复查锚（R0/R1/R2 报告、机录、Developer 取证件均引 hash），改写即断链；② 差异性质为记录性口径（P2-2 归属、P2-3 中间态数字）而非代码语义；③ 项目已有「如实注记 → 台账更正」先例（FIX-038 R1 的空 diff 口径、`e1fe160`「自引入」表述均未回改）。
- **事实补充（不改裁定）**：本批 **14 笔全部未 push**（`origin/main` = `cf0e935`，取证 #16）⇒ 该裁定属**纪律选择**而非不可变性强制；若选择 push 前 squash/rebase，MUST 同步 R0/R1/R2 报告与机录中的全部 hash 引用（成本 > 收益，**不建议**）。

### 6. 越权面 + 断言集合 + 门控复核 —— **全部通过**

- **越权面零**：两笔仅 `tests/host-abi-health.mjs` + `tests/metrics.mjs`（取证 #1）；`.governance/**` / `AGENTS.md` / `CHANGELOG.md` / `package.json` 零触碰。
- **断言集合**：REMOVED = 2（均为**同址改名**：`9h-2` / `9h-3` 标签追加限定语）+ ADDED = 3（含新增 `9h-2b`）⇒ **实质零删除、净增 1**；守卫套件断言数 **159 → 160**（取证 #2/#12）。
- **门控复核**：`node tests/run-all.mjs` = `ALL 20 SUITES + 4 RUNNER MODULES (via smoke.mjs) PASSED (28.5s)  #SKIP 2 (smoke.mjs×2)` **exit 0**（取证 #3），Coordinator 的 27.9s / 160 断言两项机验**抽样复核一致**。

### 7. 关键判定：本批（14 笔）是否具备 push 条件 —— **具备；`unresolved_blockers = 0`**

- **判定：可 push** ✅。理由：本批交付面（锚清扫 12 处 + 锚守卫扩展 + 9h/9h-2/9h-2b/9h-3 四层机器看护 + 平台中立化/可见 skip 收口）**全部经本轮与前三轮独立验证**；R0/R1 的 5 项阻塞级发现（P1-1 死锚、P1-1(new) 自满足退化、P2-1 stale 漏项、P2-1(new) 子串通道、P3 系列）**均已成闭环或已裁定为台账项**；本机门控与守卫套件双绿，零实质断言删除、零越权。
- **`unresolved_blockers = 0`**（P0 = 0 / P1 = 0 / P2 = 0；余下 P3×3 为纯卫生/局限项，不构成阻塞）。**round = 2 ≤ 2，未触及 T2 熔断**（round ≥ 3 仍 NEEDS_CHANGE 才需转 BLOCKED）⇒ 本结论为**通过终态，复审链结束，无需 R3**。
- **push 时必须遵守的口径（不得越界声称）**：
  1. **不得声称「CI 已验证」**：ubuntu 侧**未实测**；`#SKIP 6 (host-contract.mjs×2, smoke.mjs×4)` 仍为**预期值**（R0 穷举推导 + 本机 Windows 侧 `#SKIP 2` 实测复核）；push 后由 Coordinator 以首跑日志逐行核验（9h 家族为**纯文件读取判据、平台中立**，ubuntu 风险低，但仍以实测为准）。
  2. **push 后回填**：ubuntu 首跑结论（含 `#SKIP 6` 逐行 + 9h/9h-2/9h-2b/9h-3 绿）入 evidence-log。
  3. **台账项随发布/后续批记录**（不阻塞 push）：P2-2 归因、P2-3 中间态口径、R0 P3-2 取证件归档（现含 R1/R2 共 8+ 个文件）、P3-3 镜像锚数、P3-4 `CHANGELOG:59`、P3-5（可选）、P3-8 台账⑥引用位置。

---

## 四、新发现（本轮）

### P0 / P1 / P2 —— 0 条
无。

### P3（讨论/建议，不要求修改）—— 3 条

**P3-1(new)｜`ANCHOR_SOURCES[origin]` 未校验来源名：未来误写源名会抛 TypeError（套件崩溃而非干净判红）**
- 位置：`tests/host-abi-health.mjs:774-780`（`ANCHOR_SOURCES`）与 `:781-782`（`deadAnchors` filter）
- 事实依据：`ANCHOR_OBJECTS_3` 的第三个元素为字符串源名，`ANCHOR_SOURCES[origin].includes(...)` 在 `origin` 未命中键时返回 `undefined` ⇒ 调用 `.includes` 抛 TypeError（我以 `origin = 'smoke '`（尾随空格）复现该语义）。该失败**非静默**（套件非零退出、run-all 报套件失败并打印完整输出），但诊断信息是栈回溯而非「源名非法」的明确报文。
- 建议（可选，~2 行）：构造 `ANCHOR_SOURCES` 时校验 `origin ∈ {smoke, parity, wrapper, metrics}`，非法即 `throw new Error('unknown anchor source: …')` 或让 filter 用 `ANCHOR_SOURCES[origin]?.includes(assertion) ?? false` 并把非法源名计入独立数组判红。

**P3-2(new)｜§9h-2b 只覆盖「非声明源」碰撞，不覆盖**同源**注释引用（残留局限；当前无暴露）**
- 位置：`tests/host-abi-health.mjs:795-800`（`foreignSourceHits` 判据）
- 事实依据：判据第三类为「对象串出现在**其它**源」，故若在**声明源自身**的注释中引用带判别前缀的对象串、同时删除真断言，`deadAnchors` 仍会判绿（字符串匹配的固有局限；R1 的实际缺陷即此类碰撞的**跨源**变体，已由本类堵住）。我实测当前 **21/21 对象在其声明源中恰出现 1 次**（取证 #13）⇒ **目前无任何暴露面**。
- 建议（可选，~2 行）：把「对象在声明源中**出现次数 === 1**」纳入 9h-2b 第四类（数据在遍历中已在手），可把该残留彻底形式化关闭；不修亦不阻塞（现状零暴露 + 已知局限非声称无漏判）。

**P3-3(new)｜`:819` 注释仍以旧变量名指代表（代码已改 `ANCHOR_OBJECTS_3`）**
- 位置：`tests/host-abi-health.mjs:819`（注释「…MUST 登记在 **ANCHOR_OBJECTS** 首列…」）vs `:833`（`ANCHOR_OBJECTS_3.map(...)`）
- 事实依据：旧名 `ANCHOR_OBJECTS` 在代码中已不存在（grep 仅此一处注释命中，取证 #14）；纯文档 nit。
- 建议（可选）：注释改用 `ANCHOR_OBJECTS_3`（或统一把变量名回归为 `ANCHOR_OBJECTS`——三元组形式下 `_3` 后缀非必要）。

---

## 五、硬门槛裁决

| 门槛项 | 阈值 | 实测 | 裁决 |
|--------|------|------|------|
| P0 阻塞问题数 | = 0 | 0（§四 P0 无） | **PASS** |
| 5 维度全覆盖 | = 100% | 见下逐维度结论 | **PASS** |
| 每条发现标注级别 | = 100% | 新发现 P3×3 均带文件:行号 + 事实依据 + 建议；R1 4 项 + R0 未闭合 8 项逐条带处置状态 | **PASS** |
| 设计一致性检查 | 已完成 | 与项目原则 4（防护网）、**原则 8（失败/降级 MUST 可观测——R1 的静默绿通道已关闭且新增 9h-2b 把该机理本身关进看护）**、原则 9（守卫自证能力）、FIX-037 ①「禁静默降级」、`ci.yml` 头部契约、P5 单一路径逐项比对：**无偏离**（本轮三处判据全部方向收紧，且逐条经「原盲区 ⇒ 现判红」反向验证） | **PASS** |
| AI 代码专项 5 项 | 全部完成 | ① mock 残留：无（零夹具新增）② 硬编码：无（无硬编码通过；`ANCHOR_OBJECTS_3` 为判据输入）③ 幻觉引用：**无**（R0 项已在 R1 消除，本轮三条新对象逐字实存且 R2 未新增引用）④ 未实现 TODO：无（两笔 diff `TODO/FIXME/XXX/HACK` 零命中）⑤ 过度实现：无（2 文件、最小改动；无顺带重构；唯一形态问题 = 3 条 P3 卫生项） | **PASS** |

### 五维度逐项结论（摘要）

- **正确性 —— 通过**：三态仿真（基线绿 / 两个 R1 盲区判红 / 另两变异判红）与三类自满足复算（全 0）均由本审查独立复现（取证 #4/#5/#6）；`metrics.mjs` 改动为纯注释、断言本体零变化（取证 #11）；守卫套件 160 断言与门控均 exit 0（取证 #2/#3）。
- **安全性 —— 通过**：2 文件均为测试基础设施（零产品代码、零 `lib/**` 改动）；变异均为内存仿真，工作树零写操作（取证 #17）。
- **可维护性 —— 通过（附 3 项 P3 建议）**：表结构（三元组 + 声明源）自解释且注释充分（含 R1 失效机理与「不声称闭环」的如实界定）；`ANCHOR_SOURCES` 映射清晰；负向项均为卫生/可选硬化（P3-1/2/3）。
- **性能 —— 通过**：新增 9h-2b 为线性遍历（21 行 × 4 源 contains）；守卫套件整体仍 200ms 级、门控 28.5s（与前三轮同量级，无回退）。
- **测试覆盖 —— 通过**：净增 1 断言（9h-2b）且**零实质删除**；三层锚看护（入表完备性 9h-3 / 单源对象存活 9h-2 / 零自满足 9h-2b）互补且经可达性与非恒真性验证；残余覆盖边界（<12 字符短名、同源注释碰撞）**均已如实披露或实测零暴露**。

### 审查结论

> **APPROVED_WITH_NOTES**
> `unresolved_blockers=0`
>
> 理由：R1 的两项阻塞项**均已真闭合并被我以 R1 同构谓词独立复现**——对象核验改**声明单源 + 判别前缀**后，R1 的两个静默绿盲区（smoke 侧裸标签断言改名 / `lib/wrapper.js` 实现行改写）**双双判红**（`deadAnchors` 3 / 1，R1 期皆为 0）且基线仍绿；§9h-3 改**双向精确判据**后，R1 的子串注入形态**判红**（R1 期静默）且 20/20 现有候选名零误红；新增 §9h-2b 经我独立复算（三类全 0）+ 逐类构造（三类均可判红）确认**既零告警又可判别**；R1 P3-1/P3-2 的披露与表述改写**如实且与实现一致**；`metrics.mjs` 为纯注释改动、碰撞源已消除。守门套件 **160 断言 PASSED**、门控 **exit 0**、断言**实质零删除（净 +1）**、越权面零、零新引入 P0~P2。余下 **P3×3**（来源名校验、同源注释碰撞残留〔现状零暴露〕、注释旧变量名）与 R0 台账项（P2-2 / P2-3 / 取证件归档 / 镜像锚数 / `CHANGELOG:59` / `ComSpec` / `README L125` 引用位置）**均为非阻塞项**，记录为后续跟踪。**本结论为通过终态，复审链结束（无需 R3）；`round = 2` 未触及 T2 熔断。**

---

## 六、未验证项声明（事实依据红线）

1. **ubuntu 侧执行结果仍未实测**：`#SKIP 6 (host-contract.mjs×2, smoke.mjs×4)` 仍为**预期值**；本机无 Linux/WSL/docker ⇒ 不可复跑，**不得读作已验证**；`9h` 家族为纯文件读取判据（平台中立），ubuntu 风险低但仍以首跑日志为准。
2. **本审查未执行任何变异脚本、未写任何文件**：取证 #4/#5/#6/#7/#8 的仿真均为「读文件 → 内存字符串替换 → 按 9h-2/9h-2b/9h-3 判据同构复算」；Developer 的 `.test-home/fix040-r2-demo.mjs` 未由我复跑（其**输出文件** `fix040-r2-demo-output.txt` 亦未作为证据采信）。替代性验证 = ①变异目标串在当前 HEAD 实存（取证 #15）②判据同构复算结果与其声称逐项一致（取证 #4/#5/#7）③`git status` 确认工作树 `tests/**` 干净。
3. **CI 日志本体不可本地取证**（远端私有仓库）；本报告对未来 CI 的结论均为推导。
4. **推送状态为实测**：`origin/main` = `cf0e935`、本地领先 **14** 笔（取证 #16）。
5. **`9h-2b` 的「零自满足」结论范围**：仅就 `metrics.mjs` 的 21 对对照表与其四个声明源成立；不构成对全仓库锚的普适保证（该边界已在 9h-2/9h-3 注释中如实界定）。

---

## 七、建议 Coordinator 动作（按依赖排序）

1. **判定通过（本轮）**：结论 = **APPROVED_WITH_NOTES / `unresolved_blockers=0`** ⇒ 以 review-record CLI 机录本报告（canonical 名 `review-FIX-040-R2.md`）；**复审链结束，无需再 spawn Reviewer**（R0→R1→R2 三轮连续、终态为通过）。
2. **push（可执行）**：本批 14 笔具备 push 条件；push 后**立即以首跑日志核验** ubuntu 侧 `#SKIP 6` 逐行 + `9h / 9h-2 / 9h-2b / 9h-3` 绿，并把结果入 evidence-log；**任何对外表述不得声称「CI 已验证」**（首跑前）。
3. **证据归档**：`.test-home/fix040-*.mjs`（R0 4 个 + R2 3 个）与 `fix040-*.txt` 输出（R2 2 份）路径 + 复跑命令机录 evidence-log（R0 P3-2 项，仍待执行）。
4. **台账补录（不阻塞 push，收尾时一并）**：P2-2 归因更正（0o600 = `1b5cead`，一笔两问题）、P2-3 中间态口径记录（`4e5c848` → `08d562a`）、P3-3 镜像锚数（每文件 15 处 / 自身锚 9 处）、P3-4 `CHANGELOG:59` 被 supersede、P3-8 台账⑥引用位置补 2 处、P3-5（可选）；本轮 P3-1/2/3 可作为「下一批代码卫生」候选（非必须）。
5. **发布面提示**：本批 14 笔含测试基础设施与文档/注释改动 + 1 处纯增可选参（`resolveCliInvocation` 第三参）⇒ **不产生用户可见行为变化**（除 Windows 本地门控多 1 条 skip 行）；`package.json` / 版本位 / `CHANGELOG.md` 未被触碰。
