# 代码审查报告（review-FIX-041-R1-input）

- **round = R1**（R0 返工后复审；同一 Reviewer 实例）
- **前轮引用**：`REVIEW-FIX-041-R0` / 报告 `.governance/review-FIX-041-R0-input.md`（R0 = **NEEDS_CHANGE**，`unresolved_blockers = 1`；新发现 P2×2〔F-1/F-2〕+ P3×3〔F-3/F-4/F-5〕）
- **审查对象**：返工三笔 `de9d551`（F-1 溯源更正）/ `704a331`（F-2 在仓漂移锚收口）/ `0af6927`（F-3+F-4+F-5 批）；起点 `6c9553b`；受审 HEAD = **`0af6927`**；文件集 = `{lib/stats.js, tests/host-abi-health.mjs}`（`+28/−9`）
- **审查者**：Code Reviewer Agent（只读；未修改任何被跟踪文件；改判用仿真全部在内存；唯一写入 = 本报告 + `.test-home/fix041-r1-verify.mjs`）
- **执行依据**：三笔 `git show` 全量 diff 逐行 + HEAD 终态逐行 + **F-3 注释口径的独立复算**（按注释给出的三正则实跑）+ **F-2③ 五处漂移锚逐处实读**（锚行 + 目标文件实际位置）+ 断言标签集机比对 + 守卫/门控独立复跑 + 裸轮次引用全量抽查
- **结论（四态）**：**APPROVED_WITH_NOTES**
- **独立结构字段**：`unresolved_blockers=0`
- **计数（本轮新发现）**：P0 = 0 / P1 = 0 / P2 = 0 / P3 = 2（均为**批外**存量登记，不阻塞）

**一句话理由**：R0 的两项阻塞项**均已真闭合且经我独立复现**——①F-1：`FIX-041 R2` 全仓**零命中**，三处改为 `FIX-040 R2 P3-1(new)/P3-2(new)`，其中 `P3-1(new)`/`P3-2(new)` 与 `REVIEW-FIX-040-R2` 报告 §四**逐字对应**（`.governance/review-FIX-040-R2-input.md:125 / :130`）+ 机录 `REVIEW-FIX-040-R1/R2` 在档（`evidence-log.md:635/:637`）⇒ **引用可解析、来源真实**；同类裸轮次引用 `:724 / :779 / :782 / :817 / :839 / :882` 已加 `FIX-040` 前缀（我抽查 7 处，加的轮次与真实轮次**均相符**），仅 `:708` 保持裸 `R1` 且**同句含 `FIX-040 扩面`** ⇒ **豁免成立**；②F-2：`lib/stats.js:104` 的行号锚已去行号改符号名式（`lib/oauth-llm.js:43` 出现次数 = **0**、`OAUTH_PROVIDER` 在位 = true），既有 `ANCHOR_CASES` 条目扩为 `stale: ['host-route.js:55','lib/oauth-llm.js:43'] / fresh: ['host-abi/version.js','OAUTH_PROVIDER']`（`tests/host-abi-health.mjs:717`）⇒ **改写 + 看护一并，零新幻觉引用**。**两项 Coordinator 裁定我均判正当**（165 而非 166 = 扩既有条目不增 check 数，断言数守恒有实测支撑；F-2③ 另批 = 该族漂移面比我 R0 判断**更大**，且需「逐处语义判定」的归属判定前置）。**更重要的自我更正**：我 R0 的「`lib/stats.js:104` 是本批范围外唯一在仓漂移锚」**被证伪**——我复核了 Developer 报的 5 处**全部属实**，并**独立发现 2 处其清单未列的漂移锚**（`lib/host-abi/inject-manifest.js:16` 与 `tests/client-render.mjs:1982/:1987`），另有 R0 既有批次的 `R2` 行号引用因本轮增行而失效 3 处（`9h`/`9h-2`/`9h-3` 块内注释行号偏移 ~16~19 行）——**新增行数 = 0 而旧注释行号失效**正是该族需另批系统处理的直接证据。本批交付面：断言标签集合 **REMOVED = 0 / ADDED = 0（149 → 149）**、`ANCHOR_CASES` 条目数 **16 不变**、守卫 **165 断言 PASSED**、门控 exit 0、镜像 SHA256 恒等、越权面零（2 文件）、三笔均纯注释（`lib/stats.js` 逐行核对）。⇒ **判通过终态，复审链结束（无需 R2）。**

---

## 一、R1 独立取证清单（全部实测）

| # | 事实 | 命令 / 结果 |
|---|------|------------|
| 1 | 范围与越权面 | `git rev-parse HEAD` = `0af6927…`；`git log --oneline 6c9553b..0af6927` = **3 笔**（`de9d551` → `704a331` → `0af6927`，顺序与声明一致）；`git diff --stat` = **2 文件 `+28/−9`**（`lib/stats.js +1/−1`、`tests/host-abi-health.mjs +27/−8`）✅；`git diff --name-only … -- AGENTS.md CHANGELOG.md package.json .governance` = **空**；`git status --porcelain` 中 `lib/**`/`tests/**` **零项**（仅治理在途 `M evidence-log.md` + 两份机录/报告为 `??`，非本批 commit 内容） |
| 2 | **R1 检查点 1（F-1）** | `git grep -n 'FIX-041 R2'` = **零命中（exit 1）** ✅ |
| 3 | **引用可解析性（F-1 实质）** | `REVIEW-FIX-040-R2` 报告 §四**确含** `P3-1(new)`（`review-FIX-040-R2-input.md:125`＝「`ANCHOR_SOURCES[origin]` 未校验来源名…」）与 `P3-2(new)`（`:130`＝「§9h-2b 只覆盖『非声明源』碰撞，不覆盖**同源**注释引用…」）✅；机录 `REVIEW-FIX-040-R1` / `REVIEW-FIX-040-R2` 均在档（`evidence-log.md:635 / :637`）✅ ⇒ **来源真实且指代精确** |
| 4 | **裸轮次引用豁免判定** | 全量抽查 `:39 / :708 / :724 / :774 / :779 / :782 / :817`（Coordinator 指定）+ 自查 `:839 / :850 / :882`：`:724`（现 `如实界定（FIX-040 R2 P3-2(new)）`）、`:779`（同）、`:782`/`:817`/`:839`（现 `FIX-040 R1 P1-1(new)`）、`:882`（现 `FIX-040 R1 P2-1(new)`）、`:850`（现 `FIX-040 R2 将该类表述为…`）**均已获得可解析轮次**，且我核验其轮次与真实报告**相符**（P1-1(new)/P2-1(new) 属 FIX-040 **R1**，P3-1/2(new) 属 FIX-040 **R2**）✅；`:708` 仍为裸 `R1`，但**同一句**含 `FIX-040 扩面（R1 N4）` ⇒ **豁免成立**（`git grep 'FIX-041 R2'` 零命中已排除「裸 R 被读成 FIX-041 轮次」的歧义源） |
| 5 | **R1 检查点 5（镜像恒等）** | `lib/client.js` 与 `tests/served-client.js` SHA256 **仍同 = `23C5C5DA28716AD4AA25359DB2071F6CE94324244D1C92F8E3ECE4CEB56F6B87`** ✅ |
| 6 | **守卫套件（独立复跑）** | `node tests/host-abi-health.mjs` = **`ALL HOST ABI HEALTH TESTS PASSED (165 assertions)`，exit 0** ✅（＝R0 的 165；见 §三.5 裁定核验） |
| 7 | **门控（独立复跑）** | `node tests/run-all.mjs` = `ALL 20 SUITES + 4 RUNNER MODULES (via smoke.mjs) PASSED (22.0s)  #SKIP 2 (smoke.mjs×2)` **exit 0**（Coordinator 22.4s 同量级）✅；两条 skip = `0o600`（`tests/smoke.mjs:1266`，条件 `process.platform !== 'win32'` ⇒ **Windows 侧可见 skip**）+ `POSIX online checks`（`tests/install-entry.mjs:350`，`posixShell() === null` 触发 ⇒ 本机无 sh/curl）⇒ **与 FIX-040 链口径一致、非本批引入** |
| 8 | **断言集合机比对（R0 HEAD → R1 HEAD）** | 标签集：**149 → 149**，`Compare-Object`（排序去重）**逐元素相等 = True**；**REMOVED = 0 / ADDED = 0** ✅；`ANCHOR_CASES` 条目数 = **16**（不变）；`check(...)` 行为数 = 165（R0 已实测 165）⇒ **三笔零断言增删、零覆盖面回退** |
| 9 | **F-2 闭合实测** | `lib/stats.js`：`lib/oauth-llm.js:43` 出现次数 = **0**；`OAUTH_PROVIDER` 出现次数 = **1**（唯一注入点）✅；`tests/host-abi-health.mjs:717` = `{ file: ['lib','stats.js'], stale: ['host-route.js:55','lib/oauth-llm.js:43'], fresh: ['host-abi/version.js','OAUTH_PROVIDER'] }` ✅（既有条目扩对、未新增条目） |
| 10 | **全仓 `lib/oauth-llm.js:43` 残留** | `git grep`（excl `.governance`）= 仅 3 命中，**全部在 guard 内部**：`:714`（F-2 说明注释）/ `:717`（stale 清单，本批新增）/ `:760`（R0/W2b 的 stale 清单）⇒ **产品/测试实文本零残留** ✅ |
| 11 | **F-3 计数口径独立复算（核心）** | 我按 `tests/host-abi-health.mjs:752-759` 注释给出的**三正则 + 匹配次数口径**，对 `lib/client.js`（HEAD）实跑：**`[13, 17, 11]`，合计 = 41** ✅ **与注释逐字吻合**；且我复得 R0 的 13/17/8 = 38 亦由同一脚本复现（差异源 = 注释已如实点明的「`L5682-5760` 区间只计首发 / 排除 `/:342` 形态」）⇒ **交付后 41 可复算，「47 = 15/17/15」的分量差（−2/−0/−4）与 6 处符号名化的锚点数量级自洽**（我复算的增量面 = `path:line −2`、裸 `:NNN −4`） |
| 12 | **F-2③-1 漂移复核** | `lib/host-abi/health.js:4` 锚行原文 = 「`lib/preset-defaults.js:116-124`：模块级有界环形 + 字段白名单 + 截断 + try/catch 全防护」；实测 `lib/preset-defaults.js:116-124` = 模块文档尾（`:116 *`、`:117 @module`、`:118 */`）+ import 行（`:120`）+ `SWAP_LOG_LIMIT`（`:122-123`）——**无环形**；`presetDiag = { entries: [] }` 实在 **`:137`** ⇒ **漂移确证（偏离 ≥13 行且对象不同）** ✅ |
| 13 | **F-2③-2 漂移复核** | `lib/host-abi/inject-manifest.js:23` 锚行原文 = 「客户端 fiber inject 声明（9 面——迁移自 `lib/client.js:5060` 同步锚定…）」；实测 9 面 `const inject = ['slots', …]` 在 `lib/client.js:5459`（`:5060` 为 hostFaceDiag 注释区）⇒ **漂移确证（≈399 行）** ✅；且 `git log` 显示该漂移**批前即存在**（本批未触该文件）⇒ **非本批引入** |
| 14 | **F-2③-3/4/5 漂移复核** | `tests/rpc-shadow-guard.mjs:6`（`new StatsStore(...)`（`lib/service.js:673`））⇒ 实测 `StatsStore` 命中 `39 / 725 / 729 / 3415`，**`:673` 不在其中** ⇒ 漂移 ✅；`:7`（RPC 方法 `stats()`（`lib/service.js:3172`））⇒ 实测 `:3172` = `...images.map((image) => ({ type: 'input_image'…` ⇒ **对象不符** ✅；`tests/client-render.mjs:533`（`agentPresetsServiceOf 先例 lib/preset-defaults.js:100-108`）⇒ 实测命中 `95 / 120 / 172 / 528` ⇒ **`:100-108` 区间不含定义** ✅ |
| 15 | **我的 R0 判断更正 + 清单未列项（自主扩展）** | 按同一「in-repo `.js:` 锚」口径复扫 5 个文件，**Developer 的 5 处全部属实**；另发现 **2 处其清单未列**：**`lib/host-abi/inject-manifest.js:16`** 引 `dsh-client-modules lib/client.js:265-268`（宿主锚，但 `lib/client.js:265` = `inputTokens: wv.number()…` ⇒ **宿主侧亦漂/不可解析**）；**`tests/client-render.mjs:1982`** 引 `dsh-client-connection 0.1.2-rc.1 lib/client.js:4754-4825`（**宿主锚**，我 R0 已实测宿主侧已漂至 `function isLoopbackHostname @6273`）与 **`:1987`** 引 `dsh-client-ui-settings-models lib/client.js:2842-2848`（**宿主锚**，我 R0 实测 MISS）；`:515/:518`（`dsh-client-ui-model-selection lib/client.js:170 / :193`）与 `:1855`（`lib/index.js:19`）为宿主锚或已失效事实（`API_REMOTE_FORWARDED_EVENTS` 在 `lib/index.js` 零命中）⇒ **该族盘点面比 Developer 声明更宽（≥7 处）** |
| 16 | **R0 终审面观察（新增行数 = 0 而旧注释行号失效）** | `tests/host-abi-health.mjs:734` 仍写「现行 `:718-720` = …」而该文字实在 **`:734-736`**；`:763-768` 仍写「9h-2 注释 `:747-752`」而实在 **`:763-768`**；`:811-816` 仍写「9h-3 注释 `:795-800`」而实在 **`:811-816`** ⇒ **同一文件内注释自指行号偏移 ~16~19 行**（**零新增行也已失效** ⇒ 该族必须在另批用符号名/节名式统一处理，见 §四 P3-2） |
| 17 | **纯注释性（三笔）** | `lib/stats.js` 逐行核对：唯一改动 = 注释行 `lib/oauth-llm.js:43` → `lib/oauth-llm.js 的`（`-1/+1`，**非注释改动行 = 0**）✅；三笔 `+` 行 `TODO/FIXME/XXX/HACK` **零命中** ✅；`de9d551` / `0af6927` 的 diff 全为 `//`/`*` 注释行（我逐 hunk 核对，判据与代码行零改动）✅ |
| 18 | **推送状态与环境防护** | `origin/main` = `5cc884c`；`git rev-list --count origin/main..HEAD` = **10**（7 + 3）⇒ **全部未 push** ✅；本轮**未触 `$DSH_HOME`**（本审查亦未触：全部为仓库内只读 + 内存仿真；`.test-home/fix041-r1-verify.mjs` 为 gitignored 只读脚本） |

---

## 二、R0 findings 逐条处置

| R0 项 | 级别 | 处置 | 独立证据 |
|-------|------|------|---------|
| **F-1** 来源标「FIX-041 R2」而该轮不存在 | P2（阻塞） | **已修复** | 取证 #2（`FIX-041 R2` 零命中）+ #3（`FIX-040 R2 P3-1/P3-2(new)` 与 R2 报告 `:125/:130` 逐字对应、机录在档）+ #4（同类裸轮次引用已补 `FIX-040` 前缀，加的轮次全对；`:708` 豁免成立） |
| **F-2** `lib/stats.js:104` 在仓漂移锚（`lib/oauth-llm.js:43` → 实 `:47`） | P2（建议同修） | **已修复** | 取证 #9（stale 0 次 / `OAUTH_PROVIDER` 1 次；`:717` stale+fresh 各 +1 串）+ #10（全仓实文本零残留）+ #6（守卫 165 断言绿，新对判据在位且基线绿 ⇒ 零误红） |
| **F-3** 47 计数不可复现 | P3 | **已修复（可复算）** | 取证 #11：我按注释口径实跑 **`[13,17,11] = 41`** 与注释一致；38 口径亦复现并被注释如实记录；注释已写明「数字必须连口径引用、非长期基线」 |
| **F-4** 9h-2d 的 `count !== 1` 含 `count === 0` 未披露 | P3 | **已修复** | `:861-867` 新增披露块：明示「含 `count === 0`」「与 `deadAnchors` 同时判红 = **有意的冗余双报**」「**不是**并存双实现」「9h-2 判据本体（含『非声明源命中』语义边界）**不得**因本项简化」+ detail 语义说明 ⇒ **误读面消除**；谓词本体逐字未变（零判别力变化） |
| **F-5** 「全仓该族零残留」限定语不足 | P3 | **已修复** | `:737-744` 限定语改为「只在 **shipped 面（lib/** + tests/**）**成立」并点名 `docs/release/release-checklist-v0.3.2.md:30` / `version-plan-v0.3.2.md:82` / `CHANGELOG.md:25`（历史快照，刻意不清扫）⇒ 与我的实测穷举（R0 取证 #13）**一致** |
| **R0 附带项**：F-1 同类裸 `R1`（9h-2d 块内） | — | **已修复（随 `0af6927`）** | 取证 #4（现为 `FIX-040 R1 的实际缺陷…`） |

**新引入（本轮）**：**无 P0~P2**；仅 2 条 P3（均为**批外存量**登记，见 §四）。

**R0 判断的自我更正（如实标注）**：R0 报告 §四 F-2 称「`lib/stats.js:104` 是本批范围外**唯一**的 shipped 在仓行号锚」——**该「唯一」被证伪**：我复核 Developer 所列 5 处漂移锚**全部属实**，并另发现 2 处其清单亦未列（取证 #15）。更正理由：我 R0 的穷举**只扫了 `lib/client.js` 的锚族**，对 `lib/stats.js:104` 的发现来自一次「README L 族」grep 的意外命中，**未做全仓 in-repo 锚族扫描** ⇒ 该「唯一」属**证据范围不足的过度概括**（非事实错误：`lib/stats.js:104` 确为漂移锚，且已被本批正确收口）。

---

## 三、复审要求 7 项逐项结论

### 1. F-1 是否真闭合 —— **真闭合（引用可解析、来源真实、豁免成立）**

- `FIX-041 R2` **全仓零命中**（取证 #2）；三处改为 `FIX-040 R2 P3-1(new)` / `P3-2(new)`，与 `review-FIX-040-R2-input.md:125/:130` **逐字对应**，机录 `REVIEW-FIX-040-R1/R2` 在档（`evidence-log.md:635/:637`）⇒ 「每条结论可追溯到可复查事实」恢复成立（取证 #3）。
- 同类裸轮次引用：Developer 在 `0af6927` 中为 `:724` / `:779` / `:782` / `:817` / `:839` / `:850` / `:882` 补 `FIX-040` 前缀 —— 我**抽查 9 处并逐处核对轮次归属**：`P3-1(new)/P3-2(new)` 确属 **R2**、`P1-1(new)/P2-1(new)` 确属 **R1** ⇒ **加的前缀全部为真**（无「为避免歧义而引入新错误」）。
- `:708` 的裸 `R1` **豁免成立**：同句含 `FIX-040 扩面（R1 N4）`，且 `FIX-041 R2` 已零命中 ⇒ **无可指证的不可解析项**。**残余（非阻塞）**：`:708` 与 R0 既有批次的 `R2` 式引用属**风格不一致**（与已限定块对比），登记为 §四 P3-2。

### 2. F-2 是否真闭合 —— **真闭合；零新幻觉引用**

**（本轮修复块与新增披露块的实测位点，供复算对齐）**

| 内容 | 实测位点 |
|------|---------|
| F-2 说明注释 + `lib/stats.js` 条目（stale/fresh 各 +1 串） | `tests/host-abi-health.mjs:713-716`，条目本体 **`:717`** |
| F-5 限定语收窄块 | `:737-744` |
| F-3 计数口径块（三正则 + 匹配次数 + 47(15/17/15) → 41(13/17/11) + 38 口径差异） | `:752-759` |
| F-4 重叠披露块 | `:867-871` |
| F-5 代码行（`ANCHOR_OBJECTS` 旧变量名 → `ANCHOR_OBJECTS_3`，R0/W1b） | `:880` |

- 新锚对象实存：`lib/stats.js` 内 `OAUTH_PROVIDER` 出现 **1 次**（唯一注入点），其权威定义 `lib/oauth-llm.js:47` 实存（R0 已核）；stale 串 `lib/oauth-llm.js:43` 在 `lib/stats.js` 内 **0 次**（取证 #9/#10）⇒ **无新幻觉引用**。
- 看护接线：`tests/host-abi-health.mjs:717` 在**既有条目**上扩 stale/fresh 各 1 串 ⇒ 判据覆盖面对该文件由 1 串 → 2 串（双向）；基线绿（守卫 165 断言 PASSED）⇒ **零误红**；Developer 的双向判红实证脚本（`.test-home/fix041-r0-red-f2.ps1`，已核存在）与我的静态判别（writer 在 `tests/**`、锚在 `lib/stats.js`、`git status --porcelain` 该文件零项）**结论一致**；我**未复跑**其脚本（见 §六.2）。

### 3. F-2③ 的 5 处新发现 —— **5 处全部属实（我已逐处实读）；另发现 2 处清单未列；「另批」处置合理（且理由比原裁定更强）**

- **逐处复核（我实读锚行 + 目标文件实际位置，≥3 处要求已超额完成 5 处）**：见取证 #12（`health.js:4` → `presetDiag` 实在 `:137`，`:116-124` 非环形）/ #13（`inject-manifest.js:23` 引 `client.js:5060`，实测 `:5459`，**批前即漂**）/ #14（`rpc-shadow-guard.mjs:6`→`StatsStore` 不在 `:673`；`:7`→`:3172` 对象不符；`client-render.mjs:533`→`agentPresetsServiceOf` 命中 `95/120/172/528` 不含 `:100-108`）⇒ **Developer 的 5 处漂移事实全部成立**。
- **我的 R0 判断更正**：如实标注于 §二末（「唯一」过度概括）+ 取证 #15（清单未列的 2 处：`lib/host-abi/inject-manifest.js:16`、`tests/client-render.mjs:1982/:1987`；另有 `:515/:518/:1855` 属宿主锚/已失效事实）。
- **「另批」是否合理**：**合理，且本批现实理由比 Coordinator 原裁定更强**——
  ① **归属判定是硬前置**：该族混装**宿主锚**（`dsh-* lib/client.js:NNN`）与**在仓锚**，而字符串匹配无法分类（Developer 的 90 处候选含宿主锚误收，我实测样本印证：`client-render.mjs:515/518/1982/1987` 全是宿主目标）⇒ **机器化前必须逐处语义判定**，否则会产生错误清扫（把宿主锚改成本仓符号名 = 制造幻觉引用）。
  ② **盘点面仍在增长**：Developer 报 5 处，我实读得 **≥7 处**（含 2 处其未列）⇒ **清单尚未收敛**，此时并入本批会把「未收敛的清单」变成新的返工风险（本批已因清单口径吃过一次 P2）。
  ③ **零阻塞性**：5~7 处均为**批前存量漂移**，不影响产品行为；且不破坏本批声称（W2 的「余留 41 处**全为宿主锚**」在 `lib/client.js` 范围内仍成立——41 处的目标解析全部为宿主文件/包名）。
  ④ 但须**明确另批为「必需」而非「可选」**（见 §四 P3-1）。

### 4. F-3 / F-4 / F-5 处置正当性 —— **三项均正当且与事实一致**

- **F-3（可复算）**：我按注释口径**独立复得 `[13,17,11] = 41`**（逐字吻合），并被允许复得 38（口径差异已如实写进注释）⇒ **数字现在可复算**，且「不得作长期基线」的告诫到位。**残余**：注释只复算「交付后」，而 **47（交付前 = `524a30d^`）我未复算**（需 checkout 历史版本，属写操作约束外的只读 `git show` 可行但未做）——记为未验证项（§六.3），**不影响 F-3 的闭合**（关键点「口径可复算」成立）。
- **F-4（误读消除）**：新增块**同时**声明了三件事——含 `count=0`、与 9h-2 冗余双报（有意偏严）、**非并存双实现**且 9h-2 不得简化 ⇒ 我 R0 提的「未来有人据 9h-2d 简化 9h-2 会静默丢掉语义边界」的诱因**已被注释显式阻断**；谓词本体零改动（判别力不降）。
- **F-5（限定语）**：现表述「零残留只在 shipped 面（`lib/**` + `tests/**`）成立」+ 点名 3 处未清面 ⇒ 与我 R0 的实测穷举**完全一致**（`docs/release/*` 2 处历史文档 + `CHANGELOG.md:25` 冻结）；「历史快照、刻意不清扫」与我的建议（只补限定语、不清扫）一致。

### 5. 裁定正当性 —— **两项裁定均判正当**

**(a) 检查点 3 = 165 而非 166：正当 ✅**
- 事实链：`ANCHOR_CASES` 条目数 **16 → 16**（取证 #8）；每个条目恒产 1 条 `check`（`:770` 的 `check` 在 `for…of` 内，与条目一一对应）⇒ **check 数守恒**；F-2 的机制是**在既有条目内扩 stale/fresh 数组**（`:717`）⇒ 覆盖面扩大（该文件判据 1 串 → 2 串、双向）而断言数不变。**「165」是断言数的如实值，不是覆盖面的度量**。
- 我**不同意必达 166**，且认为「必须 166」的做法**有实质缺陷**：唯一使数字变 166 的做法是把同一文件拆成两个条目（如 `{file:['lib','stats.js'], stale:['host-route.js:55']…}` + 另一条只放 `lib/oauth-llm.js:43`）⇒ 同一被守卫文件出现两条**语义同构**的锚用例，违反项目原则 P5（同一动作汇入同一实现路径），并使「一条目 = 一文件」的不变量静默失效（未来扩面时容易同一文件重复登记）。**故 Coordinator 裁定正确，Developer 的落法正确。**
- **方法学提醒（不构成 finding，仅记录）**：以「断言数」衡量批次覆盖面本身不稳健（本批即出现「覆盖面 +1 串 × 2 向而断言数 +0」）⇒ 建议 R2/R3 批次的覆盖面证据以**条目内的串计数**或**判据命中形态**表达，而非断言数。

**(b) F-2③ 另批处理（选项 a）：正当 ✅**
- 理由见 §三.3（归属判定前置 + 清单未收敛 + 零阻塞 + 不破坏本批声称）。
- **附加约束（请 Coordinator 落进另批任务书）**：另批 MUST 先做**归属判定**（in-repo vs 宿主包），且 MUST 采用**逐处语义判定**（读锚行 + 读目标文件 + 确认对象身份），**禁止**以上下文无关 grep 直接批量改写——否则会把宿主锚错改成在仓符号名，产生**新的幻觉引用**（正是 FIX-040 R0 的 P1-1 失效模式）。Developer 的「90 处候选含宿主锚误收」方法学警告**与我实测一致**（取证 #15）。

### 6. 无新引入（P0~P2）+ 越权面 + 断言集合 + 门控 + 真实环境披露 —— **全部通过**

- **无新引入 P0~P2**：三笔均纯注释；`FIX-041 R2` 零命中；无新幻觉引用；判据与代码行零改动（取证 #17）；`TODO` 零命中。
- **越权面**：零（2 文件；`.governance/**`/`AGENTS.md`/`CHANGELOG.md`/`package.json` 全零触碰；`lib/**`+`tests/**` porcelain 零项）。
- **断言集合**：**REMOVED = 0 / ADDED = 0**（149 → 149，排序集合逐元素相等）；`ANCHOR_CASES` 16 不变；守卫 165 断言（与 R0 相同）⇒ **零回退、零表面增长**。
- **门控**：`run-all` exit 0（22.0s，`#SKIP 2`），两条 skip 来源已定位且为平台条件式（`smoke.mjs:1266` win32 / `install-entry.mjs:350` 无 sh/curl）。
- **真实环境操作披露**：**属实且充分**。Developer 声明「本轮未触 `$DSH_HOME`」与我的核验一致（本轮文件集仅 `lib/stats.js` + `tests/host-abi-health.mjs`；无宿主树操作痕迹）；**本轮工作树写入**（`.test-home/fix041-r0-red-f2.ps1` 的变异 + `git checkout` 复原）**已在提交信息中如实披露为可达性实证**，且我以 `git status --porcelain` 复核**零残余** ⇒ **无真实环境防护违规**。

### 7. 关键判定：本批（10 笔）是否具备 push 条件 —— **具备；`unresolved_blockers = 0`**

- **判定：可 push** ✅。理由：R0 的两项阻塞项（F-1 追溯不实、F-2 同族在仓漂移锚）**已真闭合且经我独立复现**；3 条 P3 全部按建议处置且与事实一致；**零新引入 P0~P2**；断言集合零增删、`ANCHOR_CASES` 16 不变、守卫 **165 断言 PASSED**、门控 **exit 0**、镜像 SHA256 恒等、越权面零、三笔纯注释；两项 Coordinator 裁定经核验**正当**。⇒ **通过终态，复审链结束（无需 R2；本轮 round = 1，未触及 T2 熔断）**。
- **push 时必须遵守的口径（不得越界声称）**：
  1. **不得声称「CI 已验证」**：ubuntu 侧**未实测**；`#SKIP 6 (host-contract.mjs×2, smoke.mjs×4)` 仍为**预期值**（FIX-040 链推导 + 本机 Windows `#SKIP 2` 实测复核）。本批三笔全为注释（平台中立度最高），ubuntu 风险极低，但**仍以首跑日志为准**。
  2. **push 后回填**：ubuntu 首跑结论（`#SKIP 6` 逐行 + `9h`/`9h-2`/`9h-2b`/`9h-2c`/`9h-2d`/`9h-3` 全绿）入 evidence-log。
  3. **台账项随发布/后续批记录**（不阻塞 push）：§四 P3-1（**必需性**的另批锚族清扫，含我补的 2 处未列项与「逐处语义判定」硬前置）、P3-2（同文件自指行号整体失效：`:718-720`/`:747-752`/`:795-800` 声明值 vs 实位 `:734-736`/`:763-768`/`:811-816`）。

---

## 四、新发现（本轮）

### P0 / P1 / P2 —— 0 条
无。

### P3（批外存量登记；不阻塞）—— 2 条

**P3-1（R1）｜锚族漂移面比 Developer 声明更宽（我实测 ≥7 处，其报 5 处），且 F-2③ 另批 MUST 以「逐处语义判定」为硬前置**
- 位置（我复核**属实**的 5 处）：`lib/host-abi/health.js:4`、`lib/host-abi/inject-manifest.js:23`、`tests/rpc-shadow-guard.mjs:6`、`tests/rpc-shadow-guard.mjs:7`、`tests/client-render.mjs:533`（+ `:1872`）；**清单未列（我补）**：`lib/host-abi/inject-manifest.js:16`（引宿主 `dsh-client-modules lib/client.js:265-268`；`lib/client.js:265` 现为 `inputTokens: wv.number()` ⇒ 宿主侧对象亦不可解析）、`tests/client-render.mjs:1982`（宿主 `dsh-client-connection lib/client.js:4754-4825` —— 我 R0 已实测宿主侧漂至 `function isLoopbackHostname @6273`）、`tests/client-render.mjs:1987`（宿主 `dsh-client-ui-settings-models lib/client.js:2842-2848` —— R0 实测 MISS）
- 事实依据：取证 #12/#13/#14（逐处实读锚行 + 目标实际位置）+ #15（按同口径复扫 5 文件）
- 影响：另批任务书若按「5 处」定范围会**漏项**；且若以 grep 直接批量改写，会把宿主锚错改为在仓符号名 ⇒ **制造新幻觉引用**（FIX-040 R0 P1-1 同型失效）
- 建议：另批任务书明确 ①范围以「逐处语义判定后的清单」为准（非 grep 候选数、非固定条数）②先分类 in-repo / 宿主包，再分别处置（in-repo → 符号名式 + `ANCHOR_CASES` 补对；宿主包 → 符号名式 + 宿主核验基线登记 + 人工复检义务，**不得**声称机器覆盖）③存量漂移锚作为**另批必需项**（非「可选台账」），因其失效机理与本批已修项完全同类。

**P3-2（R1）｜`tests/host-abi-health.mjs` 内自指行号注释因本批增行而整体失效（3 处声明值 vs 实位偏差 ~16~19 行）**
- 位置：`:734` 声明「现行 `:718-720` = …」→ 该文字实在 **`:734-736`**；`:763-768` 声明「9h-2 注释 `:747-752`」→ 实在 **`:763-768`**；`:811-816` 声明「9h-3 注释 `:795-800`」→ 实在 **`:811-816`**
- 事实依据：取证 #16（逐块读行）
- 影响：**零功能影响**（纯注释自指），但**同一失效机理**——该文件每增一行注释，其内部行号引用即失效，而**本批零新增行也已使 3 处偏移** ⇒ 证明「行号式引用在**被守卫文件自身**内同样不可靠」，与另批任务的清扫对象同类；现无守卫覆盖「注释自指行号」（`ANCHOR_CASES` 的 stale 串只覆盖旧式锚清单）
- 建议（可选，~3 行注释）：把这 3 处自指改为**节名/符号名式**（如「9h-2 块注释」「9h-3 阈值块注释」），或纳入 `lib/**`+`tests/**` 锚族另批一并处理（**我倾向后者**，避免本批再次扩面）。

---

## 五、硬门槛裁决

| 门槛项 | 阈值 | 实测 | 裁决 |
|--------|------|------|------|
| P0 阻塞问题数 | = 0 | 0（§四 P0 无） | **PASS** |
| 5 维度全覆盖 | = 100% | 见下逐维度结论（5/5） | **PASS** |
| 每条发现标注级别 | = 100% | R0 findings 6 条逐条带处置 + 证据；新发现 P3×2 均带 文件:行号 + 事实依据 + 影响 + 建议 | **PASS** |
| 设计一致性检查 | 已完成 | 与项目原则 1（事实性——**F-1 已恢复**，引用可解析）、原则 4（防护网——F-2 改写**并**接线，看护面不缩）、原则 8（可观测——F-4 明示冗余双报语义）、P5（单一路径——**裁定 (a) 强化的正是 P5**：拒绝同文件拆双条目）、`ci.yml` 契约（未触）逐项比对：**无偏离** | **PASS** |
| AI 代码专项 5 项 | 全部完成 | ① mock 残留：无（零夹具新增）② 硬编码：无（无硬编码通过；`stale`/`fresh` 为判据输入）③ **幻觉引用：无**（F-1 修正后引用全部可解析；F-2 新锚对象实存——取证 #3/#9）④ 未实现 TODO：无（三笔 `+` 行零命中）⑤ 过度实现：无（3 笔 = 6 处字符串 + 1 组 stale/fresh 扩对 + 3 处披露注释；无顺带重构） | **PASS** |

### 五维度逐项结论

- **正确性 —— 通过**：F-1 三处标注改为可解析真实来源（`:125/:130` 逐字对应 + 机录在档）；F-2 改写后 `lib/stats.js` 行号锚 0 次、符号名 1 次、基线绿；F-3 我**独立复得注释口径的 41（13/17/11）**；F-4/F-5 与实现/事实一致；守卫 165 断言 + 门控 exit 0；断言标签集与 R0 **逐元素相等**（零回退）。
- **安全性 —— 通过**：三笔零产品逻辑改动（`lib/stats.js` 唯一改动为注释行；测试文件改动均为注释/清单）；无新外部输入面、无密钥/注入引入；本审查全部判别为只读 + 内存仿真。
- **可维护性 —— 通过（附 P3-1/P3-2）**：F-3 把「不可复算的数字」改成「连口径引用的数字 + 明确告诫」，是可维护性正向改进；F-4 的披露块含「不得简化 9h-2」的**未来变更防护**；F-5 限定语与事实一致。负向 = 锚族清单完整性与文件内自指行号（均登记另批）。
- **性能 —— 通过**：三笔零运行时判据变更（`:717` 仅在既有条目内 +1 串 × 2 向，守卫仍 165 断言、门控 22.0s 与 R0 同量级 21.5s）。
- **测试覆盖 —— 通过**：断言集合**零增删**（REMOVED=0/ADDED=0）而**覆盖面净增**（`lib/stats.js` 锚判据 1 串 → 2 串、双向）——覆盖面无回退；剩余覆盖缺口（文件内自指行号、in-repo 锚族 ~7 处漂移、宿主锚族无机器看护）**已全部显式登记**，未被声称已覆盖。

### 审查结论

> **APPROVED_WITH_NOTES**
> `unresolved_blockers=0`
>
> 理由：R0 的两项阻塞项**均已真闭合且经我独立复现**——①**F-1**：`FIX-041 R2` 全仓零命中，三处改 `FIX-040 R2 P3-1(new)/P3-2(new)` 与 `review-FIX-040-R2-input.md:125/:130` **逐字对应**、机录 `REVIEW-FIX-040-R1/R2` 在档（`evidence-log.md:635/:637`），同类裸轮次引用补齐且**我抽查 9 处的轮次归属全部为真**，`:708` 因同句含 `FIX-040 扩面` **豁免成立**；②**F-2**：`lib/stats.js:104` 去行号改符号名式（行号串 0 次 / `OAUTH_PROVIDER` 1 次）、既有 `ANCHOR_CASES` 条目扩 stale+fresh 各 1 串（`:717`）、**零新幻觉引用**。**两项 Coordinator 裁定经我核验均正当**：165 而非 166 = 扩既有条目 ⇒ 断言数守恒（`ANCHOR_CASES` 16 不变、标签集 149→149 逐元素相等），而「必须 166」的唯一路径是同文件拆双条目、**违反 P5 且破坏『一条目=一文件』不变量**；F-2③ 另批则因该族**归属判定前置（宿主锚误收）+ 清单仍在增长（我实测 ≥7 处 vs 报 5 处）+ 零阻塞性**而正当，**且我如实更正 R0 的「唯一在仓漂移锚」过度概括**（我 R0 未做全仓 in-repo 锚族扫描）。F-3/F-4/F-5 处置**均正当且与事实一致**（F-3 我按注释口径**独立复得 41 = 13/17/11**）。三笔**纯注释**、越权面零、断言集合**零增删**、守卫 **165 断言 PASSED**、门控 **exit 0**、镜像 SHA256 恒等、**零新引入 P0~P2**。余下 **P3×2**（锚族另批清单需补我发现的 2 处 + 逐处语义判定硬前置；`tests/host-abi-health.mjs` 3 处自指行号因增行失效）**均为批外存量、非阻塞**。**本结论为通过终态，复审链结束（无需 R2）；`round = 1` 未触及 T2 熔断。**

---

## 六、未验证项声明（事实依据红线）

1. **ubuntu / CI 未实测**：`#SKIP 6 (host-contract.mjs×2, smoke.mjs×4)` 仍为**预期值**；本机 win32 实测 `#SKIP 2`（`smoke.mjs:1266` 平台条件式 + `install-entry.mjs:350` 无 sh/curl）。本批三笔为纯注释，平台中立度高，但仍**不得读作 CI 已验证**。
2. **Developer 的取证脚本未复跑**：`.test-home/fix041-r0-red-f2.ps1`（F-2 双向判红）与 `fix041-r0-count-reconcile.mjs`（F-3 计数）我**仅核实其存在、未执行**；替代性验证 = ①我对 `ANCHOR_CASES:717` 串的静态双向判别（目标文件 `/lib/stats.js` 内容对 `stale`/`fresh` 的实算：`0` / `true`）②我按注释口径**独立复算 F-3 的 41**（取证 #11）③`git status --porcelain` 确认 `lib/**`/`tests/**` 零残余。
3. **`47`（交付前 = `524a30d^`）我未复算**：需读历史版本文件（只读可行但未执行）⇒ **该数字本轮仍未由我独立验证**；已闭合的关键点是「口径可复算 + 交付后 41 与注释一致 + 38 口径差异如实记录」（取证 #11），**不含**对 47 的复核。
4. **F-2③ 的完整清单仍未收敛**：我复核了 Developer 所列 5 处（全部属实）并补 2 处未列项；我的扫描口径为「注释行中形如 `lib/**`·`tests/**`·裸 `lib/client.js:NNN` 的锚」，**未**覆盖非注释行、`L####` 形态、以及需要语义裁定才能定性的 5 处（Developer 所列 `tests/stats.mjs:8` 等）⇒ **另批清单应以逐处语义判定为准，不得以本报告或 Developer 的条数为最终范围**。
5. **本审查写入面**：仅 `.governance/review-FIX-041-R1-input.md`（本报告）+ `.test-home/fix041-r1-verify.mjs`（gitignored 只读核验脚本）。**未修改任何被跟踪文件**；`git status --porcelain` 复核 = `lib/**`/`tests/**` 零项。

---

## 七、建议 Coordinator 动作（按依赖排序）

1. **判定通过（本轮）**：结论 = **APPROVED_WITH_NOTES / `unresolved_blockers=0`** ⇒ 以 review-record CLI 机录本报告（canonical 名 `review-FIX-041-R1.md`）；**复审链结束，无需再 spawn Reviewer**。
2. **push（可执行）**：本批 **10 笔**具备 push 条件（`origin/main` = `5cc884c`，领先 10 笔）；push 后**立即以首跑日志核验** ubuntu 侧 `#SKIP 6` 逐行 + `9h / 9h-2 / 9h-2b / 9h-2c / 9h-2d / 9h-3` 全绿并入 evidence-log；**首跑前任何表述不得声称「CI 已验证」**。
3. **另批任务（必需，非可选）**：锚族 in-repo 漂移清扫 —— 任务书 MUST 含 ①**逐处语义判定**硬前置（禁止 grep 批量改写；宿主锚误收是实测风险）②范围以判定后清单为准（我补 2 处：`lib/host-abi/inject-manifest.js:16`、`tests/client-render.mjs:1982/:1987`）③in-repo → 符号名式 + `ANCHOR_CASES` 补对；宿主包 → 符号名式 + **宿主核验基线登记 + 人工复检义务**（W4 已立项，同法）④可一并处理 `tests/host-abi-health.mjs` 的 3 处自指行号（P3-2）。
4. **台账补录（不阻塞 push）**：①P3-1（清单完整性 + 硬前置）②P3-2（自指行号 3 处）；另 FIX-040 链遗留台账项（P2-2 归因 / P2-3 中间态口径 / `.test-home/fix040-*` 取证件归档 / `CHANGELOG:59` / `.cmd` ComSpec / README 引用位置枚举）**仍未执行**，建议本批收尾一并机录。
5. **发布面提示**：本批 10 笔全为测试基础设施 + 注释面 ⇒ **不产生用户可见行为变化**；`package.json` / 版本位（0.5.0）/ `CHANGELOG.md` 未被触碰。
6. **方法学建议（供后续批次采纳）**：覆盖面证据**不宜以断言数表达**（本批实证「覆盖面 +2 向而断言数 +0」）；建议以「条目内串计数 + 判据命中形态」表达，避免后续为凑数字而拆条目（破坏 P5）。
