# 更新日志（CHANGELOG）

> dsh-agent-router 的版本变更记录。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。
> 内部注解（提交号 / EV 编号）供追溯，用户可忽略。

## v0.4.3 — 2026-09-04

### 摘要

**标准插件管理版**。核心变化一句话：**`dsh plugin --profile web add / update / remove` 官方管理命令真实可用（此前该通道装而不激活）；README 安装文档双通道化，每条命令经隔离环境实测**。

### 新增

- **🔌 dsh plugin 标准管理支持（EVO-015）**：package.json 声明 `dsh.bundle` + 包内 bundle patch 层（`cordis.patch.yml`，与安装脚本写入宿主 profile 的宿主平面行逐字一致——两通道注册同一对服务 `router` / `tool-router`）。README 安装章节双通道化：方式一（推荐）`dsh plugin --profile web add`——npm 安装 dsh / npx 拉起 × 在线 git spec / 离线 `file:` 解压目录（说明 `link:` 只创建符号链接、不安装依赖故不采用），并附 `update` / `remove` 命令矩阵与「从旧方式（junction/脚本）迁移」三步；方式二安装脚本原样保留——已用脚本通道的用户零影响、可继续使用（内部注解：commit `7965c9d`；REVIEW-EVO-015-R0 APPROVED_WITH_NOTES / unresolved_blockers=0；EV-143~144）。

### 修复

- **README 离线安装链接勘误（EVO-015 顺带取证）**：v0.4.2 及此前的发行资产名**无 v 前缀**（`dsh-agent-router-X.Y.Z.tar.gz`）——旧文档链接（带 v 前缀）实为 404 坏链（GitHub API 列举 + 实下载核验）；发行包为 npm pack 形态（解压根目录固定 `package/`），旧文档的 `cd dsh-agent-router-vX.Y.Z` 目录名不存在——离线安装步骤已按真实结构改写。

### 变更

- **files 列表新增 `cordis.patch.yml`**（打包完整性——bundle patch 层随发行包分发）。
- **install-entry 新增 11 条判别断言**（dsh.bundle 声明 / patch 层逐字一致 / README 双通道命令守卫；`tests/smoke.mjs` 1087 → 1098，零回退）。

### 破坏性变更

**无。**论证（四证）：① **配置面零新增节**——`dsh.bundle` 为包清单声明（宿主 `dsh plugin` 通道读取），不新增任何用户配置项；② **依赖面零变更**——deps/peerDeps 与 v0.4.2 逐项一致，仅 files 列表 +1；③ **数据面零变更**——不触碰任何存储格式与用户数据；④ **行为面零强制**——双通道并存，既有脚本通道安装（junction + 手写 patch 行）运行时零影响（宿主 loadProfile 仅映射 profile 的 `dsh.profile.bundles` 列表，脚本通道该列表不含本包、不读包内 `dsh.bundle` 字段）。

### 版本说明

- **版本号**：0.4.2 → **v0.4.3**（版本位按用户裁决 v0.4.3 立即发布；新增面为标准管理声明与文档双通道化，零 breaking 见「破坏性变更」四证）。
- **发布范围**：v0.4.2（tag e91f77d，2026-09-02）以来 main 全部提交 **3 个**（`git rev-list --count v0.4.2..HEAD` E-1 时点实采 = 2，加本提交）。三分账：**产品提交 1 个**——EVO-015 `7965c9d`（dsh.bundle 声明 + 包内 patch 层 + README 双通道文档 + install-entry 判别）；**治理提交 1 个**——`c2be6dc`（REL-008 tracker 终态，归属 v0.4.2 发布链尾账，不入用户面）+ **REL-009（本提交——version bump + 本节收口 + README 版本校对，hash 见 git log）**。**结论行：1 + 1 + 本提交 = 3，与实采一致（2 + 本提交）；产品提交在本节语义全覆盖，治理提交不入用户面。**
- **验证基线**：E-1 时点 `node tests/smoke.mjs` **1098 ok / 0 FAIL / exit 0**（含 install-entry 判别 11 断言）。内部注解：发布执行段 E-2 十八面门控复跑（tests/ 下全部 .mjs 门控套件，metrics.mjs 观测脚本除外）与 E-3 tarball 隔离环境安装冒烟（环境变量重定向至临时目录）随发布链执行，最终以复跑实测值为准。

## v0.4.2 — 2026-09-02

### 摘要

**预设 Agent 默认模型全链承载版**。核心变化一句话：**按 DSH 预设粒度配置主 Agent 与 subagent 的默认模型——设置页新增「预设 Agent」卡片，新会话打开即显示、切换预设实时跟随；配套修复链收口（预设列表域名 / 播种静默失效 / 显示刷新单路径收敛 / 遥测可观测）**。

### 新增

- **🎯 Agent 预设默认模型（EVO-013 + EVO-014）**：设置 → Agent 路由新增「预设 Agent」折叠卡片（默认折叠、空列表起步；统一添加模板下拉直接选择 DSH 预设）——按 DSH 预设粒度配置主 Agent 默认模型与 subagent 默认模型；新会话/空白会话切换预设时模型选择器**即时跟随**（打开即显示，无需先发消息；空白判据与宿主同构——未开启过对话轮即可切）；会话内手动选择始终优先（用户主权）；subagent 未配置时继承主预设模型；未配置预设零行为变化（完全遵循 DSH 现行规则）。播种借用宿主会话模型选择通路，其附带的全局默认写入**立即自动写回恢复**（瞬态毫秒级，通常不可感知；恢复失败自动重试一次并高声告警）；多个播种事件并发到达时按内部**串行化队列**依次执行，不会污染全局默认（内部注解：EVO-013 配置面 + EVO-014 事件驱动重构——模型只跟随两个预设事件，彻底移除会话过程介入）。

### 修复

- **预设列表加载失败（FIX-022）**：roster RPC 域名误用单数 `api.agentPreset.list`（宿主命名陷阱：wire 方法名单数、JS 组名复数，真实域名为 `api.agentPresets`）→ TypeError 致「预设 Agent」下拉不可用。修复：域名对齐 + 三层防御（复数优先 / 单数回落 / 缺域明确诊断）。
- **切换播种静默失效（FIX-023）**：`ctx.agents` 属性面访问恒 undefined（插件 inject 未含 agents 服务）→ 切换预设后的播种静默失效。修复：`ctx.get('agents')` 调用时解析 + 失败 warn 可观测。
- **显示刷新单路径收敛（FIX-024 → FIX-026）**：切换预设后模型选择器显示不跟随（目标 = 全局默认时设置写无差异、刷新事件不触发）。最终态：客户端 `agent-preset/selected` 事件直驱模型目录重载（与「打开即加载」同源同对象）——中间态的服务端 emit 路径经真机取证不可达，已删除（多触发来源单路径汇入，旧路径不并存）。
- **modelDirectories 规范注入 + 结构化遥测（FIX-027）**：客户端对 modelDirectories 的裸解析在真机解析失败致显示仍不跟随。修复：宿主 inject 规范声明 + 双形态解析防御 + 结构化遥测——排障时浏览器 F12 控制台可见 `dsh-agent-router[FIX-027]` 前缀的播种链路日志。

### 变更

- **文档**：README 三大主要功能置顶刷新（①Agent 预设与 subagent 默认模型配置 ②专业 Agent 配置 ③ChatGPT 订阅登录 + 主模型调用）——简介/项目目标/特性列表对齐现能力面。
- **治理（无用户面）**：项目质量原则升级至 P-v3（新增用户报障受理与验证纪律五条 + 单路径收敛强化）——纯治理资产，用户可见行为零变化。

### 破坏性变更

**无。**论证（四证）：① **配置面零破坏**——`router.presets` 为新增配置节（缺省空字典），旧配置零迁移零阻塞；未配置预设时零行为变化；② **依赖面零变更**——除版本号与 files 列表新增 `lib/preset-defaults.js`（打包完整性，只增不减）外，package.json 依赖面（deps/peerDeps）与 v0.4.1 逐项一致，pnpm-lock 零变更；③ **数据面零变更**——无既有 Schema/存储格式变更（预设配置为新命名空间，不触碰 oauthAccounts / 统计 JSONL / 凭据文件）；④ **行为面零强制**——默认层选择权在三层主权保护之下（picked/header 优先，仅空白会话、仅预设事件播种、无模型变更监听）；已运行会话与显式指定模型的子代理不受影响。

### 已知问题

- **本项目暂无持续集成（CI）自动化**（v0.3.0 起披露延续）：回归保护依赖发布前本地全量测试网（**十八面套件清单全绿复跑**——tests/ 下全部 .mjs 门控套件，metrics.mjs 为观测脚本非门控面；见「版本说明·验证基线」）。CI 建设已列入后续排期。
- **某形态会话的 GUI 预设下拉不发切换请求（宿主域挂账）**：header 无预设信息的会话上，GUI 预设下拉切换不产生切换请求（F12 Network 零请求行实证）——DSH 宿主层问题，插件链路已验证无责（同会话直发 RPC 正常）。规避：该形态会话改用「新建会话选预设」，或任意一次直接切换动作后即恢复跟随。
- **带切换历史的空白会话重启后状态可能非最后预设配置**：重启恢复的空白老会话（此前切换过预设）显示状态可能不是最后预设的配置（resume 链 header 无预设 + 事件重放交互缝隙）——**切一次预设即恢复**；随后续批次或宿主申报处理。
- **P3 台账批（随版披露，不阻发布）**：EVO-013 / FIX-022 / EVO-014 / FIX-023 / FIX-025 / FIX-027 各任务审查台账中的 P3 级改进项已分级登记（parent 查询失败保护降级披露 / 测试缺口三处 / liveDefaultSelection 双份实现去重 / fixture 字段同构 / 覆盖观察 / 回落可选加固等），随后续批次消化；另 v0.4.1 候选批（路由体验优化 ×2 + AUDIT-001 台账 P1×3/P2×13/P3×23）记录待排期。

### 版本说明

- **版本号**：0.4.1 → **v0.4.2**（MINOR：完整新能力面——新配置节 + 新设置卡片 + 新事件行为，向后兼容；零 breaking 见「破坏性变更」四证；先例 v0.4.0：功能面 = MINOR）。
- **发布范围**：v0.4.1（tag 40816dc，2026-09-01）以来 main 全部提交 **31 个**（`git rev-list --count v0.4.1..HEAD` 实采 2026-09-02 = 30，加本提交）。三分账：**产品提交 16 个（按任务族，SHA 底账全量）**——EVO-013 ×4（`2ab27d6` 机制 / `97b04ac` UI 卡片 / `08e0461` README 使用指南 / `75434e7` Rework）+ EVO-014 ×3（`de72e0c` 事件驱动重构 / `fe0a94e` Rework / `4e1d9cc` MicroFix）+ FIX-022 ×1（`740f95a` 域名对齐 + 三层防御）+ FIX-023 ×1（`afa6ddc`——混合 commit：产品代码与治理记录同车，按 diff 实采归属产品面）+ FIX-024 ×1（`ba36836`——显示刷新中间态，已被 FIX-026 单路径收敛取代并删除，以最终态发布）+ FIX-025 ×1（`a80e935` 空白判据与宿主同构）+ FIX-026 ×1（`98299e5` 客户端直驱 + 旧路径删除）+ FIX-027 ×2（`8c4cfc5` 规范注入 + 遥测 / `6d5e357` 镜像同步）+ DOC-001 ×1（`2e1cdf8` README 功能面刷新）+ **REL-008（本提交——version bump + 本节收口 + README 同步，hash 见 git log）**；**治理提交 15 个（不承载用户可见变更）**——`39506cd` / `de101c0`（REL-007 E-6/tracker 终态，归属 v0.4.1 发布链尾账，随本版范围携带）+ 治理入仓/收尾机录 ×12（`803f9fc` / `7dc78fe` / `9c3ee78` / `4608413` / `06ada14` / `5988286` / `017a3fc` / `7f68c96` / `3578d28` / `d29bc4a` / `07c8d94`（GOV-005 P-v3 原则）/ `6a5f029`（GOV-006 终态归档））+ `4138f53`（REL-008 规划段终态：version-plan R1 入仓 + 双机录 + tracker 收口）。**结论行：16 + 15 = 31，与实采一致（30 + 本提交）；产品提交经逐 commit 对照在本节语义全覆盖，治理提交不入用户面。**
- **验证基线**：E-1 时点 `node tests/smoke.mjs` **1087 ok / 0 FAIL / 1 skip / exit 0**（含 client-render 渲染级看护 207 断言基线与 README 一致性守卫）；**用户真机复验在案**（预设卡片配置 / 新会话打开即显示 / 老会话切换跟随 / subagent 继承——EVO-013/014、FIX-022~027 用户复验链）。内部注解：发布执行段 E-2 全量门控复跑（**十八面套件清单**——tests/ 下全部 .mjs 门控套件（metrics.mjs 观测脚本除外），含 preset-defaults / routing-paths / adapter-parity / install-entry 等）与 tarball 隔离环境安装冒烟（环境变量重定向至临时目录）随发布链执行，最终以复跑实测值为准。

## v0.4.1 — 2026-09-01

### 摘要

**订阅生图 + 视觉产物画布 + 主模型官方路由版**。核心变化一句话：**ChatGPT 订阅可生成图片（gpt-image 系）、生成图经插件同源画布直达显示（不再受宿主附件通道牵制）、主模型默认走宿主官方 openai-codex 路由（token 自动注入）**。

### 新增

- **ChatGPT 订阅生图（EVO-011）**：draw agent 绑定订阅账号即可出图——经 chatgpt.com codex images 专用端点（OpenAI Images API 兼容；调研 RES-004 三层证据：DSH 自家 dsh-codex-connect 实证 + openai/codex 官方源码 + 社区探针）；gpt-image-* 模型透传（默认 gpt-image-2，dall-e-3 等旧默认自动换型并提示）；尺寸/质量参数支持 + 订阅额度错误（429 usage_limit）友好提示；真实端点实测通过（200 + PNG）。**勘误**：v0.3.0 时代「订阅 OAuth 无 Images API」的判断对 api.openai.com 平台成立，但漏了 chatgpt.com codex 后端端点（用户质疑驱动修正）。
- **视觉产物画布架构（EVO-012，用户提案）**：生成图经插件同源 HTTP 路由（`/router-assets/` 内容寻址）直达显示——彻底绕开受宿主网关断言演进反复牵制的 imageData RPC 通道（FIX-008 域历史遗留根除）；route_agent 工具卡默认折叠（摘要行 + 缩略图，过程收起、结果直出）；识别场景不再复读用户发的图（输入回显消除）；旧产物自动自愈显示；会话产物集合（输入区 🖼 图标按钮 → 网格 → 放大，悬停显示计数）。
- **主模型宿主官方路由（EVO-010，用户「复用宿主」质疑驱动）**：ChatGPT 订阅默认经宿主官方 openai-codex 路由——插件自动维护 provider 条目 + OAuth token 热注入刷新 + P9 parity 守卫自动回滚 + 降级开关「插件内置」通路保留；桥接可行性经 ARCH-003 调研（三层证据）+ 真机 PoC 实证（文本/图片全通、模型直接识图零中转）。

### 修复

- **生成图显示三连败真根因（AUDIT-001 P0-A）**：React 18.3.1 的 `ref` 是保留 prop（`createElement` 剥离）——组件以 `ref` 传图片数据导致恒 `undefined`、直达路径从未生效（必现非偶发）。修复：prop 更名 `imageRef` + 惰性 getter 单次访问快照。**历史测试全绿的原因**：测试 shim 原样透传 props，与宿主 React 语义存在 parity 缺口——已建立宿主语义同构判别 harness 看护（同类缺口不再漏检）。
- **凭据竞态（AUDIT-001 P0-B）**：登出/登录写绕过凭据文件锁、与在途 token 刷新交错——登出后凭据「复活」、新登录被旧 token 链覆写。修复：锁收敛 + 盘上 CAS 双保险。
- **并发审计 41 条台账（AUDIT-001，用户指令专项审计）**：全库并发正确性专项审计分级登记 P1×3（CLI 中止漏检/附件同名覆盖/产物归属竞态）+ P2×13 + P3×23，随 v0.4.2 批次消化（本版承载其中 P0×2，见上两条）。
- **幽灵卡三形态（FIX-019）**：统计键/宿主路由真条目混入账号管理、渲染假编辑器——判据单点化三重排除。
- **数据防护链（FIX-015）**：模型列表保存读写对称（空编辑态不再空覆盖——用户数据丢失根因修）+ 写路径防缩小守卫 + 实预填（placeholder 幻觉根治）+ 空值可观测。

### 变更

- **ChatGPT 订阅组默认不注册**：host 模式下订阅主模型由 openai-codex 官方组承载（见「新增·主模型宿主官方路由」），订阅卡保留「插件内置 / 宿主官方路由」双通路开关。
- **生成物按钮纯图标**：28px 方形按钮内的文本竖排折行修正——入口改纯图标，产物计数移入悬停提示（用户反馈驱动，2026-09-01）。

### 破坏性变更

**无。**论证（四证）：① **依赖面零变更**——除版本号与 files 列表新增 `lib/host-route.js`（打包完整性，只增不减）外，package.json 依赖面（deps/peerDeps）与 v0.3.3 逐项一致；② **配置面零破坏**——schemas 新增字段均带默认值（如订阅卡 transport 通路开关），无键删除、无默认行为翻转，旧配置零迁移零阻塞；③ **数据面零变更**——统计 JSONL / 凭据文件 / oauthAccounts 结构不变（凭据竞态修复仅收窄写窗口，不改变存储格式）；④ **行为变化均为缺陷修复与新增能力**——画布架构为显示通路新增（imageData RPC 保留为旧产物回退）、官方路由默认带降级开关（「插件内置」通路完整保留）、审计修复均为正确性收窄而非能力收窄。

### 已知问题

- **本项目暂无持续集成（CI）自动化**（v0.3.0 起披露延续）：回归保护依赖发布前本地全量测试网（十七面全绿复跑，见「版本说明·验证基线」）。CI 建设已列入后续排期。
- **对话内图片附件「加载失败」显示层遗留已根治（历史披露收口）**：v0.3.0~v0.3.3 连续披露的显示层残留（FIX-008 域）经 v0.4.1 双线根治——视觉产物画布架构（同源路由绕开 imageData RPC 通道）+ React 18 `ref` 保留 prop 真根因修复（AUDIT-001 P0-A——此前「直达路径」实际从未生效）。
- **deepseek-official 视觉端点挂起**（宿主/端点域，EV-088 披露延续）：227s/0token 挂起属上游端点现象，vision 旁路（glm 等可用服务商）可用——本仓不可修，不构成本版回滚触发条件。
- **picked 层盲区**（宿主固有 P3，延续）：selectModel 进程内选择不落 header/options 且插件无访问 API——selectModel 后首轮 prestep 与宿主判定可能短暂不一致，属宿主设计固有窗口（注释披露在案）。
- **并发审计台账 41 条待消化（本版新增披露）**：P1×3（CLI 中止漏检/附件同名覆盖/产物归属竞态）+ P2×13 + P3×23 已分级登记，随 v0.4.2 批次修复。
- **reasoning 回传缺口（本版新增披露，FIX-017 发布门必验项延续）**：多工具往返深场景 reasoning 回传验证缺口，已随主通路切换宿主官方 openai-codex 路由缓解（官方通路原生承载 reasoning）；「插件内置」降级通路的该场景仍待验证。

### 版本说明

- **版本号**：0.3.3 → **v0.4.1**（MINOR：两大新能力（订阅生图/主模型官方路由）+ 一架构迁移（视觉产物画布）+ 并发审计 P0×2 修复；零 breaking 见「破坏性变更」四证）。**无 0.4.0 中间版说明**：v0.4.0 候选内容（EVO-009 主模型直选 + EVO-010 官方路由）已于 2026-08-31 在 main 完成开发与审查，但发布链被用户裁决暂停、未及发布——0.4.0 候选与 0.4.1 批次（EVO-011/012 + FIX-020/021 + AUDIT-001）合并为一次发布，semver 允许跳版（0.3.3 → 0.4.1，MINOR 跃迁一次到位，0.4.0 号段不占用）。
- **发布范围**：v0.3.3（tag e818183，2026-08-30）以来 main 全部提交 **44 个**（`git rev-list --count e818183..HEAD` 实采 2026-09-01 = 43，加本提交）。三分账：**产品提交 30 个（按任务族，SHA 底账全量）**——EVO-009 ×2（`01729a2` 订阅账号注册主模型 provider / `bc500f7` files 列表补 oauth-llm）+ FIX-015 ×5（`e170cf2` 空模型 P8 告警 / `fef785b` 幽灵卡过滤+空提示+镜像 / `c9a8deb` 实预填+防呆 / `86fb209` 读写对称 / `af6a1f8` 防缩小盘点）+ FIX-016 ×1（`a410a52` tools 映射真形状修复）+ FIX-017 ×1（`6b2ada3` multi-turn 映射契约重构）+ FIX-018 ×3（`1d77817` 武装条件补判定 / `e70028c` 直传判定核查 / `d52b716` 镜像同步）+ EVO-010 ×4（`30dd55e` host-route 服务端 / `82ae39a` 通路开关 UI / `3ead43f` 收尾卫生 / `31ab145` R0 返工）+ FIX-019 ×3（`6e456ce` chatgpt-oauth 过滤 / `961aa13` openai-codex 三判据 / `321a5ef` 判据语义拆分）+ EVO-011 ×2（`1dd124b` 订阅生图直连 / `f3268af` UI 类型化+镜像）+ EVO-012 ×4（`6d12289` 同源路由 / `9b751a1` 折叠+直达 / `1f08e18` 输入回显消除 / `120c058` url 自愈+集合视图）+ FIX-021 ×1（`2e945f6` attachment.id 兜底）+ AUDIT-001 ×3（`b0683a2` P0×2 修复+判别入网 / `1832db0` 生成物按钮纯图标 / `2aa9316` emoji 断言修正）+ **REL-007（本提交——version bump + 本段收口 + README 同步，hash 见 git log）**；**治理提交 14 个（不承载用户可见变更）**——`4fecc06`（REL-006 E-6 尾账，归属 v0.3.3 发布链）+ `c477930`（v0.4.0 发布链暂停留痕）+ 治理收尾机录 ×12（`4b59d09` / `13cd084` / `e81ac6b` / `8120048` / `b12dc5f` / `30281d7` / `d1c5c1e` / `53cbb80` / `04231a7` / `5b24ca3` / `d8dc44a` / `2544e66`）。**结论行：30 + 14 = 44，与实采一致（43 + 本提交）；产品提交经逐 commit 对照在本节语义全覆盖，治理提交不入用户面。**口径注记：`4fecc06` 为 v0.3.3 tag 后落盘的 v0.3.3 发布链尾账（先例：v0.3.2 口径注记），不在 v0.3.3 范围计数内、计入本版治理面。
- **验证基线**：门控 **17 套件全绿 exit 0**（含 AUDIT-001 审计判别 14 断言——宿主 React 语义同构 harness（RED 复现后转绿）/ FIX-015 判别组 / oauth-main-model 40 断言 / EVO-012 判别 RED/GREEN 三轮）；`tests/smoke.mjs` **1037 ok / 0 FAIL**；served-client 镜像一致；**真实端点实测**（订阅生图 200 + PNG ≈665KB（680,946 字节；908K 为 b64 字符长度非解码后体积） / 官方路由文本+图片全通——插件同款头部+代理链+只读凭据零泄漏）；**用户真机验证**（生图直达显示 / 生成物纯图标按钮 / 官方路由多模态识图）。内部注解：发布执行段 bump 后全量门控复跑与 tarball 隔离环境安装冒烟（环境变量重定向至临时目录）随发布链执行，最终以复跑实测值为准。

## v0.3.3 — 2026-08-30

### 摘要

**用户报障三连热修 + 默认模型更新版**。核心变化一句话：**统计面板恢复显示（v0.3.0 起即断的读侧 RPC）、文本模型贴图自动切多模态、ChatGPT 绑定测试按钮不再 400、预设默认模型更新为 gpt-5.6 系**。

### 修复

- **统计面板恒 0（P0，FIX-011）**：v0.3.0 统计迁移引入实例字段遮蔽同名 RPC 方法（`stats` 字段遮蔽 `stats` 方法）→ 宿主网关 method-unavailable → 设置页统计轮询静默失败（**记录/持久化一直正常，磁盘数据完好**）。修复：stats RPC 描述符 implementation 显式指向 `statsSnapshot` + 17 描述符全量守卫测试（tests/rpc-shadow-guard.mjs 21 断言——未来任何 RPC 方法被字段遮蔽必败）。
- **文本模型发图被拦（P0，FIX-012）**：宿主准入按当前选中模型的能力拒绝带图消息（「当前模型不支持图片，请切换支持图片的模型」Toast）；插件自动接管自 FIX-002 起默认关闭 → 文本主模型贴图即被拦。修复：**图片条件化武装——贴图即自动切「\<provider\> + 多模态」包装路由、发送后保持**（文本轮经包装路由零开销委托原生模型；带图轮放行准入并改写路由提示；判别测试 tests/fix-012-image-takeover.mjs 22 断言）；无图/纯文本轮永不自动切换、用户手动选择的模型始终尊重（FIX-002 用户主权语义完整）。行为说明：**移除未发送的图片不会自动切回原模型**（宿主无安全区分「发送」与「移除」的机制——selectModel 对会话图片零校验，还原会成功切回纯文本并让下一张图被拦），切回请在模型列表手动选择。
- **ChatGPT 绑定测试按钮 HTTP 400（FIX-013）**：测试路径强制 maxTokens → codex-responses 分支发送 `max_output_tokens` → ChatGPT 后端拒绝该参数（实际调用因不带该参数一直正常）。修复：codex-responses 协议不再发送该参数（用户显式配置 maxTokens 的 OAuth agent 实际调用同样受益）。
- **发布 tarball 缺运行时模块（P0，FIX-014）**：v0.3.0~v0.3.2 的发布 tarball `files` 列表仅打包 7/12 个 lib 模块——缺 `attachments.js` / `memory.js` / `prestep.js` / `stats.js` / `wrapper.js`（均被 index/service/tool 深度导入）→ 安装后插件无法加载（E-3 隔离冷装完整 import 冒烟实证 `Cannot find module ...\lib\memory.js imported from lib\service.js`）。修复：files 列表补齐 5 模块（lib 段全量 12 项按字母序；发布时点由完整 ESM import 冒烟看护——见「版本说明·验证基线」）。**勘误披露：v0.3.0~v0.3.2 的 tarball 安装用户请升级 0.3.3**（junction 开发树安装不受影响——全部文件在位，这就是真机一直正常的原因）。

### 变更

- **ChatGPT 预设默认模型（EVO-008）**：新增账号预填模型 gpt-5.4-mini / gpt-5.4 → **gpt-5.6-sol / gpt-5.6-terra / gpt-5.6-luna**（用户指令 2026-08-30；**已配置账号的 models 字段不迁移不改写**）。client-render 正向断言锁定默认列表（防回归）。

### 破坏性变更

**无。**论证（四证）：① **依赖面零变更**——除版本号与 files 列表（FIX-014 补 5 个缺失运行时模块，只增不减）外，package.json 依赖面与 v0.3.2 完全一致（deps/peerDeps 逐项一致——reviewer 独立核验）；② **配置面零变更**——lib/schemas.js 零触碰（无键增删）；③ **数据面零变更**——统计 JSONL / 凭据文件 / oauthAccounts 结构不变；④ **行为变化均为缺陷修复而非能力收窄**——FIX-011/FIX-013 为回归/缺陷修复，FIX-012 的自动切换仅在贴图时发生且方向为扩展能力（此前被拦的带图消息现在可发）。**如实披露（no-overclaim）**：启用视觉类 agent 后贴图会自动切换会话模型到包装路由（此前需手动开启 takeoverDefaultModel 开关）——属修复而非破坏；且移除未发送图片不自动切回（见「修复·FIX-012」行为说明）。

### 已知问题

- **本项目暂无持续集成（CI）自动化**（v0.3.0/v0.3.1/v0.3.2 披露延续）：回归保护依赖发布前本地全量测试网（十五面全绿复跑，见「版本说明·验证基线」）。CI 建设已列入后续排期。
- **部分宿主下对话内图片附件可能显示「加载失败」**（延续，FIX-008 候选域）：附件识别功能已验通，服务端附件链已修复并验证 7 格式全绿；显示层残留属客户端渲染路径——本版不承载修复。
- **deepseek-official 视觉端点挂起**（宿主/端点域，EV-088 披露）：227s/0token 挂起属上游端点现象，vision 旁路（glm 等可用服务商）可用——本仓不可修，不构成本版回滚触发条件。
- **picked 层盲区**（宿主固有 P3）：selectModel 进程内选择不落 header/options 且插件无访问 API——selectModel 后首轮 prestep 与宿主判定可能短暂不一致，属宿主设计固有窗口（注释披露在案）。
- **接管语义边界（P3 讨论项，review-FIX-012-R0/R1）**：armedBy 来源标记（image 来源永不自动还原 / switch 来源走既有还原）与 deps 完整性（P2-1 已修复）的语义边界记录于审查报告，后续演进（如宿主 selectModel 增加图片校验）时复核。

### 版本说明

- **版本号**：0.3.2 → **v0.3.3**（PATCH）——三缺陷修复 + 一默认值调整，零新能力面，零 breaking（见「破坏性变更」四证）；MINOR 反论否弃：本版无新增功能面（默认模型列表调整属配置默认值更新而非新能力），v0.3.1/v0.3.2 PATCH 先例。
- **发布范围**：v0.3.2（tag d63b368，2026-08-30）以来 main 提交 **10 个**（`git rev-list --count d63b368..HEAD` 实采 2026-08-30）。三分账：**产品提交 8 个**——FIX-013 `5e15c43`（codex-responses 移除 max_output_tokens）/ FIX-011 `52331a8`（stats RPC 描述符修复 + 守卫测试）/ FIX-012 `c3831b4`（图片条件化接管 + 判别测试）/ EVO-008 `922c74f`（预设默认模型 gpt-5.6 系）/ FIX-012 `4f26846`（served-client 镜像同步）/ FIX-012 `5f68c17`（R0 P2-1 deps 修复）/ EVO-008 `785dc1e`（R0 P2-1 正向断言）/ **FIX-014（本提交——files 补齐 + 本段勘误，hash 见 git log）**；**治理提交 2 个**——`b796741` / `3e878b9`（REL-005 E-8 治理收尾尾段，归属 v0.3.2 发布链尾账，不计入产品面）。**结论行：8 + 2 = 10，与实采一致**；产品提交经逐 commit 对照在本节语义全覆盖，治理提交不入用户面；本版治理提交（发布链）随发布入仓。
- **验证基线**：门控 **15/15 套件 exit 0**（含新增 tests/rpc-shadow-guard.mjs 21 断言 / tests/fix-012-image-takeover.mjs 22 断言 / client-render 新增 gpt-5.6 正向断言）；**用户真机验证四项全过**（2026-08-30：统计面板恢复显示 / 文本模型贴图发送成功 / ChatGPT 测试按钮不再 400 / 新账号默认模型 gpt-5.6 系）。内部注解：发布执行段 bump 后全量门控复跑与 tarball 隔离环境安装冒烟（环境变量重定向至临时目录）随发布链执行，最终以复跑实测值为准；**E-3 隔离冷装自本版起升级为完整 ESM import 冒烟**（`node -e "import('dsh-agent-router')"` 全链加载 + tarball 清单核验 lib 12 模块）——历史版本冷装只验版本号与清单、探测深度不足（REL-004/005 口径，FIX-014 即由升级后的 E-3 实证），如实勘误。

## v0.3.2 — 2026-08-30

### 摘要

**多模态双修 + 账号面板 UX 版**。核心变化一句话：**纯图消息不再 400（FIX-009）、对话气泡恢复图片显示（FIX-010）、ChatGPT 订阅登录上移一级醒目位 + 移除不可用的 OAuth 官方登录入口（EVO-007）**。

### 修复

- **纯图消息 400/1213（P0，FIX-009）**：image-solo（用户只发图片无文本）经 wrapper 深改写后 user message content 变空 `[]` → 下游端点 400（GLM 1213「未正常接收到prompt参数」实测）。修复：wrapper 深改写后注入最小 text part（占位文案 `[图片已上传，请按系统提示操作]`）——委托请求恒含非空文本；4xx 快速失败（不重试不重发，错误带 provider/model 诊断）语义保持（判别测试 tests/fix-009-image-solo.mjs 9/9）。
- **对话气泡图片显示回归（P0，FIX-010 + REL-005 P1-1）**：prestep 会话路由判定与宿主实际路由分裂（读 agent.options 创建快照，宿主 selectionFor 实为 picked → 日志 header → live default）——默认模型漂移后误判 onWrapperRoute=false → 逃生组改写把图片块替换为标记文本并持久化日志层 → GUI 显示标记而非图片（用户实证 + 会话实录 ec6b01b0 铁证）。修复：prestep 判定 header 优先（与宿主 selectionFor 同序）+ 回落链补 live default（`agentDefaultModel.currentSelection()`，漂移窗口对齐）+ options 终回退——wrapper 路由会话日志层不再被改写；模型面适配保留在 wrapper stream 输入层（判别测试 tests/fix-010-gui-fidelity.mjs 13/13，含 REL-005 E 组漂移窗口判别）。
- **非 preset OAuth 账号删除路径恢复（EVO-007 R0→R1 返工）**：移除 OAuth 官方登录区块后，池内一键建号 / 历史自定义与粘贴 token / 未知 preset 值账号无删除入口、条目与凭据永久残留（P7 数据主权缺陷）。修复：账号池行「删除账号」按钮 + 高级扩展区「未入池的 OAuth 账号」列表行双入口——unset oauthAccounts 条目 + `credentials.unset(tokenRef)` 凭据清理 + 从全部账号池移除引用（preset 成员仍走 W-5 联动删除）。

### 变更

- **账号面板布局（EVO-007）**：多模态账号区新顺序 = API Key 账号 → **ChatGPT 订阅登录（一级醒目位，正式通道优先呈现）** → 子代理（无头 CLI）→ 高级扩展（仅账号池，折叠壳保留）。
- **「OAuth 账号（官方登录）」管理入口移除（EVO-007）**：官方 API 不提供 OAuth、Gemini 内置公开 Client 已被 Google 禁用——该入口自述不可用，整块移除（含文案/添加按钮/未配置态）；**oauthAccounts 数据域零触碰**（仅呈现层，EV-090），历史账号经账号池 / 未入池列表管理（删除入口见「修复」节）。README「v0.3.2 起」三处披露呼应（特性 / 多模态账号配置 / FAQ）。

### 破坏性变更

**无。**论证（四证）：① **无公共 API/协议面变更，依赖面零变更**——除版本号外 package.json 与 v0.3.1 完全一致（undici ^7.18.0 / peerDependencies 8×^0.1.0-rc.8 / files 11 项维持；产品 commits 零触碰 package.json，review-REL-005-R0「无 bump」实证）；② **无配置面变更**——lib/schemas.js 零触碰（无键增删；settings 遗留键容忍语义与 v0.3.1 相同）；③ **数据面零变更**——oauthAccounts 数据域 / 凭据文件 / 统计 JSONL 结构不变（EV-090「数据域零触碰」）；④ **入口移除对象为从未可用的误导性入口**（官方 API 无 OAuth——移除非能力收窄），行为变化均有披露与官方替代路径（README「v0.3.2 起」三处 + 本版变更节；替代路径 = 官方 API Key / ChatGPT 订阅登录 / 账号池管理）。

### 已知问题

- **本项目暂无持续集成（CI）自动化**（v0.3.0/v0.3.1 披露延续）：回归保护依赖发布前本地全量测试网（十二面全绿复跑，见「版本说明·验证基线」）。CI 建设已列入后续排期。
- **部分宿主下对话内图片附件可能显示「加载失败」**（v0.3.0/v0.3.1 已知问题延续，FIX-008 候选域）：附件识别功能已验通，服务端附件链已修复并验证 7 格式全绿；显示层残留属客户端渲染路径（与 FIX-010「气泡图片消失」为**不同缺陷面**）——本版不承载修复。
- **deepseek-official 视觉端点挂起**（宿主/端点域，EV-088 披露）：227s/0token 挂起属上游端点现象，vision 旁路（glm 等可用服务商）可用——本仓不可修，不构成本版回滚触发条件。
- **picked 层盲区**（宿主固有 P3）：selectModel 进程内选择不落 header/options 且插件无访问 API——selectModel 后首轮 prestep 与宿主判定可能短暂不一致，属宿主设计固有窗口（注释披露在案）。

### 版本说明

- **版本号**：0.3.1 → **v0.3.2**（PATCH）——零新能力语义（两处缺陷修复 + 一 UX 调整 + 收口），零 breaking（见「破坏性变更」四证）；MINOR 反论否弃：semver MINOR = 向后兼容的**新增功能**，本版无新增功能面（入口移除为清理**从未可用**的误导性入口，删除按钮双入口属缺陷补全而非新能力）；v0.2.1 PATCH 先例。
- **发布范围**：v0.3.1（tag 5ca8b87，2026-08-30）以来 main 全部提交 **14 个**（`git rev-list --count origin/main..HEAD` 实采 2026-08-30）。三分账：**产品提交 5 个**——FIX-009 `8877365`（wrapper 占位注入）/ FIX-010 `020909b`（prestep 判定 header 优先）/ EVO-007 `65226a3`（账号面板 UX：入口移除 + 布局）/ EVO-007 R0 返工 `3665d6a`（F-1 删除路径恢复 + F-2 文案）/ REL-005 收尾 `1d2bf36`（P1-1 live default 对齐 + P2-2 hygiene + README 收口）；**治理提交 9 个**——`d92dfba` / `ec94a6c` / `49ac8ab` / `0234a88` / `0c7f987` / `b664f52` / `ed229db` / `6877389` / `18d5cf1`。**结论行：5 + 9 = 14，与实采一致**；产品提交经逐 commit 对照在本节语义全覆盖，治理提交不入用户面。口径注记：`v0.3.1..HEAD` 计数 15 差 1——`4ee80e9`（REL-004 E-8 治理收尾）归属 v0.3.1 发布链（tag 后 origin/main 侧补录），不在本版 14 个内。
- **验证基线**（EV-090 Coordinator 实测 + 收尾段申报一致）：`tests/smoke.mjs` **963 ok / 1 skip / 0 FAIL / exit 0**；`tests/fix-009-image-solo.mjs` 9/9、`tests/fix-010-gui-fidelity.mjs` 13/13（含 REL-005 P1-1 漂移窗口判别 E 组）、`tests/routing-paths.mjs` 114/114、`tests/adapter-parity.mjs` 14/14、`tests/metrics.mjs` ALL PASSED、`tests/client-render.mjs` 130 断言。内部注解：发布执行段 E-2 全量门控复跑与 tarball 隔离环境安装冒烟（环境变量重定向至临时目录）随发布链执行，最终以复跑实测值为准。

## v0.3.1 — 2026-08-30

### 摘要

**转正版本**——**GPT OAuth 实验通道转正式**（DEC-026 C2）。核心变化一句话：**ChatGPT 订阅登录即正式通道——无实验开关、无实验弹窗、无实验标签（界面中英同步），登录即可用；关闭能力三层保留**。

### 新增

- **停用账号发起授权的拒绝遥测（可观测性，REL-004）**：已停用账号（`enabled=false`）发起 ChatGPT 授权登录时，拒绝动作记录原因 `account_disabled`（停用拒绝留痕；正常登录路径零事件）——停用语义可观测、可追溯。

### 变更

- **ChatGPT 订阅登录转正式通道（EVO-006，DEC-026 C2）**：「ChatGPT（实验）」入口转为正式登录通道——无需再开启任何开关，设置 → Agent 路由 → 多模态账号中直接一键登录；v0.3.0 已登录的账号升级后不受影响（凭据保留、继续可用）。
  - **实验开关与条款确认移除**：`router.oauthExperimental` / `router.oauthTosAccepted` 配置键废弃——升级零操作（旧配置中的遗留键自动忽略）；设置页不再有实验开关与条款确认弹窗。
  - **升级披露（「开过又关」用户请留意）**：v0.3.0 时期曾开启实验开关创建账号、后又手动关闭 `router.oauthExperimental` 的用户——该「关闭」偏好不随升级迁移（实验门控已随转正移除，此偏好无账号级停用对应物），升级后 ChatGPT 通道对您恢复可用；暂不使用可在账号卡片**停用账号**（`enabled=false`）或「登出并删除凭据」。
  - **风险提示保留（非阻断、非实验声明）**：界面保留平台服务条款与账号风控方面的常规风险提示。
  - **关闭能力不变**：总开关 `router.enabled`、单账号停用（`enabled`，调用与发起授权均被拦截并明确提示）、「登出并删除凭据」（恒可用，不受任何开关门控）三层路径全部保留。
  - 内部注解：DEC-026 用户裁决（「先仅将gpt实验通道转成正式的Oauth登陆逻辑，当前的验证证明OAuth登陆可行，其他的等后续的我的需求」——仅 GPT 通道，Claude/账号池方向不做）；判别测试 tests/oauth-promotion.mjs（旧实验语义必败断言 + 关闭能力保留断言）；kill-switch 转正后为三层新语义（① 通道开关 / ② 账号开关——调用与发起授权双侧判别为本版补全 / ③ 登出删除恒可用）。

### 破坏性变更

**无。**论证（四证）：① 无公共 API/协议面变更，**依赖面零变更**——除版本号外 package.json 与 v0.3.0 完全一致（undici ^7.18.0 / peerDependencies 8×^0.1.0-rc.8 维持）；② 配置键废弃带容忍迁移——schemastery 非 strict 未知字段透传，旧配置中的 `oauthExperimental`/`oauthTosAccepted` 遗留键零启动阻塞、零行为改变，升级零操作；③ 行为变化均有披露与官方替代路径——「开过又关」cohort 见「变更」节升级披露（替代路径：账号卡停用或登出删除），v0.3.0 已登录账号升级后凭据保留、继续可用；④ 关闭能力三层全保留且不弱于 v0.3.0（实验 flag 层退役属转正裁决内的语义消亡，非破坏）。

### 已知问题

- **本项目暂无持续集成（CI）自动化**（v0.3.0 披露延续）：回归保护依赖发布前本地全量测试网（十面全绿复跑，见「版本说明·验证基线」）。CI 建设已列入后续排期。
- **部分宿主下对话内图片附件可能显示「加载失败」**（v0.3.0 已知问题延续，跟踪至后续版本）：附件识别功能已验通，服务端附件链已修复并验证 7 格式全绿；显示层残留属客户端渲染路径，后续版本专项修复（FIX-008 候选域）——本版不承载修复。

### 版本说明

- **版本号**：0.3.0 → **v0.3.1**（MINOR）——转正为用户可见能力语义变化（缺省即正式通道 + 配置面废弃 + 升级行为披露），非缺陷修正，不判 PATCH；零 breaking（见「破坏性变更」四证）；0.x 阶段 MINOR 承载新能力语义（先例 v0.2.0）。
- **发布范围**：v0.3.0（tag bb81abf，2026-08-29）以来 main 全部提交 **12 个**（`git rev-list --count bb81abf..HEAD` 实采 2026-08-30）。三分账：**产品提交 7 个**——EVO-006 转正 ×3（bf667b3 服务端语义 / 6ebe9ed 客户端 UI / a4d33cd 文档口径）+ REL-004 收尾 ×4（6cbe4fc 停用拒绝遥测 / f2f3ba5 「开过又关」升级披露 / 21db990 feature-flags ②层新语义注记 / 866d4df 测试注释归属修正）；**治理提交 5 个**——b2a1f66 v0.3.0 发布链收尾入账 / a6acd06 EVO-006 入账收尾（DEC-026 裁决）/ 0eb1324 EVO-006 治理收尾 / 820d862 REL-004 收尾段治理 / a9651ad REL-004 规划段三产物入仓。**结论行：7 + 5 = 12，与实采一致；产品提交经逐 commit 对照在本节语义全覆盖，治理提交不入用户面。**
- **验证基线**（EV-086 十面，2026-08-30 M-3 无沙箱复跑权威）：`tests/smoke.mjs` **948 ok / 0 FAIL / exit 0**；九套件零回退——`tests/stats.mjs` 110、`tests/routing-paths.mjs` 114/114、`tests/adapter-parity.mjs` 14/14、`tests/attachments.mjs` 65/65、`tests/oauth-credentials.mjs` 98、`tests/oauth-loopback.mjs` 20、`tests/client-render.mjs` exit 0、`tests/oauth-promotion.mjs` 13/13、`tests/metrics.mjs` ALL PASSED。内部注解：发布执行段 bump 后全量门控复跑与 tarball 隔离环境安装冒烟（环境变量重定向至临时目录）随发布链执行，最终以复跑实测值为准。

## v0.3.0 — 2026-08-28（发布候选——正式日期以 v0.3.0 tag 为准）

### 摘要

**双主题版本**——**C-1 ChatGPT 订阅接入 + C-3 统计持久化**（DEC-025 D-1a）。核心变化一句话：**ChatGPT 订阅账号可以一键登录接入路由（实验开关，默认关闭），用量统计缺省落盘、重启不再清零**。

### 新增

- **ChatGPT 订阅一键登录（实验，C-1）**：设置 → Agent 路由 → 多模态账号 →「ChatGPT（实验）」一键登录——浏览器完成 OAuth 授权后凭据自动落盘（独立文件、仅当前用户可读），账号卡可直接选用订阅模型对话（含带图）。
  - **授权通道双路径**：优先本地回调（1455 端口，秒级完成）；端口被占用时（如 Codex CLI 等生态工具在监听）**自动降级设备码通道**——展示授权链接，在任意浏览器完成授权，插件后台轮询兑换（15 分钟有效窗口；瞬时网络错误自动退避重试，不会因单次抖动失败）。*兑现 v0.2.1「完整功能（协议分支/UI/设备码）将在 v0.3.0 发布」预告。*
  - **使用前提**：需在设置中显式开启 `router.oauthExperimental`（**默认关闭**）；首次开启需确认风险声明（实验条款，自担风险）；chatgpt.com 通常需代理可达（`oauthProxyUrl` 可配，授权端点直连）。
  - **登出即删除**：账号卡「登出并删除凭据」——删除凭据文件 + 清除账号条目 + 清除账号池引用，三处联动不留残留（含登录过程中登出的竞态保护）。
  - 内部注解：preset 账号 + codex-responses 协议分支 + per-protocol 能力接口（image/speech 留桩）；C-9 观测埋点自本版起采集（报告将于 v0.3.2 出——本版不含任何成功率结论）。
- **统计持久化（C-3）**：
  - **按天落盘**：用量统计按天写入 DSH 插件数据目录（`$DSH_HOME`）的 JSONL 文件，**重启后统计保留**（此前为内存态、重启清零）；默认保留 90 天。
  - **按天视图**：统计面板新增按天聚合视图（每日调用/失败/tokens）。
  - **CSV 导出**：统计面板新增导出按钮——可选时间范围与粒度（全局/Agent/账号级），导出 CSV（含公式注入防护）。
  - **数据安全**：原子写入、损坏自愈、版本迁移、清空保护（一键清空走软删除备份，误清可恢复）。
  - **成本单价表**：可为模型配置单价用于成本估算。
- **`router.oauthExperimental` 配置开关**（默认 false）：ChatGPT 实验通路总开关（合规三层开关的第③层；见「变更」节统计开关与 README）。

### 变更

- **【数据行为变化——请留意】统计缺省落盘**：升级到 v0.3.0 后，用量统计**默认写入磁盘**（位置：`$DSH_HOME` 插件数据目录；按天 JSONL；默认保留 90 天）——此前版本统计仅存内存、重启即清零。**不希望落盘的用户**：设置中关闭 `router.stats.persist`（false = 回到纯内存行为，此前已落盘的数据不受影响，重新开启后自动全量恢复）。
- **宿主依赖声明提升**：peerDependencies 8 个 dsh-* 包 `^0.1.0-rc.6 → ^0.1.0-rc.8`（对齐实测宿主版本）。**仍在 rc.6 宿主环境的用户升级后可能收到 peer 依赖警告/安装拒配**——请先升级 DSH 宿主再安装本版。
- 统计逻辑从 service.js 分离为独立模块 lib/stats.js（架构内聚，行为等价）。

### 修复

- **多模态路由失效、附件链断裂与会话图片消失（P0 热修，FIX-003 + FIX-003C）**：DSH 宿主静默更新（npx 缓存刷新）后，能力探测与附件注册接口行为回归——视觉 agent 收不到图片（`includeImages` 未达 / `attachmentIds` 报「未注册且宿主无法读取」/ 文件被「模型不支持图片输入」拒绝），会话气泡中的图片消失。修复：能力预检改按真实宿主提供方清单判定 + 附件懒注册自取证降级（自动探测图片尺寸、内容哈希兜底）+ 设置侧模型能力声明补全（宿主 GUI 写回配置会覆盖内置能力声明的陷阱）；经用户真机验证恢复。附件根目录回退与 WebP 尺寸探测边界两处遗留随本族并入（b6581c5 / 0782516）。
- **OAuth 代理路径在发布环境必失败（P0，FIX-006）**：运行时代理依赖 undici 此前未声明——发布安装后配置代理的 ChatGPT 调用必然报「Cannot find package」。修复：显式声明 `undici ^7.18.0` + 代理 dispatcher 主版本判别（版本不匹配立即明确报错，不静默错行为）+ pnpm-lock 入仓 + 适配器契约对齐宿主 rc.8。
- **接管开关的会话记忆缺陷（FIX-002 发布后返工）**：v0.2.1 的 `takeoverDefaultModel` 修复在客户端会话层缺少与服务器端对等的记忆机制——开启接管后切换会话（含起子代理）场景，用户手动改回的模型可能不被记住；补齐对等记忆三态 + 护栏与不变量断言（72b2670 / 0b3c15d）。
- **设备码登录两处可靠性修复（REL-003）**：① 登出×授权兑换竞态——登录进行中登出后凭据不再「复活」落盘（persist 前复查 + 竞态判别测试）；② 瞬时传输错误不再终止设备码流程——单次网络抖动改为退避重试（窗口内有界），并以独立错误原因区分「传输错误」与「授权被拒」。
- **能力自证与预检可观测（FIX-004）**：宿主 GUI 写回模型配置丢失能力声明时，插件以适配器实测能力自证兜底；预检拒绝不再静默（6dd6e5b）。
- **路由引导条件化（FIX-005）**：`route_agent` 引导提示按主模型能力分级——主模型原生支持图片输入时不再注入冗余路由引导，工具描述同步中性化（a484469）。
- **附件链在宿主演进后回归（P0，FIX-007）**：宿主 dsh-attachment 更新至 `0.1.1-rc.2`（npx 缓存静默刷新）后，已注册附件的懒注册读取以裸 id 调用宿主 `readImage`——新宿主的元数据全等校验（mediaType/字节数/尺寸必须与 ref 一致）恒拒（`ATTACHMENT_CORRUPT`）；WebP 尺寸探测存在字节偏移（VP8 有损将块尺寸当帧标签、VP8L 位拼装不完整）导致 `attachmentIds` 链全断——表现为对话内图片附件「加载失败」与部分情形下整体变慢（端点重试链放大）。修复：完整 ref 读取单点（条目元数据 → 权威 ref → 宿主读取 → 自取证降级回填）+ WebP 三分支尺寸探测器按规范重写（RFC 6386）+ 元数据补全 + 降级可观测事件（`attachment_ref_degraded`）；判别测试先红后绿 + 真实宿主隔离验证 7 格式（PNG/EXIF-JPEG/大图/VP8/VP8L/VP8X/GIF）全绿（b816601 / 08accbd）。

### 破坏性变更

**无。**论证：① 既有 OAuth 通用账号（粘贴 token）与 API Key 账号行为不变；② ChatGPT 实验通路全程由 `oauthExperimental`（默认 false）门控——不开启零可见；③ undici 为 dependencies 追加（非 peer 收窄）；④ 统计落盘虽为可感知的数据行为变化，但带等价回退开关（`stats.persist=false`）且不破坏既有数据。

### 已知问题

- **本项目暂无持续集成（CI）自动化**：回归保护依赖发布前本地全量测试网（六套件全绿复跑，见「验证基线」）。CI 建设已列入后续排期。
- **设备码授权失败时界面反馈较弱**：15 分钟窗口内若授权失败（拒绝/过期），账号卡状态变化不明显——可关闭后重试，或改用「粘贴 token」方式添加账号（后续版本将增强失败状态展示）。
- **部分宿主下对话内图片附件可能显示「加载失败」**（已知残留，跟踪至后续版本）：附件识别功能已验通（识别文本正常返回），但部分宿主/部分路径下对话内图片附件渲染可能显示「图片加载失败，点击重试」（客户端 imageData 译码面拒绝）——服务端附件链已修复并验证 7 格式全绿，显示层残留属客户端渲染路径，后续版本专项修复（见 FIX-008 候选域）。
- **统计历史规模性能未经实机标定**：100 天以上数据量的加载性能（目标 ≤200ms）为观察项，非已达标声明。
- **新装依赖解析时变**：npm 安装包不含锁文件，新安装将解析 undici 7.x 最新版（可能与开发验证版本存在 minor/patch 差异）；主版本不一致时会明确报错而非静默异常。
- **宿主 npm 环境缺陷（正交披露）**：npm 11.8.0 的依赖树解析对 dsh-* 环形 peer 关系存在缺陷（children null）——属宿主环境问题，非本插件缺陷；安装验证以 `--legacy-peer-deps` 绕行。

### 版本说明

- **版本号**：0.2.1 → **v0.3.0**（MINOR）——双主题均为向后兼容新能力面，无破坏性变更（见上）；0.x 阶段 MINOR 承载新功能（先例 v0.1.7→v0.2.0）。范围变更留痕：C-3 统计持久化原排 v0.3.1，因已在 main 完成随本版如实并入（DEC-025 D-1a）；设备码通道为兑现 v0.2.1 预告补实现（DEC-025 D-2a）。
- **发布范围**：v0.2.1（tag a1ab717，2026-08-22）以来 main 全部提交 **86 个**（`git rev-list --count a1ab717..HEAD` 实采；三分账清单基线 `.governance/gate7-commits-v030.txt` 81 行 + 后续 5 commits：a066de3 REL-003 资产入仓（治理）+ FIX-007 族 ×4——efec43f 受理（治理）/ b816601 修复（产品）/ 08accbd P8 增补（产品）/ 5cf524d 终态入账（治理））。三分账：**产品提交 36 个（SHA 底账全量，按任务族）**——EVO-002 Step 5-7 ×6（db1ddf7 / 5f5a38b / 522025e / f9d8bf0 / cbfafcf / 3e95034）+ EVO-003 ×2（1199c0b / c2d01ea）+ EVO-004 ×7（31a8c74 / 4d08990 / a9f98b2 / 9fd6110 / dd5d310 / d8ee97c / 8938a54）+ EVO-005 设备码 ×3（6355eb7 / 65e7e34 / d49ed4c）+ REL-003 ×5（dcd44fa / 6dbe57d / fbdfc61 / d78dc00 / 850b30c）+ FIX-002 发布后返工 ×2（72b2670 / 0b3c15d）+ FIX-003 ×1（b6581c5）+ FIX-003C ×1（0782516）+ FIX-004 ×1（6dd6e5b）+ FIX-005 ×1（a484469）+ FIX-006 ×4（bef08eb / 6a7ba76 / 669679e / 69ccf94）+ **FIX-007 ×2（b816601 / 08accbd）** + DEV-002 测试基建 ×1（3e0e2b5——核心通路回归套件，见「验证基线」routing-paths）；**治理提交 50 个（按类计数汇总，不承载用户可见变更）**——GOV-002 会话治理/证据入账 ×37 + GOV-003 治理记录入仓 ×1 + GOV-004 工作流升级 ×1 + REL-001 发布收尾 ×3 + REL-002 规划与 M-0 裁决入账 ×2 + REL-003 资产入仓 ×1 + 任务审查/终态闭环入账 ×3（FIX-006 ×2、EVO-005 ×1）+ FIX-007 受理/终态 ×2。**结论行：36 + 50 = 86，与实采一致；产品提交经逐 commit 对照在本节（新增/变更/修复）语义全覆盖，治理提交不入用户面。**
- **验证基线**（EV-078 六套件复跑全绿 + Coordinator 三次运行时实测；**FIX-007 后以 934 为权威**）：`tests/smoke.mjs` **934 ok + 1 skip / 0 FAIL**（账目链 873→908→918（REL-003 时点）→ 932（FIX-007 b816601 +14）→ 934（08accbd +2）——运行时实测为权威，静态预估仅供交叉参考）；`tests/oauth-credentials.mjs` 98；`tests/stats.mjs` 110 / 0；`tests/routing-paths.mjs` 114 / 114；`tests/client-render.mjs` 全绿；`tests/adapter-parity.mjs` 14 / 14（宿主 rc.8 对齐）；`tests/attachments.mjs` 65 / 65（含 FIX-007 判别断言族，真实宿主 rc.2 隔离验证 7 格式全绿——EV-082）。tarball 隔离环境安装冒烟（环境变量重定向至临时目录）已通过（EV-078：undici 7.29.0、ProxyAgent 可构造、真实 ~/.dsh 零触碰）。

## v0.2.1 — 2026-08-22

### 摘要

**P0 热修版本**——承载 2026-08-22 两个生产故障修复与一个行为修正。核心变化一句话：**多模态"接管"从默认强制改为默认关闭（用户主权），并修复宿主 dsh-llm 更新导致的多模态消息全量失败**。

### 修复

- **多模态消息全量失败（宿主接口演进兼容，FIX-001）**
  - 症状：宿主 dsh-llm 更新（0.1.1-rc.2，npx cache 静默刷新）后，所有多模态消息报 `registration.adapter.prepareCall is not a function`。
  - 根因：新宿主每次分发先调 `adapter.prepareCall`（prepared-dispatch 契约）；本插件 twin 包装适配器为手工对象字面量（无基类继承），缺该方法即全量断裂。
  - 修复：twin 显式实现 `prepareCall` 并绑定 twin 自身方法（prepared 分发不得绕过 twin 的改写/直传逻辑）；6 处测试夹具同步修复；新增**适配器接口奇偶回归测试**（动态枚举宿主基类方法——宿主未来新增方法而插件未跟进时测试即红，RISK-003 看护网）（commit f1c4c91；R6 审查 + FIX-001b 返工 cba0d98）。
- **模型选择被强制覆盖（双层接管行为修正，FIX-002/FIX-002b）**
  - 症状：① 切换会话（含起子代理）后，用户手动选回的模型被强制切回多模态包装路由；② 纯文本消息也被强制经包装路由（中间层故障被放大为全量故障）。
  - 根因：两个独立的接管面均无开关且无用户主权保护——服务端默认模型接管（三个同步触发点无条件执行）+ 客户端会话级接管（effect 依赖 sessionId，起子代理即触发强制切换）。
  - 修复：新增 `router.takeoverDefaultModel` 配置（**默认 false**）统一约束两层——关闭时包装路由仅出现在模型列表（用户手动选用），默认模型与会话选择永不被触碰；显式开启后为一次性接管（记住来源，用户改回即尊重）；关闭开关/禁用插件时恢复原 provider；旧版本遗留的强制接管选择在首次同步时自动还原（commit d264f03 + 5c8f2dc；DEC-022 用户裁决）。

### 新增

- **`router.takeoverDefaultModel` 配置开关**（默认 false）：多模态自动接管的可选开关（见上）；schema 字段 + catalog 镜像（客户端可见）。

### 变更

- **ChatGPT 订阅 OAuth 服务层就位（不可见预置，EVO-002 Step 1-4b 部分）**：凭据模块（原子写/文件锁/自动刷新）、1455 惰性回调服务、授权流 preset 分支已进入 main——全部由 `router.oauthExperimental`（默认 false）与默认关闭保护，**用户零可见**；完整功能（协议分支/UI/设备码）将在 v0.3.0 发布。

### 已知问题

- 接管关闭（默认）时，多模态能力需要用户在模型列表手动选 `xxx + 多模态` 组，或在设置中显式开启 `takeoverDefaultModel`（换来零操作体验，代价是自动切组）。
- EVO-002 Step 5+（codex-responses 协议分支/客户端 UI/设备码）未包含在本版本——`oauthExperimental` 保持关闭即无影响。

### 版本说明

- **版本号**：缺陷修正 + 向后兼容的新配置开关（默认值不改变合规行为，改变的是缺陷行为本身）→ patch 级 **v0.2.1**。
- **发布范围**：v0.2.0（2026-08-20 tag）以来 main 全部提交——EVO-002 Step 1-4b（11c42c0 / 8ba6ab18 / 128cb810 / a2567db / 564e18c，OAuth 地基，kill-switch 默认关闭）+ FIX-001/001b（f1c4c91 / cba0d98）+ FIX-002/002b（d264f03 / 5c8f2dc）。
- **验证基线**：`node tests/smoke.mjs` 656 断言全绿；`tests/adapter-parity.mjs` 14 断言（首次真实运行，R6-F1 修复后）；oauth-credentials 75 / oauth-loopback 20；R6 独立审查链闭环（APPROVED_WITH_NOTES + 绑定条件全落地）。

## v0.2.0 — 2026-08-20

### 摘要

本版本是 **v3 架构迁移（MIG-001）的收官发布**。核心变化一句话：**带图轮从"自动切换到视觉模型"改为"主 agent 轮次 + 工具调用"**——主 agent 始终是唯一参与者，图片按主模型能力分级处理（原生直传 / 改写提示），并可跨轮指代、上传任意文件、统一寻址、三级展示。原定 v0.1.8 的范围（whole-turn 图片路由默认化）已被本次架构演进整体超越，故直接发布为 **v0.2.0**（见「版本说明」）。

### 破坏性变更

- **带图轮不再自动切换视觉模型（整轮路由移除）**
  - 此前（≤v0.1.7）：消息带图时，整轮请求会被静默切换到视觉模型处理（主 agent 的上下文与工具也随之一并切换）。
  - 现在：带图轮始终由主 agent 应答（请求恒为主模型），主 agent 按需调用 `route_agent` 工具把图片交给视觉专业 agent（专业 agent 以独立调用/子会话执行，只返回文本结果）。
  - **迁移指引**：升级无需额外操作。若你的主模型**没有**图片输入能力，插件会改写图片块并向主 agent 注入提示（reminder / 标记 / 记忆段），请确认已配置至少一个视觉类专业 agent（能力标签含 `image`），主 agent 才能委派识别；若主模型**有**图片输入能力，图片会保真直传（见「新增」）。图片识别结果仍照常到达。
  - 内部注解：`lib/tool.js:199-232` 整轮路由注册与 `[用户附带图片]` 死规则移除（EV-011，Step 1，commit 7cb2024；DEC-008 / ADR-001）。
- **"用户消息带路径清单"注入规则移除**
  - 此前插件注入的路径清单声明与真实机制（原生草稿轨）矛盾，可能误导主 agent 判断"无图"；现移除，改为与真实附件通道一致的提示文本（EV-012，Step 2，commit b7261d5）。

### 新增

- **图片按主模型能力分级（preserveImageInput）**：主模型支持图片输入 → 图片块**保真直传**（零改写）；不支持 → 当前轮图片改写为标记 + 行为提示，历史轮改写为记忆段；能力查询失败安全回落改写，不静默丢图（EV-013，Step 3，commit a23b338；DEC-010 / ADR-003）。
- **imageMemory 跨轮指代**：图片轮识别结果自动回写记忆；后续文本轮追问（如"刚才图里 X 是什么"）时，历史图以记忆段（描述 + 附件 id）注入主模型——无需重传图片、无需重新描述（EV-014，Step 4，commit 374edfa；DEC-012 D-5）。
- **附件统一寻址 + `attachmentIds` 参数**：附件 id ↔ 工作区路径 ↔ `files` 参数三向映射统一管理；`route_agent` 新增 `attachmentIds` 参数，可跨轮精确引用历史附件（如"再看一眼那张图"）（EV-015/EV-016，Step 5a/5b，commit f89b8bd/f294c3c；DEC-011 / ADR-004；D-1-4 编址往返 100% 自动化验证——EV-023）。
- **任意文件上传（音频 / 视频 / 文档）**：对话框附件按钮扩展——此前仅支持图片，现在音频、视频、文档可直接上传，落盘工作区 `.router-files/`，以附件卡片 + 结构化路径文本进入消息，主 agent 可见可引用（EV-021，Step 8，commit e88dfb2；DEC-007 交互全景）。
- **工具产物三级展示**：L1 原生图片块（会话日志）/ L2 缩略图画廊（工具卡片）/ L3 路径文本 +「打开文件」——音频/视频内联播放、其他文件下载（EV-022，Step 9，commit 0554c5d）。
- **带图轮行为提示（reminder）+ 逃生组兜底**：带图轮注入带附件 id 的行为指令（主 agent 明确知道可调 `route_agent`）；用户手动切回原组 / 未接管等逃生场景下，图片轮不再以裸图片击穿端点（EV-019，Step 6，commit 1f17ea8）。
- **模态能力矩阵**：agent 目录按模态方向（image / audio / video / text / file，消费与产出）可观测、可检索；为音频/视频能力扩展留位（EV-020，Step 7，commit 12a8c71）。
- **文件通道 RPC**：`uploadFile` / `readWorkspaceFile` 通道契约就位——任意文件上传与工作区文件读取的基础设施（EV-018，Step 5c，commit 2c4b194）。
- **成功标准观测脚本**：`tests/metrics.mjs`——D-1 五指标 31 项自动化观测 + 门判定汇总（开发者可见；EV-023，Step 10，commit 53c0e40）。

### 变更

- **模型接管语义收窄**：接管从"静默模型替换"变为"准入放行 + 能力感知改写"——包装路由（twin）上运行的始终是主模型，不再发生模型替换（EV-013；DEC-009 / ADR-002）。
- **能力标签枚举化**：capabilities 从自由字符串改为枚举（image / audio / video / text / file）+ 消费/产出方向语义；未知值兼容放行（仅警告），老配置零破坏（EV-020；DEC-012 W-5 双轨兼容）。
- **`route_agent` 工具描述与真实机制对齐**：删除过时的路径清单声明，描述指向真实附件通道（EV-012，Step 2）。
- **附件寻址收敛**：图片注入 / CLI 物化 / 附件选择三处调用点统一经附件注册表寻址，消除路径与附件 id 双轨各自为政（EV-016，Step 5b）。

### 修复

- **图片注入失效（用户报告问题②）**：历史图跳过标记导致后续文本轮对图片一无所知（LP-1）→ 由 imageMemory 记忆段修复；"[用户附带图片] 死规则"误导主 agent 判"无图"（LP-3）→ 移除；空 provider 解析致图片二次剥离（LP-2）机制链 → 随整轮路由移除而消除（EV-011/EV-012；RES-001 问题②闭环）。
- **附件寻址三处分散收敛**：从"路径与附件 id 双轨、映射散落"收敛到统一注册表（EV-015/EV-016）。
- **工作区文件读取符号链接逃逸漏洞**：`readWorkspaceFile` 增加真实路径二次包含校验（fail-closed）——词法校验通过但 realpath 指向工作区外的符号链接/联接不再可读（EV-022，Step 9 返工，R12→R13）。
- **同名文件不再静默覆盖**：上传落盘采用排他写入 + 碰撞去重后缀，保持内容寻址完整性（EV-021，Step 8 返工，R10→R11）。

### 已知问题

- **端到端指标待实测**：图片到达真实视觉模型（D-1-2）、`route_agent` 带图触发率（D-1-3）、跨轮指代成功率（D-1-5）——机制面已全部就位并有自动化观测，真实比率需真实使用统计（U-3）后评估。当前门判定：**满足×2**（恒主模型 / 编址往返，自动化 100%）+ **部分满足×2**（图片到达 / 跨轮指代，机制面 100%）+ **待实测×1**（触发率）（EV-023；DEC-015）。
- **P3 记录项**：个别容器格式（如 m4a / mkv）在展示层可能类型误判（魔数检测 + 扩展名兜底的固有限制，EV-022 R12 F-4~F-10）；测试卫生遗留项（metrics.mjs 物化落盘残留、UTF-16 码元截断边界）已转后续任务域（EV-023 R14-F-01；EV-014 R4 F-2）。
- **统计为内存态**：用量统计随 DSH 重启清零（既有行为，与 v0.1.7 一致）。

### 版本说明

- **v0.1.8 跳号**：v0.1.8 原定范围（whole-turn 图片路由默认化，commit c2648d2 / 963b4f5）在开发中被 v3 架构演进整体超越；本次包含行为变更（见「破坏性变更」）与大量新能力，按语义化版本属 **MINOR 级**而非 patch 级，故直接发布 **v0.2.0**（DEC-015，用户 2026-08-20 确认）。
- **发布范围**：v0.1.7（2026-08-17 tag）以来 main 全部提交——治理接入（04f1e67，含锁文件 untrack 7c66e50）、v3 迁移 Step 1-10（7cb2024 / b7261d5 / a23b338 / 374edfa / f89b8bd / f294c3c / 98f04a3 / 2c4b194 / 1f17ea8 / 12a8c71 / e88dfb2 / 0554c5d / 53c0e40，含快照 9f485d9）、v0.1.8 原定范围（c2648d2 / 963b4f5）。
- **验证基线**：`node tests/smoke.mjs` 534 断言全绿（含附件注册表 49 断言）；`node tests/metrics.mjs` 31 项观测全过；MIG-001 全 13 单元 R1-R14 独立审查链闭环（含两次 NEEDS_CHANGE → 返工 → 复审）。
