# 真机验收清单 — 2026-09-13（v0.5.0 · 用户执行 / Coordinator 取证）

> 出具人：Coordinator（software-project-governance v0.80.0）· 出具时点：2026-09-13
> 覆盖：AUDIT-001（P0）· ARCH-004 真机复验项 · REL-012 E-7（v0.5.0）· FIX-032；附历史待复验项与已知宿主侧现象

## 一、前置事实（Coordinator 已完成核验，无需您操作）

- **插件安装点**：`~/.dsh/profiles/web/node_modules/dsh-agent-router` = **Junction → `D:\AI\agent\deepseek\plugins\router`**（本仓工作树）；该目录 `package.json` version = **0.5.0**（2026-09-13 实测）
- **注册面**：`~/.dsh/profiles/web/package.json` 含 `"dsh-agent-router": "link:D:/AI/agent/deepseek/plugins/router"`（profile = `web`）
- ⇒ **重启即加载本仓当前代码**（含本会话全部提交；无可疑版本错配）

## 二、执行前置（需您操作）

1. 重启 DSH（宿主 + 插件重新加载）
2. 刷新 GUI 页面（http://127.0.0.1:3080）

## 三、验收项（操作 → 通过标准）

| # | 归属 | 操作 | 通过标准 | 结果 |
|---|---|---|---|---|
| V-1 | REL-012 / ARCH-004 | 「设置 → Agent 路由」→ 看**总开关下方**的**宿主面健康面板与徽章**（README L153，v0.5.0 新增） | 面板可见：宿主版本 + 各宿主面健康状态（`ok`/`degraded`/`missing`）+ 最近宿主诊断事件；页面无「加载失败」 | ⬜ |
| V-2 | ARCH-004（面级降级可观测） | 同上：若任一面为 `degraded`/`missing`，读其诊断信息 | 有**明确**诊断（哪一面不可用 / 为什么）——非「某功能莫名失灵」 | ⬜ |
| V-3 | ARCH-004 / v0.5.0 原症状 | 打开设置页 + 整体使用 | **原报障不复现**：设置页不崩溃、无整体异常卡顿（症状①/②） | ⬜ |
| V-4 | AUDIT-001 **P0-A** | 触发一次**生成图片**（draw agent 或视觉路径）与一次**带图对话** | 图片经**同源画布直达显示**（v0.4.1 起 `/router-assets/` 内容寻址路径）；无「图片加载失败」 | ⬜ |
| V-5 | AUDIT-001 **P0-B** | 多模态账号 → ChatGPT 订阅：**登出**（必要时重启）再**登录** | 登出后账号**不复活**；新登录**不被顶**（凭据锁 + 盘上 CAS 生效） | ⬜ |
| V-6 | FIX-032（承载 v0.5.0） | 配置预设（主模型已配 + subagent **留空**）→ 新会话内**手动切换主模型** → 派生一个 subagent | subagent **跟随切换后**的主模型；对照：subagent **显式**配置时仍走显式值 | ⬜ |
| V-7 | EVO-014（历史待复验） | 新建 governance 会话 | 模型选择器**立即**显示预设模型（无需先发消息） | ⬜ |
| V-8 | 回归抽样（可选） | 统计信息卡（展开） | 账号卡无 `-router` 伪实体 / 无 `twin` / 无 `oauth:` 前缀；「主模型」分组卡在（与 EV-168 验收一致，无回归） | ⬜ |

## 四、已知宿主侧现象（**非本插件缺陷，勿计入失败**）

- **DEFERRED-001**：header 无预设的会话（`a222d922` 形态）上，GUI 预设下拉切换**不产生 `agentPreset.select` 请求**（您 F12 亲证零请求行；同会话直发 RPC 立即生效）——宿主 GUI 层问题，已挂账（EV-138）
- **EV-137**：带切换历史的空白老会话重启后，状态可能不是最后预设配置——**切一次即恢复**（resume 链 header 无预设 + 事件重放缝隙）

## 五、取证与入册协议（Coordinator 侧，本清单随结果更新）

- 您回传：**每项 通过 / 不通过 + 现象**；显示层项（V-1/V-3/V-4/V-8）请**附截图**（按 sha256 记录）
- 我据此**机写 evidence 行**（提交人 = 用户真机验收，Coordinator 转录）并更新 AUDIT-001 / ARCH-004 / REL-012 / FIX-032 的复验项状态
- **不通过项**：立即立案（change-triage + 修复链），保留您的原始现象描述与截图
- **边界声明（M7.7 R1）**：本批操作全部落在您的真实环境（`~/.dsh`、GUI）——Coordinator **不代跑**真实环境操作；重启/点击由您执行，我只做取证、入册与后续修复

## 六、状态

- 🔄 **执行中（2026-09-13，用户执行）** —— 第一批结果：

| 项 | 结果 | 证据 |
|---|---|---|
| V-1 | **✅ 通过** | 宿主面健康面板与徽章可见（`✓ 宿主面正常` + 五面探针 ✓ llm/settings/credentials/agentPresets/session）；截图 `sha256:1e473d90…` —— **同时证明 link 安装生效** |
| V-2 | **N/A** | 无 degraded/missing 面 |
| V-3 | **✅ 通过** | 设置页正常打开、无「加载失败」；用户报告整体功能正常（原症状①/②不复现） |
| V-4 | **✅ 通过（显示层证据）** | 对话内图片直达显示 + `route_agent · 多模型路由 成功` 卡（33.6s/21.2s）；截图 `sha256:4aa45ec2…` |
| V-5 | **✅ 通过** | ChatGPT 订阅登出 → 登录正常（用户报告） |
| V-6 | **✅ 通过** | 预设（主模型已配 + subagent 留空）→ 会话内切主模型 ⇒ 派生 subagent **跟随**；对照显式配置走显式值（用户 2026-09-13） |
| V-7 | **✅ 通过** | 新建 governance 会话 → 选择器**立即**显示预设模型（无需先发消息）（用户 2026-09-13） |
| V-8 | **✅ 通过** | 统计信息卡 / 四卡摘要正常（用户报告 + 截图） |
| **V-9** | **✅ 通过（4/4：版本三值真实 · 失败行消失 · 面探针 5→18 + 诊断事件复活 · 预设生效诊断面板有数据；未重启即生效——按观察记录，机制不作结论）** | 修复落地 + 重启后：面板**宿主版本三值显示为真实版本号**（非 `?`）且**诊断事件区能显示失败/降级事件**（若有）；若仍为 `?`，应能**直接看到失败原因**（可观测化已落地，不再静默） |

- ⚠️ **新发现（已立案）**：面板**宿主版本三值恒渲染 `?`、诊断事件恒空** —— 事实链指向 `router/hostFaceDiagnostics` RPC 值未落地，且客户端唯一取数点**吞错零观测（P8 违规）** ⇒ **FIX-046**（`TRIAGE-FIX-046`；EV-214）。**根因待可观测化后的信号判定，不臆测。**

