# 代码审查报告（review-FIX-042-R1-input）

- **round = R1**（R0 返工后复审；同一 Reviewer 实例链）
- **前轮引用（MUST 读，已读）**：`REVIEW-FIX-042-R0` / 报告 `.governance/review-FIX-042-R0-input.md`（R0 = **NEEDS_CHANGE**，`unresolved_blockers=1`；P2×1〔F-1〕+ P3×4〔F-2/F-3/F-4/F-5〕）
- **审查对象**：返工 5 笔 `246c8bb`（F-1+F-2）/ `14156b8`（F-3）/ `866f688`（F-4）/ `8aed89c`（F-5）/ `9b262ef`（F-1b 自发现）；起点 `61af16a`；受审 HEAD = **`9b262ef`**；文件集 = **3 文件 `+45/−10`**（`lib/oauth-llm.js +1/−1`、`lib/wrapper.js +2/−2`、`tests/host-abi-health.mjs +42/−7`）
- **审查者**：Code Reviewer Agent（只读；**未修改任何被跟踪文件**；唯一写入 = 本报告 + 6 个 gitignored 只读核验脚本 `.test-home/fix042-r1-*.mjs`）
- **执行依据**：5 笔 `git show` 全量 diff 逐行 + HEAD 终态逐行 + **独立复算**（多修订清点复跑 / ANCHOR_CASES 25 条目逐串实算 / 守卫自指双口径 / **栈法括号定位**）＋ 前轮既有自建工具复跑 ＋ **宿主树只读复核**（`$DSH_HOME/profiles/node_modules/@deepseek-ai/dsh-llm`）
- **结论（四态）**：**APPROVED_WITH_NOTES**
- **独立结构字段**：**`unresolved_blockers=0`**
- **计数（本轮新发现）**：P0 = 0 / P1 = 0 / P2 = 0 / **P3 = 3**（1 条本批新引入的**无效 needle**、2 条文档残留口径）

**一句话理由**：R0 的唯一阻塞项 **F-1 真闭合且经我独立复现**——注释块与证据表**双双落地三件套**（数字 + 取数修订 + 复跑命令 + 差额说明），我以**同一脚本在多修订复跑**逐项复得声明值：**`6bc3841` = 97（H1 57 / H2 40 / 22 文件）、`61af16a` = 92（H1 54 / H2 38 / 19 文件）、交集 = 84、差集 = 13 / 8 且逐条列名与声明位点逐字吻合**，算术自洽（97 − 13 + 8 = 92）；**F-1b 的「修订绑定时态口径」经我判定真正消除了自我引用不稳定性**（97/92 均绑定不可变修订 ⇒ 永久可核；同时**如实登记**「任何引用 MUST 按引用时点重跑并标注修订」——我实测该纪律**必要且成立**：`61af16a→8aed89c` 间计数由 92 变 94，正是该不稳定性的实证）。F-2 的实测计数回填与 `--numstat`/`--shortstat` **逐笔吻合**（W1 12 位点/8 文件/`+20/−19`、W4 4 文件/`+14/−4`、合计 15 文件）——**且 Developer 对我 R0 报告的一处事实更正完全成立**（`d58c5ca` 的 numstat 确不含 `tests/client-render.mjs`；我在本报告 §二 如实更正，并**追加更正我自身另一处口径**：W3 笔 numstat 实为 **9 文件**而非我 R0 所写「6 文件」、亦非提交信息「7 文件」）。F-3 的符号链式三段对象我**在宿主树逐一复核成立**（`resolveModelInfo` `:2043` → `resolveModelInfoFor` `:2046` → `adapter.resolveModel` `:2047`），**零新幻觉引用**。F-4 的双口径经我**独立计数复现**（严格 = **0**、宽 = **89**），「声明强度超过事实」的自我更正到位。**F-5 的栈法复核证实 Developer 的更正与我的更正——但方向与 R0 相反**：真未闭合位点是 **`:312`（`createOauthAdapter` 的 JSDoc，批前即存在、与 FIX-042 无关）**，**我 R0 所报 `:89` 属误归属**（`:89` 的 `（` 由 `:91` 的 `）` 结构性闭合、栈法配对成立）——已如实更正并在本报告 §二 标注。5 笔**全部为注释/登记面**（非注释改动行 = 2 行、均为 `ANCHOR_CASES` 数据条目，判据谓词逐字未改）、越权面零、断言集合**零增删**（169→169）、`ANCHOR_CASES` 25 不变、守卫 **174 断言 PASSED**、门控 **exit 0**、镜像 **SHA256 恒等**、**零宿主树操作**。余下 **P3×3**（F-3 登记文本使 `dsh-client-modules lib/client.js:265-268` 成为**新引入的自碰撞死 needle**（与既有 `:742` 同类）；守卫注释仍写「9 处 `lib/client.js:<NNN>` 自锚」而表内实测为**宿主包锚**；表中栏位「9 处 `/tmp/fix042-head.txt` 时点」数字随文本漂移）**均不影响判据、不阻塞**。⇒ **判通过终态，复审链结束（无需 R2）。**

---

## 一、R1 独立取证清单（全部实测；不采信 Developer 取证件内容）

| # | 事实 | 命令 / 结果 |
|---|------|------------|
| 1 | 范围与越权面 | `git rev-parse HEAD` = `9b262ef…`；`git log --oneline 61af16a..9b262ef` = **5 笔**（`246c8bb` → `14156b8` → `866f688` → `8aed89c` → `9b262ef`，顺序与声明一致）；`git diff --stat` = **3 文件 `+45/−10`** ✅；`git diff --name-only 61af16a..9b262ef -- AGENTS.md CHANGELOG.md package.json .governance` = **空** ✅；`git status --porcelain -- lib tests` = **空**（`lib/**`/`tests/**` 零残余）✅；`git status --porcelain -uall` = 仅 `.governance/**` 在途项（`M evidence-log.md` / `M plan-tracker.md` / R0 报告与机录 `??`，均属 Coordinator 侧，非本批 commit 内容）✅ |
| 2 | **守卫套件（独立复跑）** | `node tests/host-abi-health.mjs` = **`ALL HOST ABI HEALTH TESTS PASSED (174 assertions)`，exit 0** ✅（与 R0 相同，未见增量） |
| 3 | **门控（独立复跑）** | `node tests/run-all.mjs` = **`ALL 20 SUITES + 4 RUNNER MODULES (via smoke.mjs) PASSED (22.5s)  #SKIP 2 (smoke.mjs×2)` exit 0** ✅（与 Coordinator 21.7s / R0 21.8s 同量级） |
| 4 | **镜像对字节恒等** | 两文件 SHA256 均 = **`A98743543D9AFDA062ADB6B2F89CDA5B22B490C19A2597FE48DBDD3AB811D6A0`** ✅；`git diff --name-only 61af16a..9b262ef -- lib/client.js tests/served-client.js` = **空**（本批未触碰镜像对）✅ |
| 5 | **F-1 三件套：数字+修订+复跑命令（多修订独立复跑，核心）** | 以 R0 自建脚本（`git show <rev>` 口径 + 与 `fix042-host-family.mjs` 同一正则/分组/排除面）跑 7 个修订：<br>`6bc3841` = **97**（H1 57 / H2 40 / 22 文件）；`61af16a` = **92**（H1 54 / H2 38 / 19 文件）；`246c8bb` = 92；**`14156b8` / `866f688` / `8aed89c` / `9b262ef` = 94**（H1 55 / H2 39 / 19）<br>**交集 = 84**（声明 84）✅；**差集 = 13 / 8，且 13 条与 8 条逐条列名与声明位点逐一吻合**（13 = `lib/client.js:4971` + `inject-manifest.js:16` + `llm-selection.js:13/14/16/187` + `host-route.js:25` + `wrapper.js:261` + `client-render.mjs:1982/1987` + `rpc-shadow-guard.mjs:11/41` + `served-client.js:4971`；8 = `tests/host-abi-health.mjs:728/:729/:733/:796/:797/:798/:799/:814`）✅；算术自洽 `97 − 13 + 8 = 92` ✅ |
| 6 | **F-1 差额工具复跑** | `node .test-home/fix042-inventory-diff.mjs fix042-base.txt fix042-head.txt` = **「批前 = 97 处；HEAD = 92 处；交集 = 84」**，差集清单与 #5 逐条一致 ✅（`fix042-base.txt` 21785B / `fix042-head.txt` 21880B 均在档；`head.txt` 的行面与 `61af16a` 复跑逐条吻合 ⇒ 该时点可核） |
| 7 | **F-1b 有效性判定（关键，只读判别）** | **现写法下 97/92 不再失稳**——两者均绑定**不可变修订**（`6bc3841` / `61af16a`）并给出复跑命令，任一时点重跑均得同值（我 #5 已逐值复现）；**「再增行是否仍会失稳」= 会**（我实测 5 笔返工后 HEAD = 94/19，注释块自身新增行即含行号形态 ⇒ **注释块已如实预告**：「自我引用注意…每增一行都会再次改变计数（R0 返工笔后同一脚本实测已给 94/19）」）⇒ **残余风险已知且被显式登记**：**「当前时点数字」本身不可固化为文档值**，只能按引用时点重跑（纪律句已写入守卫 `:798-799` 与证据表「纪律」行）⇒ **该写法为正确处置**（把不稳定性从「缺陷」转为「已声明的引用纪律」） |
| 8 | **F-2 计数回填 vs 提交级实测（逐笔）** | ① W1：`fa99686` `--shortstat` = **8 files changed, +20/−19** ⇒ 声明「12 位点 / 8 文件 / `+20/−19`」中**文件数与行数逐字吻合**；12 位点由我自建 diff 解析器复算（R0 取证 #7 同法）= **12** ✅ ② W4：`d58c5ca` = **4 files changed, +14/−4** ⇒ 声明**逐字吻合** ✅ ③ 合计：4 笔 numstat 唯一文件并集 = **15**（`lib/` 8 + `tests/` 7）✅ ④ W2 断言中间态 171、W4 中间态 173 与终态 174 的关系自洽（W2 笔 +6 条目 = 165→171；W4 笔 +2 = 173；W3 笔 +1 = 174，与 `ANCHOR_CASES` 16→22→24→25 逐级对应）✅ |
| 9 | **F-2 中 Developer 对我 R0 的更正（核验）** | **更正成立**：`git show --numstat d58c5ca` = `tests/fix-012-image-takeover.mjs` / `tests/host-abi-health.mjs` / `tests/smoke.mjs` / `tests/stats.mjs`——**确不含 `tests/client-render.mjs`**；该文件的宿主锚改写确在 **`61af16a`**（其 numstat 首行之一 = `2 2 tests/client-render.mjs`）✅ 我 R0 §二 的归因错误见 §二「我方更正」 |
| 10 | **F-3 符号链式三段对象（宿主树只读复核）** | `$DSH_HOME/profiles/node_modules/@deepseek-ai/dsh-llm/lib/index.js`（版本 **0.1.5-rc.2**）：`resolveModelInfo` `:2043-2045`（`:2044 = return this.resolveModelInfoFor(...)`）、**`resolveModelInfoFor` `:2046`**、**`:2047 = const resolved = await registration.adapter.resolveModel(registration.provider.id, model, signal)`** ⇒ **三段链式逐段实存** ✅；原锚 `:1397-1403` 现为 `assembleAssistantStream` 函数体（R0 已实证）⇒ 去行号改链式**必要且正确** |
| 11 | **F-3 新锚/看护实算 + 零新幻觉** | `lib/wrapper.js` 内 `` `resolveModelInfo → resolveModelInfoFor → adapter.resolveModel` 链 `` = **1 次（在位）**；`（:1397-1403）` = **0 次（已清）**；`git grep ':1397-1403'` 全仓命中仅守卫注释/登记文本（`tests/host-abi-health.mjs:813/824/826/827`，属备忘/判据输入，非引用）⇒ **零新幻觉引用** ✅ |
| 12 | **`ANCHOR_CASES` 全 25 条目逐串实算（我自建解析器，不 import 被测文件）** | 解析出 **25** 条目、stale 串 **76**、fresh 串 **74**；对每条的声明文件实算 `staleHits` / `missingFresh` ⇒ **FAIL 条目 = 0** ✅（与守卫运行一致；含本批变更项：`lib/wrapper.js` 新链式锚在位、`（:1397-1403）` 零残留、`lib/host-abi/inject-manifest.js` 的 `:5060 先例` 零残留、`tests/stats.mjs`/`fix-012` 等既有项全绿） |
| 13 | **F-4 双口径独立复现** | 自建扫描（注释行 `^\s*(//|\*|/\*)` + 三锚形态）得**宽口径 = 33**（18 行）；Developer 脚本 `node .test-home/fix042-w2-selfref.mjs` 我**只读执行**得 **宽口径 = 89**（`path:line 57 / bare:NNN 17 / L### 15`）、**严格口径（目标解析为本文件）= 0**、UNRESOLVED = 5。差异源已定位：两者正则与「归属解析」范围不同（其含 `path:line` 57 条与跨块上下文解析），但**严格口径 = 0 在两套口径下一致** ⇒ **声明可独立复现** ✅ |
| 14 | **F-4 去行号改动与自指位点核对** | 守卫内 `:118-119` 的历史引用已改为 `` `tests/metrics.mjs` 该条目的旧锚名 ``（新串在守卫内 = **1 次**；旧串 `:118-119` 在守卫内**仍存在 1 次**——位于 `ANCHOR_CASES` 的 `tests/smoke.mjs` 条目 **stale 判据输入** `'…\n  // （符号名式锚；原写死行号'` 截断前的上下文 **`:118-119` 属 `tests/metrics.mjs` 项**，**非本文件自指**，且属**跨文件判据输入**⇒ **R0 严格口径（指向本文件）确为 0**）✅；语义等价（「旧锚名」= 原 `:118-119` 所指对象，归属显式化）✅ |
| 15 | **F-5 栈法复核（含我方 R0 误归属更正）** | 自建栈法（逐字符 `（` 入栈记行 / `）` 出栈）：<br>`lib/oauth-llm.js`：`6bc3841` = 106/105、**栈内未闭合 = `[312]`**；`61af16a` = 106/105、**未闭合 = `[312]`**；`9b262ef` = **106/106、未闭合 = [] 、孤立 = []** ✅<br>守卫：`6bc3841` = 318/317（差 1，未闭合 `[728]`）；`61af16a` = **357/355（差 2，未闭合 `[742,796]`）**；`9b262ef` = 389/390→**390/389（差 1，未闭合 `[742]`）** ⇒ **守卫差值回到批前水平（净零），未恶化** ✅<br>**R0 误归属确认**：`61af16a` 的 `:89` 处 `（` **由 `:91` 的 `）` 结构性闭合**（`:89` open=1/close=0、`:91` open=0/close=1、`:88-92` 全块平衡）⇒ **我 R0 报 `:89` 为「唯一未闭合」不成立**（我在 R0 用「计数差」而非栈法定位，把「差值唯一的候选行」误当「未闭合行」）⇒ §二「我方更正」 |
| 16 | **F-5 自家 needle 收敛与判别力** | `（:5060 先例` 在 `lib/host-abi/inject-manifest.js` = **0 次**（旧串）；`:5060 先例` = **0 次**（新串**同为 stale needle**，目标文件内零残留 ⇒ 判据正常绿）✅；**括号贡献核实**：该 `（` 原在**守卫清单文本**内（`61af16a` 守卫内 `'（:5060 先例'` = 1 次 → HEAD = 0 次），移除使守卫差值 2→1，**回到批前水平** ⇒ Developer 的「自家 needle 引入 +1 差并同步收敛」叙事**成立**（`8aed89c` 的守卫 numstat `1 1` 与该行改动逐字对应）✅ |
| 17 | **守卫判据代码行是否逐字未改 + 断言集合** | R1 范围 `61af16a..9b262ef` 的非注释改动行 = **2 行**，均为 `ANCHOR_CASES` 数据条目（`inject-manifest` 条目去 `（`；`wrapper` 条目 +stale `（:1397-1403）` +fresh 链式锚）⇒ **9h/9h-2/9h-2b/9h-2c/9h-2d/9h-3 谓词与代码零改动** ✅（我另按「剔注释 + 剔 `ANCHOR_CASES` 块 + 剔拼接常量」逐字比对亦得一致）；静态 `check('…'` 标签集 **169 → 169**，`Compare-Object` **逐元素相等**（**REMOVED = 0 / ADDED = 0**）✅；`ANCHOR_CASES` 条目 **25**（不变）✅ |
| 18 | **越权面 + 零宿主树操作（Developer H 项核验）** | 本批 `+` 行扫描 `DSH_HOME|node_modules|profiles[/\\]|C:\Users|/Users/|AppData|\.dsh` = **零命中**；`TODO|FIXME|XXX|HACK` = **零命中**；3 文件 `node --check` 全 **exit 0**；本审查对宿主树**仅 `Test-Path` + `Select-String`/`Get-Content` 只读**（F-3 复核），**未写任何宿主/配置路径**；工作树**零写**（所有变异在内存字符串上）✅ |
| 19 | **推送状态与锁状态** | `origin/main` = **`6bc3841`**；`git rev-list --count origin/main..HEAD` = **9** ⇒ 9 笔全部未 push ✅；`.governance/agent-locks.json` 现为 `active_tasks = {FIX-042}`、`file_locks = {tests/host-abi-health.mjs, lib/wrapper.js, lib/oauth-llm.js}`（与返工笔实际文件集**一致**；R0 所报的过期/范围不符已在返工中被重置）✅ |

---

## 二、R0 findings 逐条处置（已修复 / 未修复 / 新引入）

| R0 项 | 级别 | 处置 | 独立证据 |
|-------|------|------|---------|
| **F-1** W3「97/22」未绑定取数修订 ⇒ HEAD 不可复现 | **P2（阻塞）** | **已修复（真闭合）** | #5（7 修订复跑：97 / 92 / 94 逐值可复；交集 84；差集 13/8 逐条吻合；算术自洽）+ #6（工具复跑一致）+ #7（F-1b 判定：绑定不可变修订 ⇒ 97/92 永久可核；残余不稳定性已显式预告与登记）+ #7 的纪律句落位于守卫 `:798-799` 与证据表「纪律」行 |
| **F-2** 提交信息计数与实测不符（W1 9 vs 12；W3 7 vs 6） | P3 | **已修复（口径回填到位）** | #8（W1 `+20/−19`/8 文件、W4 `+14/−4`/4 文件、合计 15 文件逐项吻合；12 位点独立复算）+ #9（**Developer 对我 R0 的更正成立**） |
| **F-3** `lib/wrapper.js:266` 清单外真漂移未收口 | P3 | **已修复** | #10（宿主三段链逐段实存：`:2043/:2046/:2047`）+ #11（stale 零残留 / fresh 在位 / **零新幻觉引用**）+ #12（条目在 25 条实算中绿）+ 存量登记（`lib/client.js:4281`、`lib/service.js:1326` 不改的理由成立：宿主锚不可本仓核验、同句无具名宿主符号 ⇒ 避免新幻觉引用） |
| **F-4** 守卫「零处行号式自指」声明强度超事实 | P3 | **已修复（口径限定 + 去行号）** | #13（双口径独立复现：严格 **0** / 宽 **89**）+ #14（`:118-119` → `` `tests/metrics.mjs` 该条目的旧锚名 ``；旧串仅在**跨文件判据输入**中保留，非本文件自指）+ 守卫 `:829-838` 明写「**口径**：注释行内 `:NNN`/`L###` 且被指对象在本文件；stale 字段字面量与**他文件锚**的历史引用**不计入**」+ 「**原句属声明强度超过事实**」自我更正到位 |
| **F-5** `lib/oauth-llm.js` 未闭合括号（R0 报 `:89`） | P3 | **已修复（真位点改正）** | #15（栈法：`6bc3841`/`61af16a` 未闭合均在 **`:312`**、`:89` 结构性闭合；HEAD = 106/106、栈法零未闭合/零孤立）+ #16（自家 needle 收敛，守卫差值回到批前水平） |
| **R0 附带项**：`.md` 证据表未载三件套 | — | **已修复** | 表头新增「## 取数修订绑定（FIX-042 R0 F-1 三件套）」+「## 纪律」行（数字 + 修订 + 复跑命令 + 差额 + 不稳定预告）✅ **但注**：`.test-home/**` 为 `.gitignore:10` 覆盖面（我 `git ls-files --error-unmatch` 实测**未被跟踪**）⇒ 该表**不随包/不入仓**，**权威落点是守卫注释**（已入仓）——此点 Developer 已在守卫注释中引用工具路径，处置可接受，登记为 P3 观察 |
| **新引入（本轮）** | — | **P0~P2 = 0；P3×3**（见 §四） | — |

### 我方更正（前轮判断的如实修正，2 处）

1. **R0 报告：`lib/oauth-llm.js` 唯一未闭合括号在 `:89`** —— **不成立**。栈法实测（#15）：`:89` 的 `（` 由 `:91` 的 `）` 结构性闭合；真未闭合位点在 **`:312`（`createOauthAdapter` 的 JSDoc）**，且**批前（`6bc3841`）即存在**（与 FIX-042 无关，源出 FIX-034 时期的注释）。误因：R0 我以「全文件 `（`/`）` 计数差 = 1」定位候选行，把「差值唯一的可疑行」写成了「未闭合行」——**计数差无法定位未闭合位点，栈法才能**。⇒ 我 R0 的 F-5 **位点归属错误**；Developer 在返工说明中对我该项的更正**成立且更精确**（其称「`:89` 由 `:91` 闭合；唯一未闭合在 `:312`」与我的栈法结果逐字一致）。
2. **R0 报告：W3 笔「实测 6 文件」与 F-2 中「`client-render.mjs` 的宿主锚改写由 `d58c5ca` 完成」** —— **均不成立**。① W3 笔 `61af16a` `--numstat` 实为 **9 文件**（`lib/client.js` / `lib/host-abi/inject-manifest.js` / `lib/host-abi/llm-selection.js` / `lib/host-route.js` / `lib/wrapper.js` / `tests/client-render.mjs` / `tests/host-abi-health.mjs` / `tests/rpc-shadow-guard.mjs` / `tests/served-client.js`）——我在 R0 只数了「W3 锚位所在文件（6）+ 镜像对」，**漏计 `tests/client-render.mjs` / `tests/rpc-shadow-guard.mjs` / `tests/host-abi-health.mjs`**；② `tests/client-render.mjs` 的宿主锚改写**确在 `61af16a`**，非 `d58c5ca`（#9）。⇒ **Developer 对我 R0 的更正成立**，本报告如实更正（该更正同时使 F-2 的「7 文件」得以被正确定位为**口径未声明**：按 `lib/**` 计 5、按含镜像对计 6、按提交实际触面计 9）。

---

## 三、复审要求 7 项逐项结论

### 1. F-1 是否真闭合（唯一阻塞项）—— **真闭合；差额可复算；F-1b 有效；纪律已如实登记**

- **三件套齐备**：守卫 `:790-799` 与证据表「取数修订绑定」节**双双**含「数字 + 取数修订 + 复跑命令 + 差额说明」（我以 8 条串判定语句逐条实测在位）；`.test-home/fix042-inventory-diff.mjs` 为差额工具（含用法头）。
- **差额可复算（我独立复得，未采信 Developer 结论）**：`97 / 92 / 交集 84` 与 **差集 13 / 8** 全部一致，且**逐条列名**与声明位点一一对应（#5/#6）；算术 `97 − 13 + 8 = 92` 自洽。**声明的 H1/H2 分量亦逐个吻合**（`6bc3841` = 57/40；`61af16a` = 54/38）。
- **F-1b「修订绑定时态口径」是否真正消除自我引用不稳定性**：**是**（就「数字的正确性判定」而言）——97 与 92 分别绑定 `6bc3841` 与 `61af16a` 两个不可变修订，我**在 7 个修订上复跑逐值复现**，故这两个声明**永久可核**，不再随 HEAD 漂移。**残余风险判定（要求 3 的「若仍会，请给出残余风险判定」）**：**「当前时点」的数字仍会随每次文本增删而变**（我实测：`61af16a` = 92 → 返工笔 `14156b8` 起 = 94；此非缺陷而是**计数对象包含文档文本**的结构性事实）⇒ 残余风险 = **引用者若把「94」等时点值当长期基线即会复现 R0 的 F-1 类问题**；该风险**已被显式登记**（守卫 `:798-799`「任何引用 MUST 按引用时点重跑并标注修订、不得沿用他时点数字、不得作长期基线」+ 证据表「纪律」行），**且为正确的处置**：把「数字漂移」从隐含缺陷转为**可执行的引用纪律**。**我判该项闭合充分**。
- **「数字必须连取数修订引用」纪律是否被如实登记**：**是**（守卫 `:773-776` 既有告诫 + `:790` 明写「本批自设纪律『数字必须连口径引用』」+ `:799` 引用纪律 + 表「纪律」行：「引用必须连**取数修订 + 口径 + 复跑命令**，不得作长期基线（同 `lib/client.js`「41」口径）」）；纪律的**必要性**亦由我 #7 的实测（92→94）反证成立。

### 2. F-2 是否闭合 —— **闭合；回填逐笔吻合；Developer 对我 R0 的更正成立（已如实标注）**

- **逐笔吻合（抽查 ≥3，实为 4 项全查）**：W1 `fa99686` = `8 files changed, +20/−19`（声明 12 位点/8 文件/`+20/−19`）✅；W4 `d58c5ca` = `4 files changed, +14/−4`（声明 4 文件/`+14/−4`）✅；合计 15 文件（4 笔 numstat 并集）✅；中间态（161→171 / 173 / 174）与条目 16→22→24→25 逐级自洽 ✅。
- **我方更正**：见 §二「我方更正」第 2 条（`d58c5ca` numstat 确不含 `tests/client-render.mjs`；我在 R0 的归因错误与「W3 = 6 文件」的口径错误均已更正）。
- **残余（P3，已登记 §四）**：守卫注释 `:819` 仍写「含 `lib/client.js` 内 **9 处 `lib/client.js:<NNN>` 自锚**」，而 R0 与 FIX-041 R1 均实测该 9 处为**宿主包锚**（包名在前一行，表内归属列亦记宿主包名）⇒ 与表口径不一致（**历史口径残留**，非本批新增）。

### 3. F-3 —— **三段对象逐一实存、零新幻觉、隔行登记如实**

- 三段链（**宿主树 0.1.5-rc.2 只读复核**）：`resolveModelInfo` `:2043`（`:2044` 转调）→ `resolveModelInfoFor` `:2046` → `adapter.resolveModel` `:2047` —— **逐段实存** ✅。
- **零新幻觉引用**：新串在 `lib/wrapper.js` 实存（1 次）；`:1397-1403` 全仓仅出现在守卫的**登记文本/判据输入**中；`（:1397-1403）` 在目标文件 **0 次**（#11）；25 条目实算 **FAIL = 0**（#12）。
- **隔行站点登记如实 + 不改理由成立**：`lib/client.js:4281`（宿主 `dsh-host-apiproxy lib/index.js:2596-2630`）与 `lib/service.js:1326`（`:2582-2594`）已登记；**不改**的理由（宿主锚不可本仓核验 + 同句无具名宿主符号 ⇒ 改则制造新幻觉引用）**成立**——我复核两处原文：`client.js:4280-4281` 的宿主包名在**上一行**、`service.js:1325-1327` 的包名在**同块首行**且同句无宿主符号名，确无可稳定指代物；Developer 明示「后续批以逐处语义判定收敛」，与 FIX-041 R1 §三.5① 的硬前置一致。
- **看护**：wrapper 条目 **扩既有**（条目 25 不变、断言 174 不变）⇒ 符合 P5（同文件不重复登记），且 stale/fresh 双向均可实算（#12）。

### 4. F-4 —— **双口径可独立复现；自我更正到位；去行号语义等价**

- **双口径**：我自建扫描得宽口径 **33**（口径较窄：仅 18 行）；Developer 脚本复跑得 **宽 89 / 严格 0 / UNRESOLVED 5**——两者差异源于**正则粒度与归属解析范围**（其含 `path:line` 57 条 + 跨块上下文解析）；**关键值「严格口径 = 0」在两套口径下一致** ⇒ 声明（严格 0 / 宽 89）**可独立复现** ✅（我另以「本文件类标记」谓词独立复算亦得严格 **0**）。
- **自我更正到位**：守卫 `:829-838` 显式写明**口径**、并明写「**原句『本文件零处行号式自指』未带口径 ⇒ 属声明强度超过事实**（宽口径下另有多处他文件锚的历史引用）」——与我 R0 F-4 的判断逐字一致，属**如实自我更正**。
- **去行号语义等价**：`:118-119` → `` `tests/metrics.mjs` 该条目的旧锚名 ``（归属显式化、指代对象不变：原行号所指即 `tests/metrics.mjs` 的该条目）；旧串 `:118-119` 在守卫内的**唯一残留**位于 `ANCHOR_CASES` 的 `tests/smoke.mjs` 条目 **stale 判据输入**（跨文件判据，非本文件自指）⇒ **不违反限定后的结论**，且**不影响**该条目的判别力（#14）。

### 5. F-5（含我的 R0 误归属）—— **栈法独立复核成立；Developer 真位点结论正确；我方更正**

- **独立复核栈法结论**：`lib/oauth-llm.js` 在 `6bc3841` / `61af16a` / `9b262ef` 三点的**未闭合位点均为 `[:312]` / `[:312]` / `[]`** ⇒ **真位点是 `:312` 而非 `:89`** ✅；`:89` 的 `（` 由 `:91` 的 `）` 闭合（`:88-92` 全块平衡）。
- **R0 误归属如实标注**：见 §二「我方更正」第 1 条（**我 R0 所报 `:89` 不成立**；Developer 的更正成立且更精确——其并指出「按 `:89` 补会数值配平但**结构错配**」，该判断与栈法一致）。
- **106/106 与「栈法零未闭合/零多余」**：**成立**（HEAD = 106/106、`unclosed = []`、`orphan = []`；我另核 `lib/wrapper.js` 在该条目的 `（`/`）` 亦自平衡）。
- **自家 needle 收敛不损判别力**：`（:5060 先例` 为**含未闭合括号的变体**，其移除**只消除守卫文本自身的括号差**，目标文件内两串均 **0 次**（旧串本已零残留）⇒ stale 判据仍以 `:5060 先例` 为 needle，**判别力不变**（#16）；守卫括号差值由 2 回到批前 1（净零）✅。

### 6. 无新引入（P0~P2）+ 越权面 + 断言集合 + 门控 + 真实环境披露 —— **全部通过（附 P3×3）**

- **无新引入 P0~P2**：5 笔全为注释/登记面；非注释改动仅 2 行（`ANCHOR_CASES` 数据）；谓词零改动；`TODO` 零命中；`node --check` 3 文件 exit 0（#17/#18）。
- **越权面**：零（3 文件；`.governance/**`/`AGENTS.md`/`CHANGELOG.md`/`package.json` 零触碰；`lib/**`+`tests/**` porcelain 空）。
- **断言集合**：静态标签 **169→169**、**REMOVED = 0 / ADDED = 0**；`ANCHOR_CASES` **25** 不变；守卫 **174 断言**（运行实测）。
- **门控**：`run-all` **exit 0**（22.5s，`#SKIP 2`，两 skip 来源与基线一致）。
- **真实环境操作披露**：**属实且充分**——本批**零宿主树操作**（新增行零宿主路径命中；`+` 行亦无 `node_modules`/`$DSH_HOME` 字样），Developer H 项「宿主侧对象未核验」表述**与该批零操作一致**（**注**：F-3 的宿主链式符号来源有其 R0 轮次的宿主树只读取证与我的复核支撑，但**本轮未再触宿主树**——我本轮的宿主读取属**审查方只读复核**，已披露于 #10/#18，未写入任何路径）。锁状态：`active_tasks = {FIX-042}`、`file_locks` 三项与实际改动文件集一致 ✅。

### 7. 关键判定：本批（9 笔）是否具备 push 条件 —— **具备；`unresolved_blockers = 0`**

- **判定：可 push** ✅。理由：R0 的 **P2 阻塞项 F-1 真闭合**（三件套齐备、差额 97/92/84/13/8 我逐项独立复现且逐条列名吻合、算术自洽）；**F-1b 使 97/92 永久可核**并把残余不稳定性转为**已登记的引用纪律**；F-2/F-3/F-4/F-5 **四项 P3 全部按建议处置且与事实一致**（含我方两处自我更正）；**零新引入 P0~P2**；越权面零、断言集合零增删、`ANCHOR_CASES` 25 不变、守卫 **174 断言 PASSED**、门控 **exit 0**、镜像 SHA256 恒等、零宿主树操作、锁面一致。⇒ **通过终态，复审链结束（无需 R2；`round = 1`，未触及 T2 熔断）**。
- **push 时必须遵守的口径（不得越界声称）**：
  1. **不得声称「CI 已验证」**：ubuntu 侧**未实测**；`#SKIP 6 (host-contract.mjs×2, smoke.mjs×4)` 仍为**推算值**（本机 win32 实测 `#SKIP 2`）。本批判据为纯文本 `includes` + 注释/登记面（平台中立度最高），**但仍以首跑日志为准**。
  2. **push 后回填**：ubuntu 首跑结论（`#SKIP 6` 逐行 + `9h / 9h-2 / 9h-2b / 9h-2c / 9h-2d / 9h-3` 全绿）入 evidence-log。
  3. **引用数字时必须带修订**（本批自设纪律）：任何对 97/92/94 的引用 MUST 标注取数修订与复跑命令。

---

## 四、新发现（本轮）

### P0 / P1 / P2 —— 0 条
无。

### P3（不阻塞；登记/可选修复）—— 3 条

**P3-1（R1）｜F-3 的登记文本把 `dsh-client-modules lib/client.js:265-268` 写进守卫，使其成为**新引入的自碰撞死 needle**（自我引用的清洗过程本身产生了新的自我引用）**
- **位置**：`tests/host-abi-health.mjs:822`（条目 `{ file: ['lib','host-abi','inject-manifest.js'], stale: [':5060 先例', 'lib/client.js:5060', 'dsh-client-modules lib/client.js:265-268'], … }`）与 `:809-814`（F-3 的「隔行站点登记」注释块——**该块含 `lib/client.js:4281` / `lib/service.js:1326` / `lib/wrapper.js:266` 等行号形态文本**）。
- **事实依据**（我逐一实算）：`occurrences('dsh-client-modules lib/client.js:265-268')` —— 在**守卫自身 = 1**（即该 needle 的登记处）、在**目标文件 `lib/host-abi/inject-manifest.js` = 0**；对比基线：该 needle 在 `6bc3841` 的守卫内 **= 0** ⇒ **该碰撞由本批（F-3 登记）新引入**。后果：该 stale 项**恒绿且不可见**（目标文件已清、needle 自身文本存在于判据文件内 ⇒ 与既有 `:742` 的「反引号不成对 needle」**完全同类**的无效项；我 R0 曾判 `:742` 为「FIX-040 遗留、不构成问题」，本项为**同机制的新增实例**）。
- **影响**：**零行为影响**（守卫 174 断言仍全绿、判据谓词未改、`check` 的 `staleHits` 面仍由其余 needle 承担）；但守卫的**登记纪律自相矛盾**——同批为**守卫自身条目**专门用**拼接常量**规避「X&&X/清单文本自碰撞」（`:790-793` 明文解释），却在 F-3 的备忘文本里**逐字写入被清锚串**，使该条的 stale 侧退化为不可见项。属「文档/登记一致性」类，非判据退化（无条目覆盖丢失：目标文件已清的事实由 fresh 侧与 `const inject =` 在位共同承担）。
- **建议（~1 行，可选）**：把 F-3 登记文本中的该串改为**分段/归属式**（如「`dsh-client-modules` 的包表行（原 `:265-268` 已清）」——去掉可被判据字面命中的完整串），或对 `:822` 的该 needle 采用**与 `OLD_PRESETDIAG_LINE_ANCHOR` 同法的拼接常量**；同时建议**把「stale needle 在守卫自身内的出现次数 = 0」纳入机核**（现有 `fix042-r1-selfcheck.mjs` 未覆盖该项 ⇒ 属可扩展的守卫卫生项）。

**P3-2（R1）｜守卫注释仍写「`lib/client.js` 内 **9 处 `lib/client.js:<NNN>` 自锚**」，与表/实测的「9 处皆宿主包锚」口径不一致（历史口径残留）**
- **位置**：`tests/host-abi-health.mjs:819`（`//   清单**：对象不在本仓库面，仓库级解析宿主树（基线登记与人工复检义务见上方注释）。` 的前一行 `:819` 即「宿主锚（`dsh-*` / 宿主 上下文，含 lib/client.js 内 9 处 `lib/client.js:<NNN>`）**不入本清单**」）。
- **事实依据**：R0 报告 #18 与 FIX-041 R1 §二「口径更正」均实测：`lib/client.js` 内 9 处 `lib/client.js:<NNN>` 锚的**邻域包名**依次为 `dsh-client-connection` / `dsh-api-remotes` / `dsh-client-ui-settings-models` / `dsh-cordis-client-runner` ⇒ **全部为宿主包锚**（包名写在前一行）；证据表 `.test-home/fix042-host-family.md` 的归属列亦记为 `(相邻行) dsh-*`（表 `#4/#9/#10/#87/#92/#93` 等）。
- **影响**：**零功能影响**（仅注释口径）；但该句是后续批次理解「哪些锚不入清单」的依据，若按「自锚」理解会误以为存在 9 处**同文件自指**（实际为跨文件宿主锚）⇒ 与 R0 F-4 同类的**措辞与事实偏差**（本批为**未修改的历史文本**，非本批引入）。
- **建议（可选，~1 行）**：改为「含 `lib/client.js` 内 9 处**指向宿主包的同名文件锚**（`lib/client.js:<NNN>`，包名在上一行 ⇒ 属宿主锚）」。

**P3-3（R1）｜「当前时点数字」无处固化：证据表登记 `94/19` 亦会在下一次文本增删后失效**
- **位置**：`.test-home/fix042-host-family.md` 表头差额说明（「R0 返工笔后实测 94/19」）与守卫 `:798-799`；差额工具输入 `fix042-base.txt` / `fix042-head.txt` 为**时点快照**（`head.txt` = `61af16a` 时点 92）。
- **事实依据**：我多修订复跑得 `61af16a` = **92**、`14156b8`/`866f688`/`8aed89c`/`9b262ef` = **94**（H1 55 / H2 39）⇒ 「94」只对 `14156b8..9b262ef` 区间成立；`head.txt` 亦为 `61af16a` 的快照（其行面与 `61af16a` 复跑逐条吻合），故 `inventory-diff` 在**更晚时点**复跑会出现「HEAD 有 / head.txt 无」的额外差项。
- **影响**：**零行为影响**；属「快照工具 + 时点数字」的固有边界——本批已用**引用纪律**处置（#7），但**未在工具头/表头标注快照的时点**（`fix042-inventory-diff.mjs` 头写「批前树（6bc3841）与 HEAD 的宿主锚清单差集」，未写 `head.txt` 的取数时点）。
- **建议（可选，~1 行）**：在 `fix042-inventory-diff.mjs` 头与表头补「**快照时点**：`base.txt` @`6bc3841` / `head.txt` @`61af16a`；两者均需按引用时点重生成」。

### 未重现历史问题（明确结论）

- **幻觉引用（FIX-040 R0 P1-1 同型）**：**未重现**（#10/#11/#12：链式三段宿主实存、新锚在位、25 条目 FAIL=0）。
- **判据强度退步（FIX-040 R1 P1-1(new) 同型）**：**未重现**（断言集合零增删、谓词零改动、三条新增/变更项均双向实算可达）。
- **溯源不实（FIX-041 R0 F-1 同型）**：**未重现**（本批新增引用 `FIX-042 R0 F-1/F-1b/F-2/F-3/F-4/F-5`、`FIX-041 R1 §三.3`、`FIX-040 R2` 等全部指向实存轮次；我方两处判断被更正后亦**以「原句」引述形式**保留在守卫内，未掩盖历史）。
- **过度断言 / 不可复现计数（R0 F-1 同型）**：**未重现**（三件套 + 引用纪律；#5/#6/#7）。
- **计数不实（R0 F-2 同型）**：**未重现**（#8/#9：回填逐笔吻合）。

---

## 五、硬门槛裁决

| 门槛项 | 阈值 | 实测 | 裁决 |
|--------|------|------|------|
| P0 阻塞问题数 | = 0 | 0（§四 P0 无） | **PASS** |
| 5 维度全覆盖 | = 100% | 见下逐维度结论（5/5） | **PASS** |
| 每条发现标注级别 | = 100% | P3×3 均带 文件:行号 + 事实依据 + 影响 + 建议；R0 findings 5 条逐条带处置与证据；我方更正 2 处显式标注 | **PASS** |
| 设计一致性检查 | 已完成 | 与项目原则 1（事实性——**已恢复**：数字绑修订 + 逐笔回填 + 我方更正如实标注）、原则 4（防护网——F-3 扩既有条目、断言零删除、镜像恒等）、原则 5（单一路径——扩条目不拆双条目、无并存双路径）、原则 8（失败可观测——stale/fresh 双绿带 detail）、原则 9（宿主演进防御——宿主锚基线登记 + 人工复检义务 + **不声称机器覆盖**，F-3 链式为正向改进）、`ci.yml` 契约（未触）逐项比对：**无偏离** | **PASS** |
| AI 代码专项 5 项 | 全部完成 | ① **mock 残留**：无（零夹具新增）② **硬编码**：无（无硬编码通过；新增串均为判据输入/登记文本）③ **幻觉引用**：**无**（#10/#11）④ **未实现 TODO**：无（`+` 行零命中）⑤ **过度实现**：无（5 笔 = 注释改写 + 2 行清单数据 + 登记块） | **PASS** |

### 五维度逐项结论

- **正确性 —— 通过**：F-1 三件套的**全部数字由我在 7 个修订上独立复现**（97/92/94、交集 84、差集 13/8 逐条吻合、算术自洽）；F-3 三段宿主链**宿主树逐段实存**、`ANCHOR_CASES` 25 条实算 FAIL=0；F-4 双口径严格 0 可复现；F-5 栈法 106/106、零未闭合/零孤立；守卫 174 断言 + 门控 exit 0；静态标签集与 R0 逐元素相等。
- **安全性 —— 通过**：3 文件全部为注释/清单数据改动（非注释改动 2 行且为判据数据）；无输入面/密钥/注入引入；本审查全部变异为**内存仿真**，对工作树与宿主树零写。
- **可维护性 —— 通过（附 P3-1/2/3）**：F-1 把「不可迁移的数字」改造为「绑定修订 + 复跑命令 + 差额 + 引用纪律」的四件套，是本轮最有价值的正向改进；F-4 的口径限定与「原句」引述样式为后续自指类提供了模板；F-5 的真位点改正避免「数值配平但结构错配」的假修复。负向 = 登记文本引入死 needle（P3-1）、历史口径残留（P3-2）、快照时点未标注（P3-3）。
- **性能 —— 通过**：零运行时判据变更（条目不增、谓词不改）；守卫与门控耗时与 R0 同量级（22.5s vs 21.8s）。
- **测试覆盖 —— 通过**：静态断言集合**零增删**（REMOVED=0/ADDED=0）而**覆盖面净增 1 项**（`lib/wrapper.js` 条目新增 stale+fresh 各 1 串、双向可实算）；条目 25 不变；剩余覆盖缺口（宿主锚族无机器覆盖、隔行同类站点、死 needle 机核缺失）**已全部显式登记**，未被声称已覆盖。

### 审查结论

> **APPROVED_WITH_NOTES**
> **`unresolved_blockers=0`**
>
> 理由：R0 的唯一阻塞项 **F-1 真闭合且经我独立复现**——守卫注释与证据表双双落地**三件套**（数字 + 取数修订 + 复跑命令 + 差额说明），我在**7 个修订**上以同一脚本复跑逐值复得 **`6bc3841` = 97（H1 57 / H2 40 / 22 文件）、`61af16a` = 92（H1 54 / H2 38 / 19 文件）、交集 = 84、差集 = 13 / 8（逐条列名与声明位点逐字吻合）**，算术 `97 − 13 + 8 = 92` 自洽；**F-1b 的修订绑定时态口径有效**——97/92 绑定不可变修订后永久可核，残余的「当前时点数字漂移」经我实测（92→94）确认存在但已由**引用纪律**显式登记（守卫 `:798-799` + 表「纪律」行）⇒ 处置充分。**F-2/F-3/F-4/F-5 四项 P3 全部按建议处置且与事实一致**：计数回填与 `--numstat`/`--shortstat` 逐笔吻合（W1 `+20/−19`/8 文件、W4 `+14/−4`/4 文件、合计 15 文件）；F-3 符号链式三段在**宿主树逐段实存**（`:2043/:2046/:2047`）且**零新幻觉引用**、隔行站点表登记如实、不改理由成立；F-4 双口径经我**独立复现**（严格 **0** / 宽 **89**）且「声明强度超过事实」的自我更正到位；F-5 经**栈法**确认真位点为 **`:312`**（`:89` 由 `:91` 结构性闭合）——**Developer 对我 R0 该项的更正成立**，另**Developer 对我 R0 的 `d58c5ca` 归属更正亦成立**：**我 R0 的两处判断（`:89` 未闭合位点、W3 = 6 文件 / `client-render.mjs` 归属）均已在 §二「我方更正」如实修正**。5 笔全为注释/登记面（非注释改动 2 行、谓词零改动）、越权面零、断言集合**零增删**（169→169）、条目 **25** 不变、守卫 **174 断言 PASSED**、门控 **exit 0**、镜像 **SHA256 恒等**、**零宿主树操作**、锁面范围一致。余下 **P3×3**（F-3 登记文本新引入一个与既有 `:742` 同类的**自碰撞死 needle**；守卫注释仍写「9 处自锚」而实测为宿主包锚；差额工具/表的**快照时点未标注**）**均为文档/登记一致性项，零判据损失、零行为影响、不阻塞**。⇒ **本结论为通过终态，复审链结束（无需 R2）；`round = 1`，未触及 T2 熔断。**

---

## 六、未验证项声明（事实依据红线）

1. **ubuntu / CI 未实测**：本机为 win32；`#SKIP 2` 为本机实测，`#SKIP 6 (host-contract.mjs×2, smoke.mjs×4)` 仍为**推算值**，**不得读作已验证**；**任何对外表述不得声称「CI 已验证」**。
2. **Developer 的取证脚本**：我**只读执行**了 `.test-home/fix042-inventory-diff.mjs`（差额工具）、`.test-home/fix042-w2-selfref.mjs`（双口径）、`.test-home/fix042-r1-selfcheck.mjs`（R1 自检，exit 0）三项，并**未采信其结论**——三项的关键数值我**均已独立复算一致**（差额 97/92/84/13/8；严格口径 0；条目 25 / 标签 169→169 / 镜像恒等 / 越权空）；其余取证件（`fix042-base.txt` / `fix042-head.txt` / `fix042-w1-verify.mjs` / `fix042-w3-verify.mjs` / `fix042-w4-verify.mjs` / `fix042-r0-paren*.mjs` / `fix042-r1-diagnose.mjs` 等）**未执行**、其输出未采信（`base.txt`/`head.txt` 仅作工具输入被间接核对）。
3. **宿主侧结论的范围限定**：F-3 的宿主三段链结论**仅对本机实装 `@deepseek-ai/dsh-llm@0.1.5-rc.2`** 成立（我 `Get-Content`/`Select-String` 只读），**不构成**对表内其余 97 处宿主锚的普适保证；本批**零宿主树操作**（#18），我本轮的宿主读取为**审查方只读复核**，未写任何路径。
4. **P3-1 的「零行为影响」判定**：基于「该 needle 恒绿且无其他条目依赖它」的静态推演 + 25 条目实算 FAIL=0 + 守卫 174 断言运行全绿；我**未**做「删除该 needle 后套件仍绿」的差分运行（无必要：其恒绿已由 `occurrences(target)=0` 与 `occurrences(guard)=1` 直接确定）。
5. **`ANCHOR_CASES` 条目/断言计数的时点性**：25 条目与 174 断言为**受审 HEAD（`9b262ef`）**的实测值；若后续增删条目该数字自动变化（**不得作长期基线**，须以运行输出为准）——本点即 R0 F-1 的直接教训，已在守卫内登记。
6. **本审查的写入面**：仅 `.governance/review-FIX-042-R1-input.md`（本报告）+ `.test-home/fix042-r1-{paren,cases,selfrefcount,counts,f5verify,guardparen}.mjs`（`.gitignore:10` 覆盖；均为只读核验脚本）。**未修改任何被跟踪文件**；`git status --porcelain -- lib tests` 复核为空；未建/未留任何 worktree。

---

## 七、建议 Coordinator 动作（按依赖排序）

1. **判定通过（本轮）**：结论 = **APPROVED_WITH_NOTES / `unresolved_blockers=0`** ⇒ 以 review-record CLI 机录本报告（canonical 名 `review-FIX-042-R1.md`）；**复审链结束，无需再 spawn Reviewer**。
2. **push（可执行）**：本批 **9 笔**具备 push 条件（`origin/main` = `6bc3841`，领先 9 笔）；push 后**立即以首跑日志核验** ubuntu 侧 `#SKIP 6` 逐行 + `9h / 9h-2 / 9h-2b / 9h-2c / 9h-2d / 9h-3` 全绿并入 evidence-log；**首跑前任何表述不得声称「CI 已验证」**；**引用 97/92/94 等数字时 MUST 带取数修订与复跑命令**（本批自设纪律）。
3. **台账补录（不阻塞 push）**：① P3-1（F-3 登记文本引入的自碰撞死 needle + 建议把「stale needle 在守卫自身内出现次数 = 0」纳入机核）② P3-2（守卫 `:819` 的「9 处自锚」措辞与表口径不一致）③ P3-3（差额工具/表的快照取数时点标注）④ R0 遗留台账项继续保留（FIX-040 链的 P2-2 归因 / P2-3 中间态口径 / `.test-home` 取证件归档 / `CHANGELOG:59` / `.cmd` ComSpec / README 引用位置枚举）。
4. **后续全量改写分批方案**：**维持** R0 裁定的批 A/B/C/D（我的复算与「批 A `host-contract.mjs` 19 处」仍逐字吻合），并**沿用本轮已生效的两条约束**——①范围以「逐处语义判定后的清单」为准（`lib/client.js:4281` / `lib/service.js:1326` 等隔行站点已登记待收敛）②每批产出数字 MUST 绑取数修订 + 口径 + 复跑命令。
5. **发布面提示**：本批 9 笔全为注释 + 测试基础设施面（`lib/**` 仅注释改写，零产品逻辑改动）⇒ **不产生用户可见行为变化**；`package.json` / 版本位（0.5.0）/ `CHANGELOG.md` 未被触碰。
6. **方法学建议（供后续批次）**：①「数字 → 修订 → 口径 → 复跑命令」四件套宜作为**任何清点类产出**的模板（本批已示范）②凡在守卫文本内**逐字复述**被清锚串，宜同步采用拼接常量化或分段化（P3-1 教训），并建议把该规则纳入机核，避免「清洗自我引用时引入新自我引用」。
