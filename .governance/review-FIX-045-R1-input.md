# Code Review 报告 — FIX-045 **R1（复审）**

- **round**：**1**（复审轮；同一 Reviewer）
- **前轮引用**：`.governance/review-FIX-045-R0-input.md`（机录 `REVIEW-FIX-045-R0` = APPROVED_WITH_NOTES / `unresolved_blockers=0`；P0=0 / P1×1 / P2×2 / P3×3）
- **审查对象**：返工 commit **`cd1441a`**（`tests/host-abi-health.mjs`，声称 +41/−20，单文件）；对照前轮交付 `3e9e8a5`
- **审查依据**：任务下发的返工要点 + 工作树实读（现行 `:1466-1572`）+ 仓内治理记录交叉核验
- **本轮结论**：**APPROVED_WITH_NOTES**，`unresolved_blockers=0`（P0=0 / P1=0 / P2=0 / P3=4）
- **方法边界**：只读（Read/Grep/Glob），本轮**未执行任何命令**（严格遵守角色边界）。Coordinator 提供的机器事实（`host-abi-health` 190/exit 0、`run-all` exit 0、`U-1 .source 恒等 = true`）按指令**采信为 Coordinator 独立复跑结论**，引用处均标注来源；凡我未复现者列入 §7。

## 1. 前轮 findings 逐条比对（硬性要求 1）

| 前轮编号 | 级别 | 本轮状态 | 判定依据（可复查事实） |
|---|---|---|---|
| **F-1** 注释「匹配能力等价 / 同集 / 零行为漂移 / 严格更严 / 不降低判据强度」与事实不符 | P1 | **已修复** | ① 关键词实测：`同集` / `零行为漂移` / `严格更严` / `不降低判据强度` 全文件 **0 命中**；`等价` 仅 `:563`/`:570`（与本任务无关的既有原文）；`:1498` 出现的「判据强度」位于**承认前版错误**的勘正句内（"本节首版曾把…定性为…两项均与事实不符——已整体改写"）属如实陈述，非残留声称。② 新口径与事实一致：`:1499-1501` 明确旧组 `(js\|mjs\|ts\|tsx\|sh\|ps1)` / 新组 `js\|mjs\|cjs`、**去 4 族 / 增 `cjs`**、`cjs` 为真实补口；`:1494-1497` 明确「枚举才是面定义、正则 MUST NOT 宽于/异于枚举」；`:1507-1513` 如实声明**前瞻敏感性收缩 + 已知盲区**，并列出内容级实例（我逐条复核全部存在：`lib/service.js:539`/`:3286`、`tests/smoke.mjs:1074`、`tests/install-entry.mjs:38`、`.github/workflows/ci.yml:42`、守卫自身 `:753`/`:1131-1132`/`:1294`）。③ 配套的验收口径修正已由 Coordinator 自纠入册（`plan-tracker.md:49`：「本任务验收标准①原写『不降低既有判据强度』属我方措辞错误…审查员按字面判定不成立属正确判定」）⇒ R0 的字面不成立项闭环。 |
| **F-2** `endsWith` 弱于声明意图，前插分支可静默通过 | P2 | **已修复** | `:1536` 已改为 `ANCHOR_COVERAGE_FORM_1_SOURCE` 的**逐字恒等**（`===`）。静态推演：前插形态（`'<扩展名组>:\\d+(-\\d+)?\|' + 派生串`）下 `.source` ≠ 派生串 ⇒ 合取项 2 假 ⇒ **判红**（R0 反例已消除）；`endsWith` 的尾部-only 语义不再存在，且 `===` 额外覆盖「同组但非扩展名的文本改写」。与 R1-3 证据自洽：新实现 `1 FAILURE(S) (189 passed)` ⇒ 190 断言总数（未增删判据）中恰 1 失败 = 9h-5c ✓ |
| **F-3** 标签「枚举增项即红 / 写回字面量即红」不实 | P2 | **已修复** | `:1528-1532` 与标签 `:1534` 已按实际行为改写：正则侧**单边**改动（组内扩展名增减 / 写回不同文本 / 前插后附分支）⇒ 红；枚举侧改动经派生**自动同步 ⇒ 不红**；**源串逐字相同**的字面量回写亦不红（后续枚举变更才红）。与 R1-4 五个变异形态逐一静态一致：枚举增 `'.tsx'` ⇒ 绿（派生同步；面内 `.tsx` 文件 0、`.tsx:` 锚串 0）；前插分支 / 写回不同文本 / 组内缩减 ⇒ 红；逐字相同回写 ⇒ 绿 ✓ |
| **F-4** 枚举项形状无判据（`[]` / 正则元字符仍绿） | P3 | **未修复（已登记）** | 代码面确认未变（`:1515` 项无形状校验；`:1534-1537` 未覆盖）；登记依据：`plan-tracker.md:49`「**F-4/F-5/F-6（P3）入台账不随轮**」✓ |
| **F-5** `FORMS[0].source.match(...)` 非 RegExp 时崩栈 | P3 | **未修复（已登记）** | `:1533` 仍为 `ANCHOR_COVERAGE_FORMS[0].source.match(...)`（无 `?.` / `String()` 兜底）；同 F-4 登记 ✓ |
| **F-6** 文件头覆盖面清单未含 9h-5 族 | P3 | **未修复（已登记）** | 全文件 `9h-5` 首现于 `:1454`（块内），头部（<`:1454`）仍止于 9h-4c ✓；同登记 ✓ |
| **新引入** | — | **1 项（N-1，P3）** | 见 §3：`:1477` 概述句缺时态限定的措辞精度问题；除此之外未发现新问题（无回归，见 §2.3/§4） |

## 2. Coordinator 指定核验项：「承载文件 20 → 17（3 个在册文件失去锚承载）」是否属实

### 2.1 面规模 = 51 ✅ 独立复算
`glob lib/` 的 `.js/.mjs/.cjs` = **23**；`glob tests/` 的 `.js/.mjs/.cjs` = **26**；+ `README.md` + `.github/workflows/ci.yml`（该目录下唯一 yml）= **51** ✓（与 FIX-044 R0 独立记录的 `scanFaceSize=51` 同值）

### 2.2 失去承载集合 = 恰 {`tests/smoke.mjs`, `tests/install-entry.mjs`, `.github/workflows/ci.yml`} ✅
| 文件 | 旧形态①（被去扩展名）命中 | 新形态（①新/②/③）命中 | 判定 |
|---|---|---|---|
| `tests/smoke.mjs` | `:1074` `transport.ts:22` | **0** 命中（新①/②/③ 定向全文件检索） | **失去承载** ✓ |
| `tests/install-entry.mjs` | `:38` `install.ps1:1`、`install.sh:2` | **0** 命中 | **失去承载** ✓ |
| `.github/workflows/ci.yml` | `:42` `install.ps1:1` | **0** 命中（含形态③ `[^A-Za-z0-9_]:\d{2,4}`） | **失去承载** ✓ |
| `lib/service.js` | `:539`/`:3286` `transport.ts:22` | 仍命中：`:1325` `lib/index.js:1010-1054`、`:498` `L719-722`、`:2530` `L270` | 仍在承载（不计入） ✓ |
| `tests/host-abi-health.mjs` | `:753`/`:1131-1132`/`:1294` `.d.ts:NNN` | 仍命中：形态② 25 处（`:759-775` 等） | 仍在承载（不计入） ✓ |
| `README.md` | 0 | 0 | 两版均不承载（不计入） ✓（该文件检索已用正对照验证：`install`/`常见问题` 10 命中，排除「工具静默跳过该文件」的假阴性） |

- **完备性论证**：失去承载只可能发生在「仅命中被去扩展名（`.ts/.tsx/.sh/.ps1`）形态①」的文件上；全 face 面内该类锚串的**承载文件全集** = 上表前五行（`lib/`、`tests/` 逐目录检索 + README + workflows 定向检索）⇒ 其中仅 3 个在收敛后不再命中任何形态 ⇒ **失去集合恰为 3** ✓
- **无增益** ✓：`\.cjs:\d` 在 `lib/`、`tests/`、README、ci.yml **0 命中** ⇒ `bearingGained=[]` 成立
- **三者均在册** ✓：`ANCHOR_CASES` 条目 `:867`（smoke.mjs）/`:869`（install-entry.mjs）/`:872`（ci.yml）⇒ 失去承载**只会更难判红**，`uncoveredFiles` 不受影响 ✓
- **三豁免两版判定一致** ✓：`tests/oauth-promotion.mjs:42`（形态② `L116`）、`tests/oauth-credentials.mjs:345-346`（形态③ `:145-174`/`:175-227`）、`tests/served-client.js:4979-5016`（形态② `L5682-5760` 等）⇒ `deadExempts` 两支不受影响 ✓
- **裁定核验结论**：Developer 提出的更精确口径**属实**，且属 F-1「如实声明差集与代价」的应有内容（**在 F-1 范围内，非 scope 扩张**）✓。残留未核项：绝对值 **20/17** 未由我逐文件重算（见 §7 U-3）；但 20 与 FIX-044 R0 独立记录的旧基线 `anchorFileCount=20` 同值，且 17 = 20 − 3 与已核 delta 自洽。

### 2.3 回归面复核（无新增红/无误红）
| 项 | 结论 | 依据 |
|---|---|---|
| 9h-4/4b/4c 卫生 | ✅ 不破 | `stale: [` 标记总数仍 **35**（与 FIX-044 R0 独立记录 `item-check=35` 一致）；新增文本内 0 处；`:1508-1510` 提到的 `transport.ts:22`/`install.ps1:1`/`install.sh:2`/`.d.ts:NNN` 均**非**已登记 needle 字符串（needle 为 `directory.d.ts:52`、`install.sh:94-101` 等不同串）⇒ 无自碰撞 |
| 9h-5/9h-5b 本体 | ✅ 未动 | `:1563-1571` 逐行与 R0 一致（仅行号位移） |
| 断言计数 | ✅ 190 不变 | 块内 `check(` 仍**恰 1 条**（`:1534`）⇒ 无增删；Coordinator 复跑 `ALL HOST ABI HEALTH TESTS PASSED (190 assertions)` / exit 0（采信） |
| 常态判绿前提 | ✅ 成立 | U-1（`.source` 逐字恒等）由 Coordinator 复算 = **true**（采信）；`===` 收紧因此无假红 |

## 3. 新发现（R1）

**N-1｜P3｜`:1477` 概述句缺时态限定（纯措辞精度，不阻塞）**
- 事实依据：`:1477` 写「P3-2 形态①的扩展名集与扫描面枚举集**互不相等**（**双向差集**…）⇒ 已按**单一常量来源**收敛」；`⇒ 已…收敛` 表明所述为**改前态**，但句内无「改前/此前」限定；收敛后两者**恒等**（`:1535` 断言组恒等、`:1536` 断言源串恒等，块内 `:1490` 已用「此前」正确限定）。
- 影响：与同文件 9h-5c 标签（`===` 恒等）字面并置时，可能被读作当前态自相矛盾；无行为影响。
- 建议：`:1477` 补「改前」二字（如「P3-2 改前形态①的扩展名集与枚举集互不相等（双向差集…）」）；随下次触碰该文件时一并处理即可。

## 4. 五维度逐项结论（R1）

| 维度 | R0 | R1 | 依据 |
|---|---|---|---|
| 正确性 | ⚠️（F-1/F-2） | ✅ **通过** | 派生链 `:1514-1521` 语义正确；判据 `:1533-1537` 为「组恒等 × 源串恒等」双条件，恒等语义与声明意图一致（F-2 闭合）；`deadExempts/overlappingExempts/uncoveredFiles` 三支未变（`:1563-1567`）；面收缩不产生判红差异（§2.2 已核） |
| 安全性 | ✅ | ✅ 通过 | 无外部输入（正则源为同文件常量）、无 eval/shell/网络/写盘、无密钥；面枚举项形状未校验属健壮性问题（F-4，已登记） |
| 可维护性 | ⚠️（F-1/F-3） | ✅ **通过**（残留 N-1 措辞） | 注释已按「方向 / 差集 / 当前树无判红差异 / 代价与盲区」四段重构，事实性经逐条复核；扣分为 N-1（一句时态） |
| 性能 | ✅ | ✅ 通过 | 增量仍为 O(1)（1 次常量 `new RegExp` + 1 次 `match` + 1 次 `===`）；无新循环/IO |
| 测试覆盖 | ✅ | ✅ **通过** | 判据自身即测试；R1 双向证据齐备（前插形态：旧实现绿 / 新实现红——静默通过形态被实测复现并消灭；枚举增项仍绿=不过度约束）；范围外遗留 F-4/F-5（登记） |

## 5. 硬门槛裁决（R1）

| 门槛项 | 阈值 | 裁决 |
|---|---|---|
| P0 阻塞问题数 | = 0 | ✅ **0**（R1 无 P0；R0 亦无） |
| 5 维度全覆盖 | = 100% | ✅ 5/5（§4） |
| 每条发现标注级别 | = 100% | ✅ 7/7（F-1 已修复 / F-2 已修复 / F-3 已修复 / F-4 未修复-已登记 / F-5 同 / F-6 同 / N-1 新 P3） |
| 设计一致性检查 | 已完成 | ✅ 与 9h-5/9h-5b/9h-4* 族同型；单实现路径（全仓 `ANCHOR_COVERAGE_*` 仅此文件）；fail-closed + 结构化载荷（`form1ExtGroup`/`form1Source`）保持；修改纯粹性：单文件、测试侧、无产品代码、无夹带 |
| AI 代码专项 5 项 | 全部完成 | ✅ 5/5（§6） |
| 复审链要求 | 逐条比对前轮 + 头部 round 声明 | ✅ §1 + 本报告头部；round 1 < 3，未触复审 fuse |

## 6. AI 代码专项 5 项（R1）

| 专项 | 结论 | 依据 |
|---|---|---|
| mock 残留 | ✅ 无 | 本轮为**单文件注释/一处判定收紧**，无探针文件；仓内未见 `*.bak/*.orig/*.rej/mutant` 与守卫副本（R0 已核，且本轮唯一改动面 = `:1476-1537`） |
| 硬编码返回值 | ✅ 无 | 判据比较运行时派生值（`:1535`）+ 运行时源串恒等（`:1536`），非常量满足 |
| 幻觉 API | ✅ 无 | 无新增 API 调用（仅 `===` / `match` / `String#endsWith` 移除后更简）；`:1533` 的解析式未能命中时为 `undefined` ⇒ 判红（fail-closed） |
| 未实现 TODO | ✅ 无 | `:1476-1537` 逐行读取，无 TODO/FIXME/占位 |
| 过度实现 | ✅ 无 | 改动净增为注释事实化；代码面净变化为 1 个运算符（`endsWith` → `===`，同时增强与简化） |

## 7. 未验证 / 待验证项（R1）

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| U-1 | `.source` 逐字恒等 | ✅ **已闭环（非我复现）** | Coordinator 复算 = `true`；本轮我未执行命令（采信来源已标注） |
| U-2 | 全量门控（190 / exit 0；`run-all` 20 套件 / `#SKIP 2` / exit 0） | ⚠️ 采信 Coordinator 复跑 | 结构性一致：本轮未增删判据 ⇒ 断言数不变；未触及 skip 路径 |
| U-3 | 承载绝对值 **20 → 17** | ⚠️ 部分核验 | **delta（3 个文件、方向、完备性、无增益、在册、豁免一致）已独立核验**；绝对值未逐文件重算（与 FIX-044 R0 独立记录 `20` 同值且 `17 = 20 − 3` 自洽） |
| U-4 | 变异实跑（R1-3 双向 / R1-4 五形态） | ⚠️ 采信 + 静态推演一致 | 各形态的判定结果由代码级推演逐一如实复现；实跑输出无仓内留痕（「零探针残留」纪律的必然代价） |
| U-5 | commit 元数据（`cd1441a`，+41/−20）与 `.governance/**` 未被 Developer 改动 | ⚠️ 未独立复核（未执行 git） | 静态观察：工作树改动面与声称一致（注释重构 + `===`）；`evidence-log.md`/`plan-tracker.md` 中 FIX-045 相关行均为 Coordinator 机录条目（`TRIAGE-FIX-045`、`REVIEW-FIX-045-R0`），未见 Developer 署名改动 |

## 8. 结论

**APPROVED_WITH_NOTES** — `unresolved_blockers=0`

```
{"verdict":"APPROVED_WITH_NOTES","unresolved_blockers":0,"round":1,
 "prev_round":"review-FIX-045-R0-input.md",
 "p0":0,"p1":0,"p2":0,"p3":4,
 "findings":{"F-1":"fixed","F-2":"fixed","F-3":"fixed",
             "F-4":"open-registered","F-5":"open-registered","F-6":"open-registered",
             "N-1":"new-P3"}}
```

- **闭合判定**：R0 的 P1×1 + P2×2 **全部已修复**，逐条有可复查事实（§1）；R0 的 P3×3 按 Coordinator R0 裁定「入台账不随轮」**已登记**（`plan-tracker.md:49`）⇒ 不构成本轮阻塞；本轮新增仅 N-1（P3 措辞）。
- **Coordinator 指定核验项**：Developer 提出的「承载 20 → 17、三个在册文件失去承载」**口径属实**（§2：面 51 复算一致、失去集合恰 3 且完备性有论证、三者均在册、三豁免两版一致、无增益）⇒ 该精确口径属 F-1「如实声明」的应有内容，**在范围内**，非 scope 扩张。
- **硬门槛**：全部通过（P0=0、5 维度 100%、发现级别 100%、设计一致性已完成、AI 专项 5/5）；round 1 < 3，未触复审 fuse。
- **残留（非阻塞）**：F-4/F-5/F-6（P3，已登记）+ N-1（P3 措辞，建议下次触碰该文件时一并补「改前」二字）。若 Coordinator 采用「无 P0/P1/P2 即 APPROVED」的严格读数，本轮同样满足 APPROVED 的硬门槛；本报告取 AWN 仅为携带上述 P3 备注。
- 说明：本结论为**代码审查通过终态**，不替代运行时门控复核（U-2，采信 Coordinator 复跑）与 commit 面复核（U-5，未执行 git）。
