# 提交规范（Commit Guidelines）

本项目的版本管理规则：**一条提交只落一个逻辑变更，测试通过后立即提交**。

## 提交粒度

1. 一条提交只包含一个逻辑变更（一个关注点）：一个功能、一个修复、一次文档或重构。
2. 变更完成且 `node tests\smoke.mjs` 全部通过后，**立即提交**；不要把多个不同关注点的变更攒在一起合并提交。
3. 跨切面的功能（schema + 宿主 + 客户端 + wire codec + 测试相互依赖，中间态无法通过测试）可以作为**一个原子提交**整体落地；但功能完成后必须当时提交，**不得顺带捆绑无关修复**（如沙箱参数、重试策略这类独立修复应单独一条 `fix:`）。
4. 一次会话中完成多个逻辑变更时，逐个提交；每个提交都应是可独立回退、测试可复现的完整状态。

## 提交信息格式

- 前缀：`feat` / `fix` / `chore` / `docs` / `refactor` / `test`。
- 第一行：`前缀: 一句话概述——关键点`（中文，参考现有历史）。
- 正文：每行一条 `- 变更点`，说明动机与影响范围。

## 提交前检查清单

- [ ] `node tests\smoke.mjs` 全绿。
- [ ] 修改 `lib/client.js` 后已同步渲染测试镜像：`Copy-Item lib\client.js tests\served-client.js -Force`。
- [ ] 临时探针（`tests/gateway-probe.mjs`、`tests/bundle-check*.mjs` 等一次性调试脚本）用完即删，不入库。
- [ ] 未混入工作区产物（`.router-files/`、`*.tar.gz`、`.test-home/`、`*.log`，已在 `.gitignore` 内）。

## 高风险修改纪律（影响用户环境 / 数据）

安装脚本、配置写入、路径与链接操作、删除/覆盖类操作等**可能影响用户环境或用户数据**的修改，一律视为高风险修改，遵守以下纪律：

1. **场景枚举先行**：动手前列出全部受影响场景——在线/离线、各平台（Windows PowerShell 5.1/7、macOS/Linux sh）、首次安装/重复安装/失败回退、目标路径已存在（链接/真实目录/悬空链接）等；任何未覆盖场景不得开工。
2. **隔离环境全场景验证**：所有验证使用临时 DSH_HOME 与源码副本，**绝不触碰真实 `~/.dsh` 或真实源码目录**；每类场景都要有实测结果记录（含负向场景的预期失败）。
3. **负向见证测试**：修复必须带"旧行为会失败、新行为会成功"的回归测试（如 `tests/install-entry.mjs` 的 BOM 与依赖解析守卫），证明测试能捕获该问题，防止假绿。
4. **数据安全测试**：凡涉及可能改动用户目录的路径，必须有 marker 守卫——断言用户既有内容（真实 node_modules、自定义配置等）原样保留、链接与目录属性不被改变。
5. **端到端验证**：影响启动/加载链路的修改，必须在隔离环境真实启动 DSH 并探活（如 `router/catalog`），不能只验证脚本退出码。
6. **回退安全护栏**：任何"删除后重来"的流程（链接失败回退拷贝等），删除后必须确认目标已移除再继续，防止把源码/数据递归写入自身；护栏逻辑同样纳入测试。
7. **验证充分才允许提交与发布**：以上全部通过、`node tests\smoke.mjs` 全绿后方可提交；发布版本前需在对应平台（无法本机验证的平台在 CI 验证）复跑安装入口测试。

## 历史修复原则

- 发现某条提交混入多个无关变更：**未推送**时可 `git reset --soft` 重拆后再逐条提交；**已推送**则不再重写历史，在后续提交中说明即可。
- 重写只限本地未发布的历史；`origin/main` 上的记录不可变。

## 历史教训

- 2026-08：曾把「无头 CLI 子代理功能」与「图片生成可靠性修复」混为一条提交（`e256111`），且功能完成后未及时提交、积压多轮。教训：功能完成的当下就提交；独立修复单独提交。本文件由此而立，后续开发务必遵守。
- 2026-08：在线安装命令 `irm|iex` 曾因 install.ps1 带 BOM 在 param 块报 InvalidLeftHandSide（v0.1.3 修复）；随后又暴露"裸源码无 node_modules 导致插件依赖解析失败、击穿 DSH 启动"（v0.1.4 修复）。教训：安装入口的验证不能只覆盖本开发机（开发机的依赖 junction 掩盖了缺陷），必须构造隔离的"新用户环境"复现。上述「高风险修改纪律」由此两条事故固化。
