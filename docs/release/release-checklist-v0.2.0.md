# v0.2.0 发布检查清单（Release Checklist）

| 项 | 值 |
|---|---|
| 发布版本 | v0.2.0（2026-08-20） |
| 上一版本 | v0.1.7（2026-08-17 tag） |
| 任务 | DEV-003（DEC-015 用户确认升级 v0.2.0） |
| 检查日期 | 2026-08-20 |
| 依据 | MIG-001 完结（EV-011~023，R1-R14 审查链）；DEC-015；release-checklist SKILL 六步 |
| 生命周期状态 | `lifecycle_state: released`（2026-08-20：发布 commit 即本提交（含 bump/CHANGELOG/README/docs.release/治理终态），tag `v0.2.0` 指向该 commit——candidate→released 转换随发布提交完成；无 release-ledger 工具，文字标注机制） |
| 关联产出物 | `CHANGELOG.md` / `docs/release/rollback-plan-v0.2.0.md` / 版本决策记录（本文档末尾） |

---

## 第一步：发布范围确认

| # | 检查项 | 通过标准 | 状态 | 说明 |
|---|---|---|---|---|
| 1 | 版本号已定义 | 遵循语义化版本（major.minor.patch） | ✅ PASS | v0.2.0；0.1.8 → 0.2.0 为 MINOR bump（理由见「版本决策记录」） |
| 2 | 变更范围已列出 | 所有变更项有清单（commit 列表） | ✅ PASS | v0.1.7 tag 以来 main 全部提交：治理接入（04f1e67 / 7c66e50 锁文件 untrack）+ v3 迁移 Step 1-10（7cb2024 / b7261d5 / a23b338 / 374edfa / f89b8bd / f294c3c / 98f04a3 / 2c4b194 / 1f17ea8 / 12a8c71 / e88dfb2 / 0554c5d / 53c0e40，含 9f485d9 快照）+ v0.1.8 原定范围（c2648d2 / 963b4f5）；清单见 CHANGELOG.md |
| 3 | 变更类型已标注 | 每项变更标注：新功能 / 修复 / 优化 / 破坏性变更 | ✅ PASS | CHANGELOG.md 五节齐全：破坏性变更 / 新增 / 变更 / 修复 / 已知问题 |
| 4 | 发布时间窗口已确定 | 有明确的发布时间计划 | ✅ PASS | 2026-08-20（DEC-015 用户确认）；发布后 48h 观察期（见第五步） |

## 第二步：变更日志检查

| # | 检查项 | 通过标准 | 状态 | 说明 |
|---|---|---|---|---|
| 1 | CHANGELOG 已更新 | 覆盖本次所有变更（commit 列表与 changelog 对照一致） | ✅ PASS | CHANGELOG.md 新建；每条变更可追溯（内部注解含提交号 / EV 编号：EV-011~023、Step 1-10 提交号） |
| 2 | 破坏性变更已高亮 | 不兼容变更有明确说明和迁移指引 | ✅ PASS | 2 项（整轮路由移除 / 路径清单注入规则移除），均带行为对比 + 迁移指引 |
| 3 | 依赖变更已记录 | 新增或升级的依赖有版本号和变更原因 | ✅ PASS | 无新增/升级 runtime 依赖（sharp 为 peerDep、tesseract 为系统二进制——RES-002 审查结论，DEC-006；本次迁移零新依赖，已在 CHANGELOG 摘要层注明） |
| 4 | 已知问题已列出 | 已知但未修复的问题有说明和 workaround | ✅ PASS | D-1 待实测项（含测量方法）+ P3 记录项（m4a/mkv 展示级误判等）+ 统计内存态 |

## 第三步：回滚方案验证

| # | 检查项 | 通过标准 | 状态 | 说明 |
|---|---|---|---|---|
| 1 | 回滚方案已编写 | 有具体的回滚步骤（不是泛化描述） | ✅ PASS | `docs/release/rollback-plan-v0.2.0.md`：三层——安装态（用户侧重装 v0.1.7 tarball）/ 开发态（git revert 迁移范围或 checkout v0.1.7）/ kill-switch（总开关 router.enabled=false） |
| 2 | 回滚方案已验证 | 在测试环境执行过回滚操作，确认可成功 | ✅ PASS | **门控面已验证**：总开关关闭态零介入由 Step 3/6 自动化断言覆盖（EV-013 / EV-019，发布前 smoke 全绿证明）；git 层回滚为机械操作（revert 范围已定义、验证程序=回滚后跑 v0.1.7 基线断言，见回滚方案「验证方式汇总」） |
| 3 | 数据兼容性 | 回滚后数据是否兼容（数据迁移场景必检） | ✅ PASS | 无数据迁移（纯代码发布，可逆发布）；imageMemory 为进程内缓存（重启即清，无持久化残留）；`.router-files/` 上传文件不参与回滚 |
| 4 | 回滚影响范围 | 回滚操作的影响已评估，不会造成额外损失 | ✅ PASS | 回滚丢弃 v0.2.0 全部行为变更与新能力，回到 v0.1.7 旧机制（含旧已知问题）；agent/账号配置存于 DSH settings 不随插件回滚丢失；治理记录（.governance/）如随 revert 回退可单独恢复（见回滚方案注意事项） |

## 第四步：发布后验证计划

| # | 检查项 | 通过标准 | 状态 | 说明 |
|---|---|---|---|---|
| 1 | 核心功能验证清单 | 列出发布后需要验证的核心功能项 | ✅ PASS | 见下表 |
| 2 | 监控指标基线 | 发布前的关键指标已记录（用于对比） | ✅ PASS | smoke.mjs 534 断言 + metrics.mjs 31 项观测 + D-1 门判定（满足×2 / 部分满足×2 / 待实测×1）为发布前基线（EV-023） |
| 3 | 告警规则就绪 | 发布相关的监控告警已配置 | ✅ PASS | 不适用——本地插件（无独立告警体系）；以观察期人工核对替代 |
| 4 | 验证责任人 | 每项验证有明确的负责人 | ✅ PASS | Coordinator（会话内验证）；真实使用指标（U-3）由用户日常使用提供 |

### 核心功能验证清单（发布后）

| # | 验证项 | 验证方式 | 责任人 |
|---|---|---|---|
| 1 | 安装链路 | 在线/离线安装命令按 v0.2.0 版本号执行成功；DSH 重启后设置页出现路由配置（tarball 离线安装验证属 DEV-003 执行段） | Coordinator |
| 2 | 冒烟基线 | `node tests/smoke.mjs` → ALL PASSED（534 断言） | Coordinator |
| 3 | D-1 观测 | `node tests/metrics.mjs` → 31 项观测全过 + 门判定输出 | Coordinator |
| 4 | 图片通路 | 带图消息 → 主 agent 轮次 + `route_agent` 委派（直传分支：主模型有视觉；改写分支：无视觉 marker+reminder） | Coordinator |
| 5 | 跨轮指代 | 图片轮后文本轮追问"刚才图里 X"→ imageMemory 记忆段命中 | Coordinator |
| 6 | 任意文件上传 | 音频/视频/文档上传 → 附件卡片 + 路径文本 + 打开文件/内联播放 | Coordinator |
| 7 | 总开关（kill-switch） | `router.enabled=false` → route_agent 拒绝调用、统计暂停、恢复即还原 | Coordinator |

## 第五步：数据验证计划（无数据替代标准）

**适用判定**：小项目（本地个人插件，无可量化用户指标、无 A/B 分流条件）→ 采用 release-checklist SKILL「无可量化用户指标的小项目」替代标准：

- **核心功能冒烟通过**：`node tests/smoke.mjs` 534 断言全绿（发布前已达成，EV-023）。
- **D-1 观测脚本**：`node tests/metrics.mjs` 31 项自动化观测全过 + 门判定输出（发布前已达成，EV-023）。
- **无新 bug 报告 48 小时**：发布后观察期（≥48h）内无 P0/P1 级新问题报告。
- **回滚触发条件**（发布后 48h 观察期）：
  1. 出现 P0/P1 级新 bug（回归、崩溃、核心通路失效）；
  2. 端到端图片通路不可达（D-1-2 真实使用反馈：图片未到达视觉输入）；
  3. `route_agent` 带图触发率首轮 U-3 统计 <90% 且无缓解手段（D-1-3）。
- **成功标准（量化）**：单机自动化部分 100% 通过（已达成）；真实使用部分（D-1-2 / D-1-3 / D-1-5）在观察期采集首轮样本并记录基线，作为后续版本对比数据。

## 第六步：发布决策

| 决策 | 条件 | 状态 |
|---|---|---|
| **可以发布** | 全部检查项通过，数据验证计划就绪 | ✅ **可以发布** |

**决策理由**：

1. MIG-001 完结——全 13 单元（Step 0-10 含 5a/5b/5c）实施闭环，每步独立提交 + 测试全绿 + 独立审查（EV-011~023）；
2. R1-R14 独立审查链全部通过（终态 APPROVED_WITH_NOTES，`unresolved_blockers=0`，含两次 NEEDS_CHANGE → 返工 → 复审闭环）；
3. D-1 门判定：满足×2（恒主模型 / 编址往返，自动化 100%）+ 部分满足×2（图片到达 / 跨轮指代，机制面 100%）+ 待实测×1（触发率）——待实测项均有明确测量方法且机制面就位，发布风险可接受（EV-023）；
4. DEC-015 用户确认：解除发布暂停并升级 v0.2.0（2026-08-20）；
5. 回滚方案就绪且可逆发布（无数据迁移），回滚 <5 分钟（门控面已有自动化断言支撑）。

**遗留项（不阻塞发布，观察期跟进）**：D-1 端到端待实测（②图片到达 / ③触发率 / ⑤成功率——真实使用后采集）；P3 记录项若干（转 DEV-002 域）；R14-F-01 测试卫生（转 DEV-002）。

---

## 版本决策记录（0.1.8 → 0.2.0，semver MINOR bump）

| 项 | 值 |
|---|---|
| 决策编号 | 随 DEC-015（2026-08-20 用户确认） |
| 决策 | 版本号直接升 **v0.2.0**（跳过 v0.1.8 号） |
| bump 级别 | **MINOR**（0.1.8 → 0.2.0，主版本 0 不变，次版本 1 → 2） |
| 理由 | ① 行为变更：整轮路由移除（带图轮不再自动切换视觉模型）——非兼容性修复，按语义化版本属行为级变更；② 大量新能力：N-1~N-9 十项新增机制（附件统一寻址、imageMemory、任意文件上传、三级展示、模态矩阵等）——远超 patch 级；③ v0.1.8 原定范围（whole-turn 图片路由默认化，c2648d2 / 963b4f5）已被 v3 架构演进整体超越，0.1.8 号与实际变更量严重失配（DEC-015） |
| 不升 major 的依据 | 插件公开接口保持兼容：`route_agent` 工具调用形态不变、配置 schema 兼容（老配置 type=image 或 capabilities 含 image 双轨零破坏，DEC-012 W-5）；破坏性行为变更在 CHANGELOG 中醒目标注并附迁移指引（语义化版本 2.0.0 对 0.x 版本要求） |
| 决策记录 | DEC-015（decision-log，2026-08-20）：C1 解除发布暂停并发布 v0.2.0；RISK-002（架构冲突）随迁移完结关闭 |
| 状态 | 已执行（本发布） |
