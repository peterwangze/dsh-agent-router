# 风险记录（risk-log）

| 编号 | 识别日期 | 风险描述 | 影响 | 概率 | 等级 | 缓解措施 | 触发条件 | 责任人 | 状态 | 最后更新 |
|---|---|---|---|---|---|---|---|---|---|---|
| RISK-001 | 2026-08-18 | 测试仅 4 个冒烟文件且无 CI，多模态路由近期改动（whole-turn 默认化、takeover、image-to-image）缺回归保护 | 回归缺陷可能随发布流出 | 中 | 中 | DEV-002 补核心通路自动化测试（现含基线观测常态化——DEC-017）；CI 缺口纳入演进路线评估（RES-003 方向 C） | v0.1.8 发布（DEV-003）前未完成 DEV-001 | Coordinator | 活跃（**2026-08-20 观察期义务已履行（DEC-017，提前于 48h 截止）**：DEV-001 经用户方向授权裁决为重定义关闭——基线对象已随 v3 Step 1 移除，534 smoke 断言 + 31 项 D-1 观测构成现行基线，观测常态化并入 DEV-002。回归保护主轨道 = DEV-002 + RES-003 演进路线；新升级条件：下一版本发布前 DEV-002 未完成，或 DEV-002 长期搁置（>2 周））。**2026-09-12 状态更新（REL-012/v0.5.0 发布链）**：CI 缺口**已接线**（EVO-024 B6：`.github/workflows/ci.yml` 单 job + 门控单入口 `node tests/run-all.mjs`，随 v0.5.0 push 生效）且**首跑已产出信号**——run 34696694673 = **failure**（19/20 套件通过；失败 = Windows 专属测试臂在 ubuntu 运行器执行 + bare 夹具 ENOENT，属测试基础设施平台容错覆盖不足，非产品缺陷）→ **FIX-038 立案**（修复后复跑达 CI 绿 = 本风险主轨道闭合条件）；v0.5.0 发布说明已如实披露首跑未通过（不声称 CI 通过） | 2026-09-12 |
| RISK-002 | 2026-08-18 | 架构级（用户报告）：当前方案中专业 Agent 参与主 agent 轮次的设计与 DSH 宿主约束冲突（专业 Agent 只能作为被主 agent 调用的工具）；图片附件→提示词注入转换可能失效，主 agent 完全忽略附件图片资源 | 多模态路由核心通路不可用/不可靠；v0.1.8 发布已暂停 | 高 | 高 | RES-001 重新调研（问题定位 + 修正方案 + 替代方案对比）；DEV-003 阻塞直至结论 | RES-001 未产出结论前任何发布动作 | Coordinator | **已关闭（2026-08-20，DEC-015 用户确认）**：v3 迁移 MIG-001 全 13 单元闭环（R1-R14 审查链）——整轮路由已移除（问题①）/preserveImageInput 分级改写+M2 编址+pre-step 三通道（问题②）/D-1 机制面观测全过；端到端待实测项已留测量方法（真实使用中持续验证） | 2026-08-20 |
| RISK-003 | 2026-08-21 | 宿主接口演进：DSH 宿主包（dsh-llm 等）版本更新引入 adapter 接口新增（实证：prepareCall），本插件手工镜像对象（twin adapter）缺新方法即全量断裂——FIX-001 坐实一次（宿主新增 prepareCall → 接管路由 TypeError，用户报多模态消息全部失败） | 宿主更新后多模态接管路由不可用直到热修；用户感知为"路由出问题" | 中 | 高 | ① 接口奇偶回归测试（枚举宿主 adapter 基类方法 vs twin 实现面——FIX-001 落地）；② 宿主更新后跑全量测试 node tests/*.mjs；③ 症状知识库：registration.adapter.X is not a function = 手工镜像缺方法 | 宿主 node_modules 变更后首次接管路由调用 | Coordinator | **活跃**——FIX-001 修复+奇偶测试派发中；宿主更新无预警通道（npx cache 静默刷新），依赖测试看护**｜**2026-09-12 状态同步（ARCH-004 链闭合）：**风险保持活跃**（宿主持续演进，不关闭）但**缓解已结构化落地**——依赖最小化（删死依赖 + 声明面锚定实测基线 0.1.5-rc.2）/ 解耦单点（`lib/host-abi/` 分域适配层替代散点直取）/ **严格校验与看护**（`tests/host-contract.mjs` 契约快照四类面 + 声明面比对 + 消费点黑名单 + 字段级 schema + 事件白名单；`host-version-snapshot.mjs` 基线快照；域行为判别）/ 边界可调测性（面级降级 + `router/hostFaceDiagnostics` 诊断面板与徽章）；**预警时机** = 每次 push 经 CI 复跑（FIX-038 修复后达绿）+ 宿主升级后本地门控；**残余风险如实保留**：宿主 ABI 面外的新形态（纯运行时/时序依赖面）仍可能未被静态守卫覆盖（见 README 边界清单） | 2026-09-12 |
