# 本地依赖 junction（冒烟测试用，指向 profiles 依赖树，不可提交）
node_modules/
*.log


# 运行时下载目录（files URL 落盘）与测试产物
.router-files/

# 安装脚本测试目录
.test-home/

# 发布产物
*.tar.gz

# 调研参考源码（npm pack 拉取，不入库）
.tmp-research/

# CLI 冒烟测试临时目录
.tmp-cli-smoke-*/

# 判别测试残留 DSH_HOME（P2-2：已改 mkdtemp 临时目录 + finally 清理；本条为残留防御）
.tmp-fix0*-home/

# 检查工具目录
.inspect-vision-router/

# 治理运行时锁状态（会话级瞬态，不入库；其余 .governance/ 治理记录入库）
.governance/agent-locks.json

# release tarballs (build artifacts — regenerate via npm pack, upload to GitHub releases)
*.tgz
